;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스구상 콤비네이션계 액션구상파일
;
;	콤비네이션계 액션구상를 정리한 파일
;
;Version		Reason
;1. 71			@구상기입 체크 사양에. 그 외 섬세한 수정
;1. 68 Rel2		일부 개변
;1. 60			단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;콤비네이션계 ACT300～312
;========================================================================
;·TFLAG:ACT(300=Ｗ수음/301=Ｗ펠라/302=Ｗ키스/303=Ｗ스마타/304=Ｗ파이즈리/305=Ｗ풋잡
;·306=기승위이중공격/307=대면이중공격/308=변칙흐트러진모란/309=항문이중공격/310=Ｗ역강간/311=골짜기/312=이중이라마치오)
;·조수가 존재하는 경우,50%의 확률로 콤비네이션에 파생한다
;·TFLAG:ACT가 GET_COMBIACTNUM(TFLAG:ACT)로 덧쓰기되어 함수내에서 ACT번호가 정의된다
;·콤비네이션시의 ASSI는 반드시(ASSI:1)된다
;·ACT_ABLE는 기본적으로 파생 바탕으로 준거
;-------------------------------------------------
;좀처럼손가 돌지 않는 개소. 초기의 무렵에 써 그대로
;지금은 사용하지 않게 된 조건이라든지 비교적 있구나w
;-------------------------------------------------
;────────────────────────────────────
;ACT300, Ｗ수음
;────────────────────────────────────
;·조교자와조수의 2명이조교대상에손코키 한다
;·ACT10, 손으로 애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_300
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT300, Ｗ수음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;절정로 이끌리고
	;=================================================
	;IF 구상기입 체크(0) && TCVAR:MASTER:애태움도 == 1
	;ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 반응을 물으면서 잡은 육봉을 상하시키고
	;절정직전에 애태우고 있다…
	;=================================================
	;애태움중
	;=================================================
	;IF 구상기입 체크(0) && TCVAR:MASTER:애태움도 > 1
	;ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 「있잖아、우리들의 손으로 농락당하는 건 어떤 기분이야…？」 
		PRINTFORMW 「후후… 아무것도 말할 수 없을 정도로、기분이 좋아져버린 모양이네」
		RETURN 1
	ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「저기 %CNAME(ASSI)%、봐 줘」
		PRINTFORMW 「이 변태 씨는 우리들이 문질러주기를、기다릴 수 없게 된 모양이야？」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「에잇、나랑 %조수가% 문질러 줄게」
		PRINTFORM 「마지막 한 방울까지 짜내 줄 테니까… 
	ELSE
		PRINTFORMW 「저기、%CNAME(ASSI)%도 도와줘」
		PRINTFORM 「우리들 손으로 가서 미쳐버리게 하자…
	ENDIF
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT301, Ｗ펠라
;────────────────────────────────────
;·조교자와조수의 2명이조교대상에펠라티오를 실시한다
;·ACT12, 입으로 애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_301
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT301, Ｗ펠라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 사이에 두어 혀를 얽히게 해라
	;사정를 촉했다…
	;=================================================
	;절정로 이끌리고
	;=================================================
	;IF 구상기입 체크(0) && TCVAR:MASTER:애태움도 == 1
	;ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 중심으로 졸졸 혀를 기 조생
	;절정직전에 애태우고 있다…
	;=================================================
	;애태움중
	;=================================================
	;IF 구상기입 체크(0) && TCVAR:MASTER:애태움도 > 1
	;ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「어라、그렇게 발기하고는… 빨아줬으면 하는 거야？」 
		PRINTFORMW 「%조사처리(CNAME(MASTER, TARGET),"는")% 누구한테 빨아줬음 하는 걸까」
		PRINTFORMW 「나？ %CNAME((ASSI:1), TARGET)%？ 그게 아니면……」
		PRINTFORM 「두사람 동시에 해 주길 바라는 걸까…
		CALL PRINT_SYMBOL_K14 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 서로 경쟁하도록(듯이) %CALLNAME:MASTER%의 남성기에 혀를 기게 했다…
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「나랑 %조수가% 빨아줄게♪」 
		PRINTFORMW 「사양말고 마음껏 가버려도 좋다구？」 
	ELSE
		PRINTFORMW 「자아、%조수랑% 둘이서 뽑아줄게」 
		PRINTFORMW 「우리들의 혀 기술을 즐겨줘♪」 
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT302, Ｗ키스
;────────────────────────────────────
;·조교자와조수의 2명이조교대상에키스를 실시한다
;·ACT15, 키스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_302
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT302, Ｗ키스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;조교대상삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「잠깐、언제까지 %조수랑% 키스할 거야！」
				PRINTFORMW 「%조사처리(CNAME(MASTER, TARGET),"는")% 지금 나와 하고 있는 거니까、조금은 생각해 달란 말얏！」
				PRINTFORMW 「저기 %CNAME(ASSI)%、당신도 일부러 그러고 있는 거 아냐？」
			CASEELSE
				PRINTFORMW 「읏……츄읏……자、좀 더 음란하게 혀를 휘감아 줘…」
				PRINTFORMW （%CNAME(ASSI)%에게 보여줄 거야。후후…）
				PRINTFORML 「어라、%CNAME(ASSI)%。따분하게 해버렸으려나…？」
				PRINTFORMW 「미안하지만、조금 기다려줘。우후후…」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;누움
	;=================================================
	IF 구상기입체크(100) && TFLAG:누움
;OriginalString : 「%KOJO_RAND_K14(("키스"), 1)%」
		PRINTFORMW 「%KOJO_RAND_K14(("키스"), 1)%」
		CALL PRINT_KOJO_COLORW(ASSI, "『츗… 응츗… 츄웃…』", TARGET)
;OriginalString : 「楽しいわね♪」
		PRINTFORMW 「즐겁네요♪」
		CALL PRINT_KOJO_COLORW(ASSI, "『에에, 정말로♪』", TARGET)
;OriginalString : 
		PRINTL
;OriginalString : 「ウフフ…」
		PRINTFORMW 「우후후…」
		CALL PRINT_KOJO_COLORW(ASSI, "『후후…』", TARGET)
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「자아、나랑 %조수가% 키스 해줄게♪」 
		PRINTFORMW 「모두에게 사랑받는 %CNAME(MASTER, TARGET)% 행복한 사람이네？ 후훗…」
	ELSE
		PRINTFORMW 「있잖아、%CNAME(ASSI)%도 키스해줘？」
		PRINTFORM 「우리들의 키스로 황홀하게 해 주자…
		CALL PRINT_SYMBOL_K14 
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;────────────────────────────────────
;ACT303, Ｗ스마타
;────────────────────────────────────
;·조교자와조수의 2명이조교대상에스마타를 실시한다
;·조교대상에페니스가 있는 것이 전제
;·ACT16, 성기를 서로 비빈다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_303
;-------------------------------------------------
;·지문은조교대상가누움인가 아닌가로 분기.
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT303, Ｗ스마타
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 음순으로 %CALLNAME:MASTER%의 남성기를 끼워 넣어 켜기 시작했다…
	;=================================================
	;조교대상가누움
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:누움
	;ENDIF
ENDIF
[SKIPEND]

;────────────────────────────────────
;ACT304, Ｗ파이즈리
;────────────────────────────────────
;·조교자와조수의 2명이조교대상에파이즈리를 실시한다
;·조교대상에페니스가 있는 것이 전제
;·ACT17, 파이즈리한다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_304
;-------------------------------------------------
;·지문은애태움도에 의해 분기. 각 분기내에서조교자＆조수의가슴의 사이즈에 의한 파생
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT304, Ｗ파이즈리
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생원의파이즈리자체가 발생하지 않기 때문에 사실상의 죽음에 구상가 되어 있다고 한다
;앨리스씨손와발 이외를 사용하는 것은싫어한다로부터w
;------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;조교대상가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;누움
		IF TFLAG:누움
			;갈 것 같은 것을참다 하고 있다		지금은 그다지 사용하지 않게 된 조건
			IF TCVAR:MASTER:가버려 > 5
				PRINTFORMW 「의외로 잘 견디네？ 뭐 쓸데없는 발버둥이지만♪」
				PRINTFORMW 「저기 %CNAME(ASSI, TARGET)%。좀 더 격렬하게 해줘」
				PRINTFORMW 「우리들을 상대로 참다니、싱거운 변태에게는 뼈저리게 느끼게 해줄게♪」 
			;갈 것 같다
			ELSE
				PRINTFORMW 「슬슬 한계인 것 같네？」 
				PRINTFORMW 「있잖아、%조사처리(CNAME(MASTER, TARGET),"는")% 어느 쪽 가슴을 더럽히고 싶어？」
				PRINTFORMW 「나？ 아니면 %CNAME(ASSI, TARGET)%？」
				PRINTFORMW 「뭐어 좋아。일단은 편하게 해줄게♪」
			ENDIF
		;통상
		ELSE
			;갈 것 같은 것을참다 하고 있다
			IF TCVAR:MASTER:가버려 > 5
				PRINTFORMW 「필사적으로 참고 있는데 미안하지만…」
				PRINTFORMW 「우리들은 %조사처리(CNAME(MASTER, TARGET),"가")% 가는 모습을 어서 보고 싶은걸」
				PRINTFORMW 「자아 %CNAME(ASSI, TARGET)%、얼른 짜내 버리자♪」
			;갈 것 같다
			ELSE
				PRINTFORMW 「두사람에게 괴롭혀져서、이제 참을 수 없게 된 모양이네？」 
				PRINTFORMW 「저기이 %CNAME(ASSI, TARGET)%。어떻게 할까？」
				PRINTFORMW 「이대로 보내 버릴까、아니면 애태우게 할까…」
				CALL PRINT_KOJO_COLORW(ASSI, "『우후후…』")
				PRINTFORMW 「우후후…」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 「에잇、좀 더 허리를 흔들어！」  
		PRINTFORMW 「%CNAME(ASSI, TARGET)%도 어리광을 받아주면 안 돼？ 우후후…」
		RETURN 1
	ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「어라 어라、그렇게나 빳빳히 세우고서는…
		PRINTFORMW 「그렇게나 우리 가슴을 범하고 싶은 걸까？」
		PRINTFORMW 「저기 %CNAME(ASSI, TARGET)%、어떡할래？ 이거」
		CALL PRINT_KOJO_COLORW(ASSI, "『우후후…』")
		PRINTFORMW 「우후후…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「나랑 %CNAME(ASSI)%의 가슴으로 샌드위치 해줄게」 
		PRINTFORMW 「%조사처리(CNAME(MASTER, TARGET),"는")% 어느 가슴이 마음에 드려나？ 우훗…」
	ELSE
		PRINTFORMW 「저기、나랑 %CNAME(ASSI)% 중 누구 가슴에 끼우고 싶어？」
		PRINTFORMW 「어라、둘 다 한번에가 좋아…？ 그것도 나쁘지 않네♪」 
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT305, Ｗ풋잡
;────────────────────────────────────
;·조교자와조수의 2명이조교대상에풋잡하기
;·조교대상에페니스가 있는 것이 전제
;·ACT18, 풋잡하기로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_305
;-------------------------------------------------
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 협력해 %CALLNAME:MASTER%의 남성기를발로 훑어 냈다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT305, Ｗ풋잡
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교대상가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:가버려
		;누움
		IF TFLAG:누움
			;갈 것 같은 것을참다 하고 있다
			IF TCVAR:MASTER:가버려 > 5
				SELECTCASE AUTORAND(2, 650)
					CASE 0
						PRINTFORMW 「어라 어라、열심히네에。참아봤자 어차피 곧 가버릴 주제에」
						PRINTFORMW 「저기、아직 싸고 싶지 않아？ 우리 다리를 정액으로 끈적끈적하게 만들고 싶지 않아？」 
						PRINTFORMW 「후훗、좋아。자아 %CNAME(ASSI)%、슬슬 이 변태에게 일격을 날리자♪」
					CASE 1
						PRINTFORMW 「의외로 끈질기네？ 벌써 쿠퍼액으로 번들번들해졌는데도」
						PRINTFORMW 「저기 %CNAME(ASSI)%。좀 더 강하게 해 줘」
						PRINTFORMW 「그러는 편이 이 변태는 더 좋은 거야♪ 우훗…」
				ENDSELECT
			;갈 것 같다
			ELSE
				SELECTCASE AUTORAND(2, 651)
					CASE 0
						PRINTFORMW 「%CNAME(MASTER, TARGET)%、자기 허리가 움직이고 있는 것도 눈치채지 못한 거야？」 
						PRINTFORML 「저기 %CNAME(ASSI)%、발을 멈춰。이 변태에게 스스로 하도록 하자」
						PRINTFORMW 「이봐、스스로 허리를 흔들어보라구！ 우리들 발이랑 섹스할 생각으로 하란 말이야」
					CASE 1
						PRINTFORMW 「있잖아、왠지 평소보다 딱딱한 기분이 드는데？」
						PRINTFORMW 「설마 %CNAME(ASSI)%의 발이 더 좋았다거나 그런 말은 아니겠지…」
						PRINTFORMW 「이봐、진짜야？ 제대로 말하지 않으면 보내주지 않을 거야？」
				ENDSELECT
			ENDIF
		;통상
		ELSE
			;갈 것 같은 것을참다 하고 있다
			IF TCVAR:MASTER:가버려 > 5
				SELECTCASE AUTORAND(2, 652)
					CASE 0
						PRINTFORMW 「참고 있는 걸 보니 괜히 괴롭히고 싶어지네…」
						PRINTFORMW 「저기 %CNAME(ASSI)%。조금 격렬하게 하자♪」
					CASE 1
						PRINTFORMW 「저기、%CNAME(ASSI)%、잘 봐 둬。이렇게 하면 이 변태는 기뻐하는 걸」
						PRINTFORMW 「봐봐、부들부들해서 막 폴발할 것 같지？」
						PRINTFORMW 「자、당신도 똑같이 해봐。슬슬 최후의 일격을 날리는 거야♪」
				ENDSELECT
			;갈 것 같다
			ELSE
				SELECTCASE AUTORAND(2, 653)
					CASE 0
						PRINTFORMW 「저기、나랑 %CNAME(ASSI)%의 발、어느쪽이 더 기분 좋아？」
						PRINTFORMW 「솔직하게 말하면 바로 가게 해줄게…？」
						PRINTFORMW 「대답에 따라서는 벌이 필요할지도 모르겠지만 말얏！」
					CASE 1
						PRINTFORMW 「방금 전부터 계 %CNAME(ASSI)%의 고간만 보고 있지 않아？」
						PRINTFORMW 「저기、듣고 있어？ 이봣、이쪽을 보라고 이 변태！」
				ENDSELECT
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;밀어넘어짐상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:밀어넘어지는중
		;연속실행
		IF TFLAG:ACT == TFLAG:전ACT
			PRINTFORMW 「넘어뜨려진 채 우리들한테 장난감 취급 당해서、괜히 흥분해버린 것 같네？」
			PRINTFORMW 「후후… 이건 간단히 보내버려서야 재미가 없으려나」
			PRINTFORMW 「저기 %CNAME(ASSI)%。천천히 가지고 놀아 주자♪」
		;通常
		ELSE
			PRINTFORMW 「우훗、움직이기 못하게 한 다음에…」
			PRINTFORMW 「나랑 %조수가% 철저하게 쥐어 짜내줄게♪」 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		SELECTCASE AUTORAND(3, 654)
			CASE 0
				PRINTFORMW 「여자 두명에게 발길질당해서 흥분해버린 거야？」
				PRINTFORMW 「정말이지 기가 막히는 변태네！」
				PRINTFORMW 「저기 %CNAME(ASSI)%。이 변태는 당신 발도 마음에 든 모양이야？」
				PRINTFORMW 「우후후…」
				CALL PRINT_KOJO_COLORW(ASSI, "『쿡쿡…』")
			CASE 1
				PRINTFORMW 「그렇네에、풋잡 정말 좋아하는 변태에게는 참을 수 없겠네에？ 이거」
				PRINTFORMW 「어라 어라、솔직하게 수긍하기는。정말、어쩔 수가 없네♪」
				PRINTFORMW 「에잇、나랑 %CNAME(ASSI)%의 발을 좀 더 즐기게 해줄게。 우후후…」
			CASE 2
				PRINTFORMW 「어라 어라、두사람에게 훑어내져서  굉장히 행복해 보이네？」
				PRINTFORMW 「자、%CNAME(ASSI)%도 저 한심한 얼굴을 잘 보도록 해♪」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「저기 %CNAME(ASSI)%、저걸 봐」 
		PRINTFORMW 「우리 발로 문질러 주기를 기다릴 수 없던 모양이야？」
		PRINTFORMW 「우후후…」
		CALL PRINT_KOJO_COLORW(ASSI, "『후후…』", TARGET)
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE AUTORAND(2, 659)
		CASE 0
			PRINTFORMW 「자아、나랑 %CNAME(ASSI)%의 발로 문질러 줄게」
			PRINTFORMW 「풋잡 좋아하는 변태인 %플레이어는% 참을 수 없겠지？ 우후후…」
		CASE 1
			PRINTFORMW 「저기、%CNAME(ASSI)%도 도와줘」
			PRINTFORMW 「이 변태를 우리들 발로 승천시키는 거야」
			PRINTFORMW 「자아、각오해。나랑 %조수가% 한계까지 짜내 줄 테니까♪」 
	ENDSELECT 
	RETURN 1
ENDIF

[SKIPSTART]
in Mothball v1. 71
;────────────────────────────────────
;ACT306, 승마 두 구멍 고문
;────────────────────────────────────
;·기승위중의조교대상에조수가Ａ삽입 한다
;·ACT32, 기승위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_306
;-------------------------------------------------
;ACT파생에의 대입은 TEQUIP를 참조해 행해지고 있다
;예를 들면 ACT파생 1등의 경우, 전회ACT가기승위라고는 할 수 없기 때문에 주의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT306, 승마 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0)
	;=================================================
	;연속(기승위로부터)
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
	ENDIF
	;=================================================
	;체위 변경(Ｖ성교계로부터)			페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 2
	ENDIF
	;=================================================
	;Ａ성교로부터						페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	ENDIF
	;=================================================
	;Ｖ성교봉사계로부터					0203판 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	ENDIF
	;=================================================
	;Ａ성교봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
	ENDIF
	;기타·조건없음
ENDIF

;────────────────────────────────────
;ACT307, 대면두 구멍 고문
;────────────────────────────────────
;·조교자가조교대상의Ｖ에삽입했다뭐, 조수가조교대상의Ａ에삽입 한다
;·ACT32, 대면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_307
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT307, 대면두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0)
	;=================================================
	;연속(대면좌위로부터)
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
	ENDIF
	;=================================================
	;체위 변경(Ｖ성교계로부터)
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 2
	ENDIF
	;=================================================
	;Ａ성교로부터						페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	ENDIF
	;=================================================
	;Ｖ성교봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	ENDIF
	;=================================================
	;Ａ성교봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
	ENDIF
	;기타·조건없음
ENDIF

;────────────────────────────────────
;ACT308, 변칙흐트러진모란
;────────────────────────────────────
;·조교대상의Ｖ에배면좌위로삽입하면서, 조수에 결합부의 상황을 실황시킨다
;·ACT33, 배면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_308
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT308, 변칙흐트러진모란
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0)
	;=================================================
	;연속(배면좌위로부터)
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
	ENDIF
	;=================================================
	;체위 변경(Ｖ성교계로부터)
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 2
	ENDIF
	;=================================================
	;Ａ성교로부터						페니스밴드에서의Ａ성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 3
	ENDIF
	;=================================================
	;Ｖ성교봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
	ENDIF
	;=================================================
	;Ａ성교봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
	ENDIF
	;기타·조건없음
ENDIF

;────────────────────────────────────
;ACT309, 항문두 구멍 고문
;────────────────────────────────────
;·조교자가조교대상의Ａ에삽입했다뭐, 조수가조교대상의Ｖ에삽입 한다
;·조교대상이남자의 경우는 발생하지 않는다
;·ACT33, 항문섹스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_309
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT309, 항문두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0)
ENDIF
[SKIPEND]

;────────────────────────────────────
;ACT310, Ｗ역강간
;────────────────────────────────────
;·조교자와조수로, 조교대상의 페니스를 교대로삽입 하고 즐긴다
;·ACT103, 역강간로부터의 파생
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_310
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT310, Ｗ역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;연속(역강간로부터)
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 1
	;ENDIF
	;=================================================
	;체위 변경(Ｖ성교봉사로부터)
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:ACT파생 == 2
	;ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE AUTORAND(2, 670)
		CASE 0
			PRINTFORMW 「자아、%CNAME(ASSI)% 어서와」 
			PRINTFORMW 「둘이서 범해버리는 거야♪ 우훗…」 
		CASE 1
			PRINTFORMW 「이봐、나랑 %조수가% 범해 줄게」 
			PRINTFORM 「둘이서 한계까지 짜내 줄 테니까…
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT311, 골짜기
;────────────────────────────────────
;·3Ｐ의 일종
;·조교자와조수가 상하과 겹쳐져, 조교대상가 두 명의보지를 교대로 범한다
;·ACT95, 정상위시킨다로부터의 파생
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=Ｖ성교봉사계로부터/3=Ａ성교계로부터/4=Ｖ성교계로부터/5=Ａ성교봉사계로부터)
;·0203판 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_311
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT311, 골짜기
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;안빼고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:안빼고
		CALL PRINT_KOJO_COLORW(ASSI, "『아읏…… 앗… 안됏! 그것, 안돼엣…!』", TARGET)
		PRINTFORMW 「%CNAME(ASSI)%의 목소리…이 얼마나 음란한지……」
		PRINTFORMW 「저기、잠깐！ 언제까지 둘이서 할 거야！」
		PRINTFORMW 「나도、방금 전부터 계속…읏후읏…！…앙…그런…갑자기 당해버리면……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF TCVAR:MASTER:가버려
		SELECTCASE TCVAR:MASTER:가버려
			;조교대상가 갈 것 같은 것을참다 하고 있다
			CASE IS > 5
			CASE IS > 5
				PRINTFORMW 「이봐요、좀 더 허리를 흔들어、%조수가% 불쌍하잖아？」
				PRINTFORMW 「어라、갈 것 같아？ 그런 것 같대네、%CNAME(ASSI)%」
				PRINTFORMW 「있는 힘껏 죄어서 짜내주도록 해♪」
			;조교대상がイキそう
			CASE IS > 0
				PRINTFORM 「앗…앙…격렬햇、시、응아앗…
				CALL PRINT_SYMBOL2_K14
				PRINTFORMW 「갈 것 같은…거네？ 저기이、%조사처리(CNAME(MASTER, TARGET),"는")% 어느쪽에 싸 주려나？」
			;通常
			CASEELSE
				PRINTFORMW 「응…앗…！…한창 좋을 때 뽑는 건 괴롭네、이거…」
				PRINTFORMW 「그치만……앙앙 울고 있는 %CNAME(ASSI)%도 귀여워서……」
				PRINTFORMW 「자 %CNAME(ASSI)%、키스하자。츄읏…응……」
		ENDSELECT
	ELSE
		IF AFFECTION("상사상애")
			PRINTFORMW 「자아、좋아。나랑 %조수를% 한번에 범해줘」
			PRINTFORMW 「그치만、처음은 나부터 해야만 해！」
			PRINTFORMW 「%CNAME(ASSI)%에게는 미안하지만、조금 기다려줘。우훗…」
		ELSE
			PRINTFORMW 「자、마음에 드는 쪽부터 찔러 넣어도 좋아…」 
			PRINTFORMW 「%조사처리(CNAME(MASTER, TARGET),"는")% 어느쪽부터 범해 주려나？ 우후후…」 
		ENDIF
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;────────────────────────────────────
;ACT312, 이중이라마치오
;────────────────────────────────────
;·조교자와조수의 페니스에조교대상가봉사 하는플레이
;·이 단계에서는 명령만
;·ACT51, 입으로 애무를 강요로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_312
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT312, 이중이라마치오
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0)
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입 체크(0) && TCVAR:TARGET:가버려
	ENDIF
	;=================================================
	;연속실행
	;=================================================
	IF 구상기입 체크(0) && TFLAG:ACT == TFLAG:전ACT
	ENDIF
	;기타·조건없음
ENDIF
[SKIPEND]
