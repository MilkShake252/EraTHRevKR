;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지구상 조교자행동 전이벤트구상파일
;
;	조교자행동 전이벤트구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 조교자행동 전구상
;========================================================================
;·TRAIN_PROCCES.ERB내, @TRAIN_CANCEL로부터 불려 간다
;·구상주의 파라미터 표시의 직후에 표시. 조교지시는 이미 결정되고 있다
;·조교중지선언의 교환이나, 조교턴 개시전에주인님로부터 한 마디, 와 같이 쓰는 일도 가능
;·오전, 오후의조교개시는 아니고, 조교턴의 개시전에는 매회 불려 가는 개소
;·그 때문에도 있어, 조교중지시와경과시간 0 이외의 개소는 IF 구상기입 체크(25) 등으로서 확률 표시하는 일을 추천
;·조교중지시에게만 지문이 존재한다
;·문장 표시 후에 본체측의 DRAWLINE 처리 있어
;-------------------------------------------------
;FIRSTEVENT(0)～나, FIRSTEVENT(10)～등으로 해서 테두리마다 할당하고 있습니다만
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENT는 사실상 무한하게이벤트번호를 늘릴 수 있으므로
;필요에 응해 임의에 할당해 주세요
;-------------------------------------------------
@KOJO_EVENT_K41_12(ARG)
;-------------------------------------------------
;인수 내역…
;ARG(0=조교 가능/1=구상주의 피로로 중지/2=조교대상의 피로로 중지/3= 포상에 의한 중지)
;-------------------------------------------------

;────────────────────────────────────
;조교중지 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;────────────────────────────────────
;·피로, 혹은 포상에 의한조교중지의 경우의구상
;·기입 체크=1의 경우는구상표시 후 본체측에서 PRINTL의 공행이 들어간다
;·기입 체크를=2로 하면, 지문을 표시하지 않는다
;·이라고 할까 RESULT < 2로 판정되므로 2이상이라면 같은 거동이 됩니다만 일단 비추천이라고 하는 일로
;-------------------------------------------------

;오리지날은 RESULT치 1을 돌려주고 있습니다만, 구상측의 지문과 내용이 입으므로, RESULT치 2로 해 둡니다@/L

;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && ARG == 3
	;기입 체크를 2 이하에했다경우는 이하의 지문이 표시된다. 표시 타이밍은구상표시의 뒤
	;%CALLNAME:TARGET%는금일의조교를 중지해,
	;%CALLNAME:MASTER%멈추거나와했다시간를 과 했다…
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자의 피로에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;기입 체크를 2 이하에했다경우는 이하의 지문이 표시된다. 표시 타이밍은구상표시의 뒤
	;조교의 시간이다가, 들어 온 %CALLNAME:TARGET%는 어쩐지 피곤한 모습으로,
	;이번은 휴일이라고 말해 나갔다…
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(1)
		PRINTFORMW 「후우……오래 기다리셨습니다」
		PRINTFORMDW 방에 들어온 모미지는, 딱봐도 피곤해보였다
		PRINTFORMDW 꼬리는 힘없이 축 처져, 푼식푼식하던 꼬리의 털도 흐트러져 있었다
		PRINTFORMW 「죄송합니다……오늘은 쉬도록하죠」
		PRINTFORMW 「상사에게, 허가는 받았으니까……」
		PRINTFORMDW 비틀비틀거리는 발걸음으로 모미지는 방을 나갔다……
		;오리지날은 RESULT치 1을 돌려주고 있습니다만, 구상측의 지문과 내용이 입으므로, RESULT치 2로 해 둡니다@/L
		RETURN 2
	ENDIF
	
	;-------------------------
	;처음 이외
	;-------------------------
;OriginalString : 「금일はお休みにします……」
	PRINTFORMW 「오늘은 쉬도록 하지요……」
	PRINTFORMDW 평소의 일과 조교, 두 일을 병행하는것은 피곤할것이다
	PRINTFORMDW 비틀거리는 발걸음으로 모미지는 방을 나갔다……
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상의 피로에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	;기입 체크를 2 이하에했다경우는 이하의 지문이 표시된다. 표시 타이밍은구상표시의 뒤
	;조교의 시간이다가, %CALLNAME:TARGET%는 심한 모습의 %CALLNAME:MASTER%를 봐 당분간 생각하면,
	;이번은 쉬어도 좋다고 말해 나갔다…
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(2)
		PRINTFORMW 「우와……꽤 지치신것 같네요」
		PRINTFORMDW %CALLNAME:MASTER%를 한번보고, 모미지는 %CALLNAME:MASTER%의 피로한 기색을 알아챘다
		PRINTFORMW 「오늘은 쉴테니, 휴식을 취하도록 하세요」
		PRINTFORMDW 처벌일텐데, 그렇게 물러도 되는건지 물어보자, 모미지는 진지한 얼굴을 했
		PRINTFORMW 「저희의 목적은 벌을 주는것이지, %CNAME(MASTER, TARGET)%를 죽이려는건 아니니까요」
		PRINTFORMDW 모미지는 나중에 물과 약을 가져오는것을 약속하고 방을 나갔다……
		RETURN 2
	ENDIF
	
	;-------------------------
	;처음 이외
	;-------------------------
	PRINTFORMW 「이 이상은 무리인것 같네요. 오늘은 여기까지입니다.」
	PRINTFORMDW 미안합니다, 라고 뒤처리 도중 계속 모미지에게 사과하자 모미지는 신경쓰지 말라고 고개를 저었다.
	PRINTFORMW 「너무 나가버린건 저이니 사과하실 필요는 없어요」
	RETURN 1
ENDIF

;-------------------------------------------------
;·이하, ARG:0(조교 가능) 상당
;·TRAIN_PROCCES.ERB내, @BEFORE_ACT로부터 불려 간다
;·이 시점에서 ACT는 벌써 결정하고 있지만 ACT파생의 결정은 이루어지지 않았다
;·NOWEX의 리셋트는 되지 않았다
;·이것들의 개소를 빠진 뒤로 ACT 개시가 된다
;·분기의 증감은자유로 할 수 있습니다만, 우선도에는 주의합시다
;-------------------------------------------------

;────────────────────────────────────
;조교개시 직후 FIRSTEVENT(10)～
;────────────────────────────────────
;·경과시간가 0인 경우에 표시된다
;·그 회의조교개시 직후에 한 번만으로, 이후 그조교중에는 표시되지 않는다
;·이 개소를 빠진 뒤로 ACT 개시가 된다
;-------------------------------------------------
;  여기서 IS_POLICY("금일", "징계·") 등으로금일의방침를 말하게 하는 일도 가능했다거나 합니다만
;  EVENT17, 오늘의방침구상와 입을 가능성이 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교개시 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && ! TFLAG:경과시간
;ENDIF

;────────────────────────────────────
;징계· FIRSTEVENT(20)～
;────────────────────────────────────
;·TFLAG:징계·플래그가 서 있는 경우에 표시되는 개소
;·징계·플래그가 서 있어도, 가학계 이외의 ACT가 되는 경우가 있다
;-------------------------------------------------
;가학계 ACT, 파생하는 일에 의해가학계에 포함될 가능성이 있는이라마치오
;의상 변경을 요구한다/기분 나쁘게 웃는다/애널바이브/후배위/항문섹스/자위/야외플레이/방뇨/발핥기강제
;이상계 ACT/성교봉사계 ACT
;상기의 ACT 이외는 TFLAG:징계·플래그에 의해 ACT_ABLE로 규제된다(0616판 시점)
;-------------------------------------------------
;-------------------------------------------------
;·1회의조교로 1회만 표시된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계·개시 
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:징계·플래그 && ONCEEVENT(20)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(20)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「죄송합니다, 이대로는 끝이 없으니까」
			PRINTFORMW 「체벌을 시작하겠습니다」
		;얼라이먼트부
		ELSE
			PRINTFORMW 「아직 이쪽이 얕잡아보이는것 같으니」
			PRINTFORMW 「최후의 수단으로 페벌을 하도록 하겠습니다.」
			PRINTFORMW 「후후후, 실은 한번쯤 해보고 싶었거든요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;전회ACT가징계·선언
	;=================================================
	;IF 구상기입 체크(0) && IS_PREVACTNAME("징계한다고 위협한다")
	;ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%는 질리지도 않나보군요」
			PRINTFORMW 모미지는 작게 한숨을 내쉬면서 %CALLNAME:MASTER%를 덮쳤다……
	;アライメント負
		;얼라이먼트부
		ELSE
;OriginalString : 「まさかわざと人を怒らせたりとかしてないですよね？」
			PRINTFORMW 「설마 일부러 저를 화나게 한다던가하는것은 아니지요？」
			PRINTFORMW 「어쨌든, 저도 점점 즐거워지기 시작했으니 상관 없지만서도」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;징계·개시 이외를 처리하는 개소
;━━━━━━━━━━━━━━━━━━━━━━━━
;징계·안
;구상기입 체크(확률％, 입력했다숫자의 확률로구상를 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && TFLAG:징계·플래그
	;=================================================
	;전회ACT가징계·선언 한편 이번 ACT가가학계
	;=================================================
	;IF 구상기입 체크(0) && MENUMATCH(TFLAG:ACT, "가학") && IS_PREVACTNAME("징계한다고 위협한다")
	;ENDIF
	
	;=================================================
	;전회ACT가징계·선언
	;=================================================
	;IF 구상기입 체크(0) && IS_PREVACTNAME("징계한다고 위협한다")
	;ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「조금은 솔직해주세요」
			PRINTFORMW 「저라도, 이런 괴로운 일은 그다지 하고싶지 않습니다……」
		;얼라이먼트부
		ELSE
			PRINTFORMW 「벌받는 기분은 어떤가요？」
			PRINTFORMW 「애완견과 같습니다. 말을 듣지 않으면 벌을 받아도 어쩔 수 없죠？」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;질내, 장내사정직후 FIRSTEVENT(30)～
;────────────────────────────────────
;NOWEX:사정내지 NOWEX:MASTER:사정로사정종의 참조도 가능했다거나 합니다만
;EVENT20, 절정구상에 동종의 분기와 입을 가능성도 있습니다
;-------------------------------------------------
;관련 변수…
;
;TFLAG:전회질싸(1=Ｖ성교/2=Ｖ성교봉사/3=Ｖ성교(드라이사정)/4=Ｖ성교봉사(드라이사정)/5=Ａ성교/6=Ａ성교봉사/7=Ａ성교(드라이사정)/8=Ａ성교봉사(드라이사정)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정직후(조교대상→조교자)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질싸, 2, 4) > 0
	;=================================================
	;드라이사정
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:전회질싸 == 4
	;ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(50) && TFLAG:전회질싸 == 2
;OriginalString : 「あっ……はぁ……はぁ……」
		PRINTFORMW 「아……후~……후~……」
		PRINTFORMDW 모미지는 질내를 가득 채운 정액의 뜨거움을 느끼려는듯 하복부에 손을 대었다
		PRINTFORMW 「안에, 싸버렸군요……」
		PRINTFORMDW 참지 못하고 안에 싸버린것을 사과하자, 모미지는 상기된 표정으로 고개를 저었다
		PRINTFORMW 「제가 한 일이니까, %CNAME(MASTER, TARGET)%가 사과할 필요는 없습니다」
		PRINTFORMW 「그 대신……조금이라도 좋으니 저에게 빠져주세요……」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정직후(조교대상→조교자)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && GROUPMATCH(TFLAG:전회질싸, 6, 8)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정직후(조교자→조교대상)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질싸, 1, 3)
	;=================================================
	;드라이사정
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:전회질싸 == 3
	;ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(50) && TFLAG:전회질싸 == 1
		PRINTFORMW 「하아……후우……」
		PRINTFORMDW 숨을 높이면서, 모미지는 페니스를 뽑아냈다
		PRINTFORMDW 모미지는 마음껏 질내에 사정한 여운에 잠겨있는것 같다
		PRINTFORMW 「이거……아기, 생겨버리는걸까요？」
		PRINTFORMDW %CALLNAME:MASTER%는, 모르겠다, 라고 대답할 수 없었다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내사정직후(조교자→조교대상)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && GROUPMATCH(TFLAG:전회질싸, 5, 7)
;ENDIF

;────────────────────────────────────
;성교중·성교봉사중 FIRSTEVENT(40)～
;────────────────────────────────────
;관련 변수…
;TEQUIP:성교중(조교자가조교대상에삽입중) (1=정상위/2=후배위/3=기승위/4=대면좌위/5=배면좌위/6=항문섹스)
;TEQUIP:성교봉사중(조교대상가조교자에삽입중) 내용은 상기의 것과 같음
;
;관련 함수…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)와 동의
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)와 동의
;-------------------------------------------------
;오리지날로 확립 1%가 되어 있었던 것이 작자의 의도라고도 생각되지 않기 때문에, 독단과 편견으로 이쪽에서 결정해 버립니다@/L
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(조교자→조교대상)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:성교중
	;=================================================
	;페니스밴드사용
	;=================================================
	IF 구상기입체크(50) && !PENIS(TARGET) && TEQUIP:조교자페니스밴드
		;성교계 ACT에 계속되는 경우
		IF MENUMATCH(TFLAG:ACT, "성교")
;OriginalString : 「だ、ダメですっ、本物のおちんちんじゃないのに」
			PRINTFORMW 「이다, 안됩니다 , 진짜의 자지도 아니면서」
;OriginalString : 「私も、気持ちよくなって、腰が止まらないですっ」
			PRINTFORMW 「 나도, 기분 좋아져, 허리가 멈추지 않습니다」
		;그 이외
		ELSE
;OriginalString : 「どんどん、入っていっちゃいますっ」
			PRINTFORMW 「자꾸자꾸, 들어가 말해버릴」
;OriginalString : 「%CNAME(MASTER, TARGET)%の中っ、もっと奥まで……っ！」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 안, 좀 더 안쪽까지……!」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;항문섹스(Ａ성교)
	;=================================================
	;IF 구상기입 체크(0) && A_SEX()
	;ENDIF
	
	;=================================================
	;그 외의성교계(Ｖ성교)
	;=================================================
	;IF 구상기입 체크(0) && V_SEX()
	;ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(조교대상→조교자)
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:성교봉사중
	;=================================================
	;페니스밴드사용
	;=================================================
	;IF 구상기입 체크(0) && ! PENIS(MASTER) && TEQUIP:페니스밴드
	;ENDIF
	
	;=================================================
	;항문섹스(Ａ성교봉사)
	;=================================================
	;IF 구상기입 체크(0) && A_SEX(MASTER)
	;ENDIF
	
	;=================================================
	;그 외성교봉사계(Ｖ성교봉사)
	;=================================================
	IF 구상기입체크(50) && V_SEX(MASTER)
		;성교봉사계 ACT에 계속되는 경우
		IF MENUMATCH(TFLAG:ACT, "성봉")
			PRINTFORMW 「아읏, 기분, 좋, 네요」
			PRINTFORMDW 모미지는 아래쪽에서 쳐올려지는 쾌락이 당황하면서도 받아들이고 스스로 허리를 움직이고있었다
			PRINTFORMW 「앗, %CNAME(MASTER, TARGET)% 더, 더, 움직여, 주세요」
			PRINTFORMDW 그 모습은 마치, 먹이를 노리는 한 마리의 늑대를 연상케했다
		;그 외의 경우
		ELSE
;OriginalString : 「んっ、気持ちいい、ですかっ、私の、中はっ」
			PRINTFORMW 「으응, 기분, 좋아욧, 제, 안…！」
			;역레, 혹은대면좌위시킨다의 경우
			SIF GROUPMATCH(TEQUIP:성교봉사중, 3, 4)
				PRINTFORMDW 모미지가 %CALLNAME:MASTER%의 위에 걸터앉아, 억누르듯 성기를 물고 늘어졌다
			PRINTFORMW 「늑대, 에게, 잡아먹히는, 기분, 은, 어떤가요？」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;절정했다직후 FIRSTEVENT(50)～
;────────────────────────────────────
;·조교자, 조교대상가절정했다경우에 표시되는구상
;·개별의 부위별절정를 줍고 싶으면, IF 구상기입 체크(0) && NOWEX:Ｃ절정등의 분기를 추가해 주세요
;·사정종은 NOWEX:사정/NOWEX:MASTER:사정로 참조하는 것이 가능합니다
;-------------------------------------------------
;관련 변수…
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;
;관련 함수…
;TRUECHECK 실로 되는 것이 몇개 있을까을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가절정했다직후
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 0
	;=================================================
	;조교자사정
	;=================================================
	;IF 구상기입 체크(0) && NOWEX:사정
	;ENDIF
	
	;각 부위의절정가 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	;IF 구상기입 체크(0) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 1
	;ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(25)
		PRINTFORMW 「아앗, 아아아……가버려, 가버려요…………！」
		PRINTFORMW 「핫, 핫, 하앗, 하앗, 하앗, 하앗…………」
		PRINTFORMW 「가, 가요, 앗, 아읏, 아흣, 아흣, 아흐윽…！」
		PRINTFORMW 「────！！　히야아아아아아아아아아앗！！」
		PRINTFORMDW 모미지는 부들부들 몸을 떨며, 온 몸으로 쾌감을 받아들이며 절정에 달했다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상가절정했다직후
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 0
	;=================================================
	;사정
	;=================================================
	IF 구상기입체크(25) && NOWEX:MASTER:사정
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「うわー、うわー」
			PRINTFORMW 「우왓, 우왓」
			PRINTFORMDW 내뿜어진 정액을, 모미지는 조심스럽게 손가락으로 훑어 핥아보았다
			PRINTFORMW 「이상한 냄새……정액이라는건, 신기하네요」
			PRINTFORMDW 역시 입에 넣는것은 저항이 있는지, 모미지는 손가락으로 실을 당기며 정액을 만지며 놀았다
		;얼라이먼트부
		ELSE
			PRINTFORMW 「우와아……잔뜩 내버렸네요」
			PRINTFORMDW 흩날린 정액을, 모미지는 손가락으로 훑어 입에 넣었다
			PRINTFORMW 「……맛은 없네요」
			PRINTFORMDW 모미지는 눈쌀을 찌푸리며 입가를 닦았다
		ENDIF
		RETURN 1
	ENDIF
	
	;각 부위의절정가 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	IF 구상기입체크(35) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 1
		IF RAND:2
			PRINTFORMW 「지금 %CNAME(MASTER, TARGET)%, 무척 볼만했어요」
			PRINTFORMW 「참고로 묻겠는데, 어디가 기분 좋았나요？」
			PRINTFORMW 「가슴인가요？ 엉덩이인가요？ 그것도아니면─가장 민감한 곳인가요？」
			PRINTFORMW 「설마, 전부 다 인가요？」
		ELSE
			PRINTFORMW 「일의 극에 달하다, 라는건 이런건 말하는건가요？」
			PRINTFORMW 「몸을 만지는것으로 그렇게 격렬하게 절정하다니……」
			PRINTFORMW 「참고는 되었지만, 제가 저렇게 되었다면 조금 무서울지도」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(25)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「괜찮나요？」
			PRINTFORMDW 모미지는 위로하려는듯 %CALLNAME:MASTER%의 손을 잡았다……
		;얼라이먼트부
		ELSE
			PRINTFORMW 「가버렸네요？」
			PRINTFORMDW 모미지는 추격하는듯이 %CALLNAME:MASTER%의 민감한 부분을 손가락으로 되짚었다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;조교방침변경 FIRSTEVENT(60)
;────────────────────────────────────
;·조교방침가 바뀌었을 때에 표시한다
;·어디까지나방침인 것로행동를 세세하게 결정하는 것은 아니다
;-------------------------------------------------
;관련 변수…
;TFLAG:턴방침		이 턴의조교방침(1=휴식/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴의조교방침(내용은턴방침와 같음)
;
;관련 함수…
;IS_POLICY	조교방침명칭과 합치하면 진을반환
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교방침변경
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침 1:휴식（확실히체력회복행동가 된다）
	;=================================================
	IF 구상기입체크(25) && IS_POLICY("지금", "휴식")
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「조금 쉬죠」
			PRINTFORMW 「마지막까지 이런 페이스로는, 서로의 몸이 남아나질 않으니까요」
			;アライメント負
		ELSE
			PRINTFORMW 「조금 쉬죠」
			PRINTFORMW 「회복하면 다시 시작할테니, 이틈에 느긋하게 쉬어두세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 2:소프트
	;=================================================
	IF 구상기입체크(25) && IS_POLICY("지금", "소프트")
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그럼, 조금 더 가볍게 가보죠」
			PRINTFORMW 「이정도라면, 그다지 부담도 없을 것 같으니까요」
			;アライメント負
		ELSE
			PRINTFORMW 「그럼, 가벼운 조교로 하도록하지요」
			PRINTFORMW 「가벼운 조교이니까, 제대로 따라주시는거죠？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 3:노멀
	;=================================================
	IF 구상기입체크(25) && IS_POLICY("지금", "노멀")
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그럼, 보통으로 할까요」
			PRINTFORMW 「역시, 보통이 제일가는 기본이지요」
			PRINTFORMW 「……수수한것은……조금 슬프지만……」
			PRINTFORMDW 보통으로 하는것에 뭔가 트라우마라도 있는걸까……
			PRINTFORMDW 어차피 나에겐 일어나는것도 회화도 스펠카드도……라던지가, 작은 소리로 들린듯한 기분이 들었다
				;スペルカードはできましたがまあこのままにしておきましょう
			;アライメント負
		ELSE
			PRINTFORMW 「그럼, 보통으로 할까요」
			PRINTFORMW 「조금 지루할지도 모르겠지만, 무엇이든 밸런스가 중요하니까요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 4:하드
	;=================================================
	IF 구상기입체크(100) && IS_POLICY("지금", "하드")
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그럼, 조금 강하게 가요」
			PRINTFORMW 「괴로울지도 모르겠지만, 죄송합니다……」
			;アライメント負
		ELSE
			PRINTFORMW 「그럼, 더 강하게 하는거네요」
			PRINTFORMW 「조금 하드하게 가지 않으면, 조교라고 할 수 없으니」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 5:이상
	;=================================================
	IF 구상기입체크(100) && IS_POLICY("지금", "이상")
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「うーん……あまり気は乗りませんけど……」
			PRINTFORMW 「음……별로 신경쓰지는 않습니다만……」
			PRINTFORMW 「필요하다면, 할 수 밖에 없죠」
		;アライメント負
		ELSE
			PRINTFORMW 「저도 너무 자신을 이상하다고 생각하는것은 아니지만」
			PRINTFORMW 「한번 하는 이상, 끝까지 가보도록 하곘습니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;조건 없음 FIRSTEVENT(70)～
;────────────────────────────────────
;·여기까지의 조건에 히트 하지 않았던 경우에 표시되는 개소
;·BASE로 소분기를 마련해 있습니다만, 임의의 조건을 추가하는 일도 할 수 있습니다
;------------------------------------------------
;관련 변수…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변/5, 이성/6, 흥미/7, 초조함/8, 만족)
;
;관련 함수…
;TENSION	인수로 지정했다BASE치를 나타낸다(50%이상은 높은,25%이상은 일반,25% 미만은 낮다)
;------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;조건 없음
;구상기입 체크（확률％, 입력했다숫자의 확률로구상를 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0)
	;=================================================
	;만족 50％이상
	;=================================================
	IF 구상기입 체크(0) && TENSION(TARGET, 8) == "높다"
	ENDIF
	
	;=================================================
	;이성 25％이하
	;=================================================
	IF 구상기입 체크(0) && TENSION(TARGET, 5) == "낮다"
	ENDIF
	
	;=================================================
	;흥미 25％이하
	;=================================================
	IF 구상기입 체크(0) && TENSION(TARGET, 6) == "낮다"
	ENDIF
	
	;=================================================
	;초조함 50％이상
	;=================================================
	IF 구상기입 체크(0) && TENSION(TARGET, 7) == "높다"
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입 체크(0)
	ENDIF
ENDIF
[SKIPEND]
