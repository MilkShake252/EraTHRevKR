;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지구상 회화계 개별 리액션구상파일
;
;	회화계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어선택했다COM가 자연스러운 행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운 행동/1=무리없는 행동/2=무리한 행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금 기분을 물어본다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은조교대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K41_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일했다 있고)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「역시 말하기 어려운건가요？」
		PRINTFORMW 「그렇군요……솔직하게 대답해주기 싫은거군요……」
	;얼라이먼트부
	ELSE
;OriginalString : 「やっぱり言いにくいですか？」
		PRINTFORMW 「역시 말하기 어려운건가요？」
		PRINTFORMW 「뭐어, 무엇이든 생각한대로 입에 담는것이 항상 옳은것은 아니니까요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「죄, 죄송합니다」
		PRINTFORMW 「기분이 나쁜것을 알고 있습니다」
		PRINTFORMW 「하지만……그래도, 그만둘수가 없어서」

	;アライメント負
	ELSE
		PRINTFORMW 「과연, 의사가 확실한 분이라는것은 알겠습니다」
		PRINTFORMW 「하지만 때와 장소를 생각하는 것이 좋다, 라고 충고드리겠습니다」
		PRINTFORMW 「용기와 만용은 다르다, 그것을 깨닫고 나서는 이미 늦습니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「음……그렇네요. 역시 이런 질문은 조금 곤란하네요」
		PRINTFORMDW 빈정거림이 통하지 않았던 것일까, 모미지는 아주 진자하게 반응했다
	;アライメント負
	ELSE
		PRINTFORMW 「곤란한가요？ 어쩔 수 없네요, 다음번까지 생각해주세요」
		PRINTFORMDW 빈정거림이 통하지 않았던 것일까, 모미지는 아주 진자하게 반응했다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(30)
		;호의 2000 이상
		IF AFFECTION("조교완료")
			PRINTFORMW 「정말인가요？ 기쁘네요」
			PRINTFORMW 「솔직한 사람은, 저도 좋아합니다」
		;好意500未満
		ELSE
			PRINTFORMW 「정말인가요？ 기쁘네요」
			PRINTFORMW 「지금은 아직, %CNAME(MASTER, TARGET)%의 마음에 응답할 수 업지만……」
		ENDIF
		RETURN 1
	ENDIF
	
	;CFLAG:M호의 >= 1000혹은 FAVOR(1000)가 파생의 조건의 1개여서
	;오리지날의 FAVOR(500)는 확실히 죽어 분기가 된다. 라는 것으로 만지작거립니다@/L
	;=================================================
	;호의호의 2000 이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("조교완료")
		;-------------------------------------------------
		;성교계/성교봉사계로질내사정의 후
		;-------------------------------------------------
		IF GROUPMATCH(TFLAG:전회질싸, 1, 2) > 0
			;성교봉사계로질내사정의 후
			IF TFLAG:전회질싸 == 2
				PRINTFORMW 「정말……안에 내면서 그런 말을 하다니, 반칙이에요」
				PRINTFORMW 「안에 싸버린것에, 화내는것을 화내지 않을수가 없잖아요」
			;성교계로질내사정의 후
			ELSE
;OriginalString : 「中に出してしまったのに、よかったのですか？」
				PRINTFORMW 「안에 싸버렸는데, 좋았나요？」
				PRINTFORMW 「아기가 생겨버릴지도 몰라요？」
				PRINTFORMW 「정말, 정말 괜찮은가요？」
			ENDIF
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그, 그렇게 확실한 말을 들어버리면」
			PRINTFORMW 「너무 부끄러워서, 낯간지러워요」
			PRINTFORMW 「하지만, 전혀 싫은건 아니니까요」
		;얼라이먼트부
		ELSE
;OriginalString : 「そ、そんなこと言われたら」
			PRINTFORMW 「그, 그런 말을 들어버리면」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%를, 좀더 괴롭히고 싶어지잖아요」
			PRINTFORMW 「그리고 저에게 그런 일면이 있었는것이 놀라워요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「솔직하게 대답해주셔서 감사합니다」
			PRINTFORMW 「저, 말에 내포된 의미를 찾는 취미는 없으니까」
			PRINTFORMW 「솔직하게 말해주셔서 기쁩니다」
		;アライメント負
		ELSE
			PRINTFORMW 「솔직하게 대답해주셔서 감사합니다」
			PRINTFORMW 「저, 말에 내포된 의미를 찾는 취미는 없으니까」
			PRINTFORMW 「솔직하게 말해주셔서 기쁩니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일했다 있고
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입 체크(0) && TFLAG:ACT파생 == 5
ENDIF
[SKIPEND]
;REACT0, 지금 기분을 물어본다

;────────────────────────────────────
;REACT1, 자신을 어떻게 생각하는지 물어본다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은조교대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K41_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「그런가요. 그걸로 안심했습니다」
		PRINTFORMW 「누구든지、다른 사람에게 미움받고 싶지는 않을테니까요」
		PRINTFORMW 「물론, 저도입니다」
	;アライメント負
	;얼라이먼트부
	ELSE
;OriginalString : 「そうですか。安心しま했다」
		PRINTFORMW 「그런가요. 그걸로 안심했습니다」
		PRINTFORMW 「그럼, 이대로 계속해도 괜찮겠네요？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게 응답한다의 경우
;REACT분류는"거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「우으으……예상은 하고있었지만, 이렇게 확실하게 들어버리면 역시 버티기 힘들어요」
		PRINTFORMDW 모미지는 구석에 웅크리고 손가락으로 바닥에 「の」를 그리고있다……
	;アライメント負
	ELSE
		PRINTFORMW 「당연한 반응이라고 생각합니다」
		PRINTFORMW 「하지만, %CNAME(MASTER, TARGET)%도 저처럼 처세에 그다지 능숙한 분은 아닌것 같네요」
		PRINTFORMW 「하지만, 그런 반항적인 말을 들으면, 다시 끔찍한 일을 할 수 밖에 없잖아요？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「화가 나진 않지만……조금, 곤란할지도 몰라요」
		PRINTFORMDW 원래 고지식한 성격의 모미지에게 반어법은 통하지 않은것 같다
	;アライメント負
	ELSE
		PRINTFORMW 「괜찮습니다. 끔찍한 일을 한 자각은 있기때문에, 당연하다고 생각합니다」
		PRINTFORMDW 원래 고지식한 성격의 모미지에게 반어법은 통하지 않은것 같다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 조교대상호의가 적은지, REACT분류가태연하게 한다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「저도 마찬가지에요」
		PRINTFORMW 「좀더 저를 좋아해주신다면, 저도 이제 심한 일을 할 필요가 없지요」
	;アライメント負
	ELSE
		PRINTFORMW 「심한 일은 가능한 한 하지 않을 생각입니다만, 어느정도가 심한 일이 될까요？」
		PRINTFORMW 「그것을 확인하기 위해 좀 더 여러가지를 시험해보지 않으면 안되겠네요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다로, 조교대상호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(40)
		PRINTFORMW 「엣？　에……？　저기、그、좋아？　에……」
		PRINTFORMDW 의외의 대답이었는지, 모미지는 갑자기 소재없는 시선을 굴리며 버둥거리기 시작했다……
		PRINTFORMDW 역시 짜증이 난걸까？ 반응이 곤란해서, 모미지는 그것을 눈치채서,
		PRINTFORMW 「아, 아뇨, 달라요. 단지, 이렇게 명확하게 『좋아』라는 이야기, 얼굴 앞에서 들어본적은 없어서,」
		PRINTFORMW 「기쁜듯한……부끄러운것듯한……낯간지러운듯한……」
		PRINTFORMDW 모미지는 공중에 손가락 끝으로 「の」를 그리며, 뺨을 붉게 물들인채 시선을 자유롭게 굴려댔다
		PRINTFORMW 「……하지만, 싫지는 않았요요」
		PRINTFORMDW 겨우 들을 수 있을정도의 말. 이 말을 마지막으로, 모미지는 잠시 시선을 돌린채 침묵하고있었다……
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
		PRINTFORMW 「좋아, 인가요. 감사합니다」
		PRINTFORMW 「그건 안에 싸게 해주었기 때문입니까？」
		PRINTFORMDW 그것만이 아냐, 라고 %CALLNAME:MASTER%는 강한 의지를 담아 대답했다
		PRINTFORMW 「그런가요. 그럼……저기……와흣,」
		PRINTFORMDW 모미지는 새빨갛게 변한채, %CALLNAME:MASTER%로부터 시선을 돌리고 말았다
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「억지로 넣어서, 게다가 안에 내버렸는데도 말인가요？」
		PRINTFORMW 「기분 좋았다,라고 하는건가요？」
		PRINTFORMW 「음……저는 잘 모르겠네요」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「어, 그러니까……그, 감사합니다」
		PRINTFORMDW 몇 번이나 반복해도 익숙하지 않은지, 모미지는 사춘기 소녀처럼 허둥지둥, 머뭇머뭇거리면서 답례의 말을 전해왔다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
		PRINTFORMW 「좀더 대단한 일, 인가요？」
		PRINTFORMW 「저기, 제 지식으로는 더이상 대단한 일이라는것이 상상이 가지 않는데……」
		PRINTFORMW 「서, 설마 엉덩이로 하는건가요？！」
		PRINTFORMW 「거, 거기에 이, 이, 임……신……도 입니까────？！」
		PRINTFORMDW 모미지는 여러가지 상상……아니, 망상을 시작한 것 같다……
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		PRINTFORMW 「이 이상 대단한 일이라고 해도……」
		PRINTFORMW 「제 지식으로는 이것도 충분히 대단한 일이라고 생각하는데요」
		PRINTFORMW 「조교의 길은 멀군요……」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「더 대단한것, 인가요？」
			PRINTFORMW 「정말, 저에게 대체 무엇을 할 생각인건가요」
			PRINTFORMDW 모미지는 뺨을 부풀리며 노려보고있지만, 진심으로 화가 난 것 같지는 않았다
	;アライメント負
		ELSE
			PRINTFORMW 「좀 더 대단한 것, 인가요？ %CNAME(MASTER, TARGET)%도 좋아하시네요」
			PRINTFORMW 「그런 변태씨의, 솔직함에 대단한 일을 해줄까, 결심이 무뎌지는데요」
			PRINTFORMDW 즐거운듯 %CALLNAME:MASTER%의 얼굴을 들여다보는 모미지의 표정은, 일부러 강아지에게 맡겨 괴롭히는 사람같았다
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신을 어떻게 생각하는지 물어본다

;────────────────────────────────────
;REACT2, 성적인 이야기를 한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K41_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「읏─？！　무시당했어요？！」
		PRINTFORMW 「힘내서, 힘들게 화제를 돌렸는데……」
		PRINTFORMDW 모미지는 주눅이 들어 바닥에 「の」자를 그리고있다……
	;アライメント負
	ELSE
		PRINTFORMW 「너무하네요. 모처럼 %CNAME(MASTER, TARGET)%가 흥미를 가지고있을법한 화제로 했는데」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「저도 부끄러웠지만, 그만둘 수 있을리가 없어요」
			PRINTFORMDW 모미지의 귀는 곤란한 감정을 표현하는듯 축 늘어져있었다……
		;アライメント負
		ELSE
			PRINTFORMW 「뜻밖이네요. 저는 그냥 이야기를 했을 뿐이에요？」
			PRINTFORMW 「조금이라도 말을 할 기력은 없나요？」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「하, 하지만, 그런 반응을 하면 오히려 부끄럽다고 할까,」
			PRINTFORMW 「우으……하다못해 가볍게 웃어라도주세요～」
	;アライメント負
		ELSE
			PRINTFORMW 「기운이 없네요─」
			PRINTFORMW 「야한 이야기를 역으로 받아쳐, 순수한 백랑텐구 소녀를 농락해줄 정도의 기개는 없는건가요？」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;징계·포인트가산 「이 철면피의 돼지째!」
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;내용적으로 파생 0으로 한정하는 것이 타당하다라고 판단 섬했다@/L
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「어, 어째 이상한 방향으로 스위치가 들어가버렸다？！」
			PRINTFORMW 「쓸데없는 말을 해 부스럼을 긁어버린 꼴인가요」
			PRINTFORMW 「하, 하지만, 조교로선 올바린 쪽인거죠……」
			PRINTFORMW 「그, 그럼 열심히 기분좋게합시니다！」
	;アライメント負
		ELSE
			PRINTFORMW 「스위치가 들어가버렸나요？」
			PRINTFORMW 「짐승씨네요─. 기분좋게 해드릴까, 말까요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("자위를 시작한다")
	;=================================================
	;REACT파생 1, 「승손에 자위 하지마」
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;=================================================
	;REACT파생 0, 「승손에 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「갑자기 무엇을 하는건가요？！」
			PRINTFORMW 「도와주세요 선배～」
			PRINTFORMW 「이 사람, 저만으론 감당할 수 없을지도 몰라요～」
		;アライメント負
		ELSE
			PRINTFORMW 「…………」
			PRINTFORMW 「아……죄송합니다. 너무 이해를 넘어선 행동이라 머리가 잠시 생각을 포기했습니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에 관한 이야기를 함

;────────────────────────────────────
;REACT3, 상냥하게 위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K41_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는손도 있습니다
;-------------------------------------------------
;  우선회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는선택 가능하게 됩니다
;  필요에 응해 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「아, 아무리 기분이 나쁘다고해도, 무시하는건 좀 아니잖아요～」
		PRINTFORMDW 모미지는 눈물로 항의하고있다……
	;アライメント負
	ELSE
		PRINTFORMW 「무시입니까, 그런가요……」
		PRINTFORMW 「위로는 필요없어, 더 강하게 가도 좋아, 라고 이해하면 되겠습니까？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무뚝뚝하게 응답한다")
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「역시, 저 같은것은 위로를 해도 아무것도 되지 않는건가요？」
		PRINTFORMW 「죄송합니다……하다못해, %CNAME(MASTER, TARGET)%에게 부담이 가지않도록 주의를 기울이고 조교를 할 테니까요」
	;アライメント負
	ELSE
		PRINTFORMW 「너에게 받는 위로는 필요없다, 라는 것인가요？」
		PRINTFORMW 「그렇다면 이쪽도 그다지 죄책감이 솟지 않지때문에, 다행이네요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 애교 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("상냥하게 응답한다")
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「조금은 기분이 좋아졌나요？」
		PRINTFORMW 「다행이다……에헤헤」
	;アライメント負
	ELSE
		PRINTFORMW 「그 대답이 본심에서 나오는지 저는 모르겠습니다만」
		PRINTFORMW 「그러한 반응을 해주면, 저 또한 기쁩니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("강하게 응답한다")
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「죄, 죄송합니다」
		PRINTFORMW 「결코 %CNAME(MASTER, TARGET)%를 어린아이 취급한것은 아니지만……」
	;アライメント負
	ELSE
		PRINTFORMW 「꽤 강하게 나오시네요」
		PRINTFORMW 「이쪽에서 조금 가감해서 말하는 태도가 이렇게 되는것이라면」
		PRINTFORMW 「그건 이쪽에 허를 찌르는게 아니에요？ 단순한 반항기와 같은겁니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
	;얼라이먼트정
	IF ALI(0)
;OriginalString : 「조교に慰めなんて필요あるのか……ですか」
		PRINTFORMW 「조교에 위로받을 필요는 있는가……입니까」
		PRINTFORMW 「우으으……지당합니다만……지당합니다만～」
	;アライメント負
	ELSE
		PRINTFORMW 「그런 태도로 돌아온다면」
		PRINTFORMW 「다음부터는, 상냥하게 하고싶지 않아져요？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그……방금, 위로받고계시지 않았나요？ 적어도 저는 그렇게 생각했습니다만」
			PRINTFORMW 「거기서 하드하게 해줘, 라는 말을 들어버리면 저는 매우 당황할 뿐인데요」
			PRINTFORMDW 모미지는 뜬금없는 자신의 말에 당황하고있는것 같다……
		;アライメント負
		ELSE
			PRINTFORMW 「뭐라고 말해야할까요……뭐어, %CNAME(MASTER, TARGET)%가 그렇게 말하는거라면」
			PRINTFORMDW 모미지의 뒤에, 뭔가 검은 오오라가 감돌기 시작했다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「괘, 괜찮은건가요？！」
			PRINTFORMDW 의외의 반응에 모미지는 당황하고있는것 같다……
	;アライメント負
		ELSE
			PRINTFORMW 「사람이 바뀌었군요……%CNAME(MASTER, TARGET)%」
			PRINTFORMDW 모미지는 이상한것을 본 듯한 눈길로 %CALLNAME:MASTER%를 보고있다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「申し訳ありませんが、それはできません」
			PRINTFORMW 「죄송합니다만, 그럴 수 없습니다」
			PRINTFORMDW 모미지의 말에서 강한 의지가 느껴졌다……
		;アライメント負
		ELSE
			PRINTFORMW 「안돼요, 용서할 수 없습니다♪」
			PRINTFORMDW 모미지는 무척이나 밝은 미소로 대답해왔다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「申し訳ありません……」
			PRINTFORMW 「죄송합니다……」
			PRINTFORMW 「지금의 저에겐, 사과하는것과, 최대한 부담을 주지 않는 방식으로 조교하는것 밖에 할 수 없습니다」
	;アライメント負
		ELSE
			PRINTFORMW 「죄송합니다」
			PRINTFORMW 「결과에는 원인이라는게 있습니다」
			PRINTFORMW 「이번 경우에는, 용서하는것이 조금……」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;오리지날에서는얼라이먼트-의 부분에서했다가, 이쪽이 보다 적당하다고 생각되므로 개변 섬했다@/L
;OriginalString : 「だいぶ솔직になってきま했다ね」
		PRINTFORMW 「꽤나 솔직하게 되셨네요」
		PRINTFORMW 「하지만, 기분좋게 하는가, 아프게 하는가는 저에게 달려있습니다」
		PRINTFORMW 「어느쪽이 될지는, %CNAME(MASTER, TARGET)%가 하기 나름이므로 잊지 말아주세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		PRINTFORMW 「에, 에에에엣？！」
		PRINTFORMW 「저에게 기분좋게 되고싶어진건가요？！」
		PRINTFORMW 「아뇨, 확실히 아픈쪽보단 그쪽이 좋습니다만, 확실히 잘 할 수 있을까……」
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게 위로한다

;────────────────────────────────────
;REACT4, 엄하게 위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K41_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	;얼라이먼트정
	IF ALI(0)
;OriginalString : 「え？　ええーっ」
		PRINTFORMW 「에？ 에에─」
		PRINTFORMW 「꽤 진심으로 위협할 생각……이었지만서도……」
		PRINTFORMW 「저, 그렇게 박력없었나요……」
		PRINTFORMDW 모미지는 벽에 「の」자를 그리며 우울해져있었다……
	;アライメント負
	ELSE
		PRINTFORMW 「목소리도 나오지 않는건가요？」
		PRINTFORMW 「설마, 언제까지 위협만 할 거라고 생각하는것은 아니겠죠？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 마음 약하게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("마음 약하게 응답한다")
	;REACT분류"용서를 빌다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
;OriginalString : 「す、すみません」
		PRINTFORMW 「죄, 죄송합니다」
		PRINTFORMW 「너무 겁줘버렸네요」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		PRINTFORMW 「놀라셨나요？」
		PRINTFORMW 「잊지 말아주세요. 제가 강아지가 아니라 늑대라는것을」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
	;얼라이먼트정
	IF ALI(0)
;OriginalString : 「むぅ、あまり怖がられていないです」
		PRINTFORMW 「무으, 그다지 놀라지 않으시네요」
		PRINTFORMW 「좀더 박력을 낼 수 있게 연습해볼까……」
	;アライメント負
	ELSE
		PRINTFORMW 「강아지 흉내 잘 내네, 라고요？」
		PRINTFORMW 「……화낼거예요？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(7)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMDW 자칫하면 먹혀서 죽어버릴지도 모른다, 라고 %CALLNAME:MASTER%의 본능이 경고했다
			PRINTFORMDW %CALLNAME:MASTER%는 솔직하게 고개를 숙여 용서를 빌었다
			PRINTFORMDW 눈앞의 소녀가 강아지같은 성격을 가지고있다고 해도,
			PRINTFORMDW 요괴의 산의 백랑텐구라는 사실을 잊지 말라고 자신에게 타일렀다……
		;アライメント負
		ELSE
			PRINTFORMDW 자칫하면 먹혀서 죽어버릴지도 모른다, 라고 %CALLNAME:MASTER%의 본능이 경고했다
			PRINTFORMDW %CALLNAME:MASTER%는 솔직하게 고개를 숙여 용서를 빌었다
			PRINTFORMDW 눈앞의 소녀가 강아지같은 성격을 가지고있다고 해도,
			PRINTFORMDW 요괴의 산의 백랑텐구라는 사실을 잊지 말라고 자신에게 타일렀다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「分かっていただけま했다ね」
			PRINTFORMW 「드디어 깨달으셨나요」
			PRINTFORMW 「정말, 위협은 실행되지 않은채 끝날수도 있어요. %CNAME(MASTER, TARGET)%가 모르는 요괴의 산 사회에서는」
	;アライメント負
		ELSE
			PRINTFORMW 「용서해드릴까요, 어떻게할까요……」
			PRINTFORMW 「라던가, 농담이에요. 지금 %CNAME(MASTER, TARGET)%, 무척 볼만한 얼굴을 하고있다고요？」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다")
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	;IF 구상기입 체크(0) && TFLAG:REACT파생 == 0
	;ENDIF
	
	;내용적으로 파생 1으로 한정하는 것이 타당하다라고 판단 섬했다@/L
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「た、確かに脅했다のは私ですが」
			PRINTFORMW 「화, 확실히 위협한것은 저입니다만,」
			PRINTFORMW 「그렇게 무서워하면 그것 나름대로 충격이네요」
	;アライメント負
		ELSE
			PRINTFORMW 「그렇게 무서웠나요？」
			PRINTFORMW 「이건 이걸로……왠지 기분 좋네요」
			PRINTFORMW 「전에 산에 온 인간의 무녀와 마녀는, 저의 협박따위 전혀 효과가 없었기에」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄하게 위협한다

;────────────────────────────────────
;REACT5, 의상 변경을 요구한다·개별 리액션
;────────────────────────────────────
;·조교대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K41_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------

;오리지날의 SELECTCOM에 의한 분기에서는 현재의 사양에 대응 할 수 없기 때문에
;독단과 편견으로 재편성 해 버립니다@/L

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("받아들인다")
			;얼라이먼트정
			IF ALI(0)
				PRINTFORMW 「저, 저기. 너무 평범하게 옷을 벗으면, 저로서도 부끄럽다고할까」
				PRINTFORMDW 모미지는 당황해서 눈을 돌렸다
				PRINTFORMDW ……어쩌라는거야
			;얼라이먼트부
			ELSE
;OriginalString : 「脱ぎま했다ね」
				PRINTFORMW 「벗으셨네요」
				PRINTFORMW 「그럼, 다음은……」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("소극적으로 한다")
			;COM10, 부끄러워한다
			IF IS_NOWCOMNAME("부끄러워한다")
				;처음
				IF FIRSTREACT(0)
					PRINTFORMW 「에, 저기, 그러니까. 옷을 벗는것 만으로 그렇게 부끄러워하면, 저로서도 어떻게 하면 좋을지……」
					PRINTFORMDW 모미지는 얼굴을 새빨갛게하고 시선을 아래로 향했다
					PRINTFORMDW ……정말 이 인선, 실수가 아닐까
				;조건 없음
				ELSE
					PRINTFORMW 「부끄러워하셔도 안먹혀요！ 처음도 아니니 확실하게 해주세요！」
					PRINTFORMDW 그렇게 말하는 모미지도 상당히 부끄러워하는것 같은데……
				ENDIF
				RETURN 1
			ENDIF
			
			;그 외
			IF ALI(0)	;얼라이먼트+
				PRINTFORMW 「무관심인거네요」
				PRINTFORMW 「하지만, 벗은 옷은 제대로 정리해주세요？」
				PRINTFORMDW ……꽤 꼼꼼한 조교자도 있던것이다
		;アライメント負
			ELSE
				PRINTFORMW 「방금 한 대답은 조금 좋지 않다고 생각합니다.」
				PRINTFORMW 「옷을 벗는 방법 하나로도, 상대에게 주는 인상은 상당히 바뀐다고요？」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("적극적으로 한다")
			;얼라이먼트정
			IF ALI(0)
				PRINTFORMW 「꽤 적극적으로 변하셨네요」
				PRINTFORMW 「아뇨, 저로써로 좋은 경과라고 생각합니다만,」
				PRINTFORMW 「그, 그렇게 당당하게 벗으면 저로서도 조금 부끄러울까나, 하고……」
				PRINTFORMDW 모미지는 옷을 벗은 %CALLNAME:MASTER%의 몸을 힐끗힐끗 훔쳐보길 반복했다……
		;アライメント負
			ELSE
				PRINTFORMW 「꽤 적극적으로 변하셨네요」
				PRINTFORMW 「옷을 벗으면 기분좋은 일을 할 수 있다고 학습한건가요？」
				PRINTFORMW 「라는 말이던가, 이건, 음……파블로프의 개, 였었죠？」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;손때 좋게 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("태연하게 한다")
			;얼라이먼트정
			IF ALI(0)
				PRINTFORMDW 좋아！ 이 벗은 몸을 그 두 눈에 새겨두는것이 좋을거야！
				PRINTFORMW 「꺄아아아아아아？！ 무서워욧？！」
		;アライメント負
			ELSE
				PRINTFORMDW 좋아！ 이 벗은 몸을 그 두 눈에 새겨두는것이 좋을거야！
				PRINTFORMW 「여기 변태씨가 있는데요？！」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		;-------------------------------------------------
		;부끄러워서 벗겨지지 않았다
		;-------------------------------------------------
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 「……음─」
			PRINTFORMDW 모미지는 불만스러운 표정으로 어떻게해야 부탁을 들어줄지를 생각하고있는듯 하다……
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;싫어해 벗지 않았다
		;-------------------------------------------------
		IF PALAM:MASTER:반항 > PALAM:MASTER:치정
			;COM11, 싫어한다
			IF IS_NOWCOMNAME("싫어한다")
				;얼라이먼트정
				IF ALI(0)
					PRINTFORMW 「그, 벗으라고하면 누구든 싫다고 생각하겠지만」
					PRINTFORMW 「거기를 어떻게좀 해주시면 안될까요？」
				;アライメント負
				ELSE
					PRINTFORMW 「벗으라고하면 누구든 싫다고 생각하겠지만」
					PRINTFORMW 「이것이 부탁이 아닌 『명령』이라는 것을 잊지 말아주세요？」
				ENDIF
				RETURN 1
			ENDIF
			
			;그 외
;OriginalString : 「そうですか……」
			PRINTFORMW 「그런가요……」
			PRINTFORMDW 말은 정중했지만, 모미지의 눈에는 불만의 기색이 가득했다……
			RETURN 1
		ENDIF
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적으로 한다/받아들인다")
		;이 개소에서 범용적으로 사용할 수 있는 내용은 아니기 때문에 COM로 결정하고 쳐 합니다@/L
		;-------------------------------------------------
		;COM10, 부끄러워한다
		;-------------------------------------------------
		IF IS_NOWCOMNAME("부끄러워한다")
			;처음
			IF FIRSTTIME(10)
				PRINTFORMW 「저는 잘 모르겠습니다」
				PRINTFORMW 「어째서 옷을 입으라고했는데 부끄러워하시는 건가요？」
		;条件なし
			ELSE
				PRINTFORMW 「어째서 옷을 입으라고했는데 부끄러워하시는 건가요？」
				PRINTFORMW 「설마, 나체로 있는것이 좋았던건가요？」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그렇게 알몸으로 있는것이 싫었나요？」
			PRINTFORMW 「뭐어, 무리도 아니지만……」
		;アライメント負
		ELSE
			PRINTFORMW 「그렇게 알몸으로 있는것이 싫었나요？」
			PRINTFORMW 「지금이라도 명령, 취소할까나……」
		ENDIF
		RETURN 1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		;-------------------------------------------------
		;COM11, 싫어한다
		;-------------------------------------------------
		IF IS_NOWCOMNAME("싫어한다")
			;얼라이먼트정
			IF ALI(0)
;OriginalString : 「え、ええーっ？」
				PRINTFORMW 「에, 에엣─？」
				PRINTFORMDW 모미지는 %CALLNAME:MASTER%가 옷을 입는 것을 거부하는것을 이해할 수 없는것 같다……
		;アライメント負
			ELSE
				PRINTFORMW 「싫다면 그걸로도 상관없습니다」
				PRINTFORMW 「짐승들처럼, 알몸으로 있어도 상관없어요？」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「저, 정말로 이해할 수 없네요……」
			PRINTFORMW 「저기, 설마 조교당해 야생으로 환원되었다,」
			PRINTFORMW 「같은건 아니지요……？」
		;アライメント負
		ELSE
			PRINTFORMW 「그렇게나 옷을 입고싶지 않은건가요？」
			PRINTFORMW 「그럼, 그쪽에 있는 %CNAME(MASTER, TARGET)%의 옷은 태워버려도 상관 없겠네요？」
			PRINTFORMDW %CALLNAME:MASTER%는 당황해서 그것만은 참아달라고 용서를 빌었다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상 변경을 요구한다

;────────────────────────────────────
;REACT6, 합의를 요구한다·개별 리액션
;────────────────────────────────────
;·조교대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K41_6
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가소극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("소극적으로 한다")
;OriginalString : 「やっぱり、ちょっと怖いですか？」
	PRINTFORMW 「역시, 조금 무섭다고요？」
	PRINTFORMW 「……하지만, 그래도 저를 받아들여주시는 거죠？」
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「죄송한, 하지만 기쁜, 조금 복잡한 기분이네요」
		PRINTFORMW 「제가 해드릴 수 있는것은 조금이라도 더 상냥하게 해드리는 것뿐입니다만」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%를, 잘먹겠습니다」
	;얼라이먼트부
	ELSE
		PRINTFORMW 「죄송한, 하지만 기쁜, 조금 복잡한 기분이네요」
		PRINTFORMW 「제가 해드릴 수 있는것은 조금이라도 기분좋게 해드리는 것뿐입니다만」
		PRINTFORMW 「도중에 역시 그만두자고해도 멈출 수 없으니, 각오해주세요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가태연하게 한다의 경우
;-------------------------------------------------
;IF 구상기입 체크(0) && IS_COMGRONAME("태연하게 한다")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가적극적으로 한다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("적극적으로 한다")
	PRINTFORMW 「정말요？！」
	PRINTFORMDW 모미지의 얼굴이, 보면 알 수 있을정도로 환하게 빛났다
	PRINTFORMW 「고맙습니다！」
	PRINTFORMW 「솔직히, 거절당하는게 아닐까, 내심 불안했습니다」
	PRINTFORMW 「가능한 %CNAME(MASTER, TARGET)%가 괴롭지않게, 상냥하게 할테니까요」
	IF ALI(0)
;OriginalString : 「できるだけ、%CNAME(MASTER, TARGET)%が苦しくないよう、優しくしますので」
		PRINTFORMW 「가능한 %CNAME(MASTER, TARGET)%가 괴롭지않게, 상냥하게 할테니까요」
	ELSE
;OriginalString : 「%CNAME(MASTER, TARGET)%が気持ちよくなれるよう、頑張りますねっ」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%가 기분좋게 될 수 있도록 노력하겠습니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게 해 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
;IF 구상기입 체크(0) && IS_COMGRONAME("받아들인다")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의 하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서"거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("거부")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(4)
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「これまでの조교とは一線を画しますからね」
			PRINTFORMW 「지금까지의 조교와는 분명하게 구별되니까요」
			PRINTFORMW 「싫다고 말하신다면 어쩔 수 없습니다」
		;アライメント負
		ELSE
			PRINTFORMW 「역시 무서운건가요？ 여기까지 치닫는것은」
			PRINTFORMW 「지금의 %CNAME(MASTER, TARGET)%라면 받아줄지도 모른다, 라고 생각했습니다만」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「やはり、無理やり奪われるのは嫌ですか？」
			PRINTFORMW 「역시, 억지로 빼앗기고 싶지는 않은거지요？」
			PRINTFORMW 「아뇨……걱정하지 마세요. 나쁜쪽은 이쪽이니까요」
	;アライメント負
		ELSE
			PRINTFORMW 「조금이지만 좋은 관계를 쌓아왔다고 생각했습니다만」
			PRINTFORMW 「아직 시간이 조금 부족했던걸지도 모르겠네요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를 요구한다

;────────────────────────────────────
;REACT7, 징계한다고 위협한다·개별 리액션
;────────────────────────────────────
;·선언해도징계·이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K41_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더 하드하게 해
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("더 하드하게 해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「완전히 포로가 되어버린건가요？」
		PRINTFORMW 「그런 소질이 있을줄이야,」
		PRINTFORMW 「정말 어쩔 수 없는 분이네요, %CNAME(MASTER, TARGET)%」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		PRINTFORMW 「에에에에에────？！」
		PRINTFORMDW 모미지는 의외의 반응에 혼란스러워하는 것 같다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다") 
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「싫습니다」
		PRINTFORMDW 모미지는 단호하게 말을 잘랐다
		PRINTFORMDW 그 강력한 의지는 쉽게 무너지지 않을 것 같다……
	;アライメント負
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		PRINTFORMW 「용서한다, 용서하는것은 아무래도 좋습니다」
		PRINTFORMW 「필요하니까, 하는겁니다」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다")
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「체벌이 그렇게나 싫은건가요？」
		PRINTFORMW 「다음부턴, 좀 더 상대방의 입장을 생각하고 행동하는것이 좋습니다」
		PRINTFORMW 「이번？ 죄송합니다만, 그만두지 않을겁니다」
		;고통에 의한 비명의 경우
		SIF !TFLAG:REACT파생
;OriginalString : 「頑張って耐えてください」
			PRINTFORMW 「열심히 참아보세요」
	;얼라이먼트부
	ELSE
;OriginalString : 「みっともないですよ」
		PRINTFORMW 「보기 흉하네요」
		;고통에 의한 비명의 경우
		SIF !TFLAG:REACT파생
;OriginalString : 「痛いのが嫌なら、最初から意固地になって抵抗なんてしなければよかったんです」
			PRINTFORMW 「아픈게 싫다면 처음부터 저항하지 않았으면 좋았을걸」
		PRINTFORMW 「나중에 후회해서 돌이킬 수 없다는 것은, 인간에게도 요괴에게도 마찬가지에요」
	ENDIF
	RETURN 1
ENDIF
;REACT7, 징계한다고 위협한다

;────────────────────────────────────
;REACT8, 고함치다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K41_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
;OriginalString : 「最近、忘れてはいませんで했다か？」
	PRINTFORMW 「최근, 잊지 않았나요？」
;OriginalString : 「\@ ALI(0) ? 仮にも私が狼だということに # 私も狩る方の立場だということに \@」
	PRINTFORMW 「\@ ALI(0) ? 이래뵈도 제가 늑대라는것을 # 저도 사냥하는 쪽이라는 것을 \@」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명지른다")
	;얼라이먼트정
	IF ALI(0)
;OriginalString : 「少しは理解していただけま했다か？」
		PRINTFORMW 「조금 이해하실 수 있었나요？」
		PRINTFORMW 「외형으로 상대를 경시하면, 곧바로 먹혀버릴 수 있는 세계도 있다는것을」
	;アライメント負
	ELSE
		PRINTFORMW 「이 정도로 비명을 지르시는 건가요？」
		PRINTFORMW 「산의 텐구에겐, 그런 약한 텐구는 필요없어요？」
	ENDIF
	RETURN 1
ENDIF
;REACT8, 고함치다

;────────────────────────────────────
;REACT9, 기분 나쁘게 웃는다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K41_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
;OriginalString : 「\@ ALI(0) ? がーん！？　無視されま했다っ！？ # 無視するなんてひどいですよ～ \@」
	PRINTFORMW 「\@ ALI(0) ? 으읏─？！ 무시당했어요？！  # 무시하다니 너무해요～ \@」
;OriginalString : だって、あれでどう反応してよいものやら……
	PRINTFORMDW 하지만, 그거에 어떻게 반응해야 좋은건데……
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를 빌다")
	;파생마다 분리해 버리는 것이 타당하다라고 판단 섬했다@/L
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 번가슴구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「천천히 들려주세요. 저는 분명 미소를 지었는데, 어째서 용서를 요구받는건가요？」
		PRINTFORMDW 얼굴은 웃고있었지만, 목소리는 웃고있지 않았다
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「笑顔を贈ったのに、なぜか許しを請われてますっ！？」
		PRINTFORMW 「미소를 선물했는데, 어째서인지 용서를 요구받고있다？！」
		PRINTFORMDW 모미지는 충격을 받았는지, 그 자리에 멍하니 서있었다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명지른다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명지른다")
	;TEQUIP:(관장＋항문플러그/삼각목마)장착중등의 경우는고통의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서손간을 거는필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 고통에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「제, 제 미소가 그렇게 무서웠나요？！」
			PRINTFORMDW 우울해질 정도라면 처음부터 하지 않았으면 좋을텐데……
	;アライメント負
		ELSE
			PRINTFORMW 「사람이 웃는 얼굴로 분위기를 누그러트리려하는데, %CNAME(MASTER, TARGET)%는 비명을 지르시는군요」
			PRINTFORMDW 아니, 그 웃음, 역으로 무서웠단말이야……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한초조함
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분 나쁘게 웃는다
