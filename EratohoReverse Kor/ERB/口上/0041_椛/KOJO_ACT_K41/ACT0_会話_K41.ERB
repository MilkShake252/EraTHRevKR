;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지구상 회화계 액션구상파일
;
;	회화계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금 기분을 물어본다/1=자신을 어떻게 생각하는지 물어본다/2=성적인 이야기를 한다/3=상냥하게 위로한다/4=엄하게 위협한다/5=의상 변경을 요구한다/6=합의를 요구한다/7=징계한다고 위협한다/8=고함치다/9=기분 나쁘게 웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분 나쁘게 웃는다는이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는구상측의 RESULT(종단의 RETURN 1 등)에 의해구상가 표시당했다의지 아닌지,
;·또 개소 마다의 특유의 거동(미약등구상를 스킵 따위)을 제어하고 있습니다
;·@구상기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는구상측에서 결정하고 쳐 하는필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금 기분을 물어본다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·조교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K41_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금 기분을 물어본다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「참고로 들어두고 싶은것이 있습니다」
		PRINTFORMW 「조교당하는 입장으로, 지금 기분이 어떻습니까？」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그……제 안에 잔뜩 내버리셨네요」
			PRINTFORMW 「어땠나요？ 기분 좋았나요？」
		;アライメント負
		ELSE
			PRINTFORMW 「그……제 안에 잔뜩 내버리셨네요」
			PRINTFORMW 「어떻게해요？ 만약 임신해버리면 큰일이에요？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「죄, 죄송합니다！ 기분좋아서 무심코 전부 내버렸습니다！」
			PRINTFORMW 「저기……혹시 화나셨나요？」
		;アライメント負
		;ねえどんな気持ち？中出しされてどんな気持ち？
		ELSE
			PRINTFORMW 「잘먹었습니다. 무척 좋았습니다」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%은 어땠나요？」
		ENDIF
		RETURN LOCAL
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 기분, 어떻습니까？」
			PRINTFORMW 「제가 그다지, 사람의 기분을 파악하는 요령좋은 성격이 아니라서」
			PRINTFORMW 「입으로 들려주시면 기쁠거예요」
	;アライメント負
		ELSE
			PRINTFORMW 「어떤가요？ 지금 기분은」
			PRINTFORMW 「하고싶은 말이 있다면 들려주세요」
			PRINTFORMW 「저도 그다지 요령이 있지는 않으니까」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT1, 자신을 어떻게 생각하는지 물어본다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신을 어떻게 생각하는지 물어본다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「안에……싸버렸습니다」
			PRINTFORMW 「이런짓을 해놓고 이제와서이지만」
			PRINTFORMW 「제가 남성을 덮치는 도중 드러나는 저를, 어떻게 생각하십니까？」
		;アライメント負
		ELSE
			PRINTFORMW 「전부 내버렸습니다」
			PRINTFORMW 「후후……%CNAME(MASTER, TARGET)%에게 있어, 저는 무엇인가요？」
			PRINTFORMW 「움직일 수 없는 자신을 조련하는 사람인가요？」
			PRINTFORMW 「아니면, 성욕을 해소시켜주는 형편좋은 여자인가요？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「저기, 가능하면 정직한 대답을 원합니다.」
			PRINTFORMW 「저를, 좋아하나요？」
	;アライメント負
		ELSE
			PRINTFORMW 「거짓말하지말고 정직하게 대답해주세요」
			PRINTFORMW 「저를, 좋아하나요？」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 성에 관한 이야기를 함
;────────────────────────────────────
;·조교자가 성에 대한지식를 말하기 시작한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·조교자에성지식가 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에 관한 이야기를 함
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「그럼, 조금 야한 이야기라도 할까요」
		PRINTFORMDW 모미지는 %CALLNAME:MASTER%와 마주하면서 자랑스러운 표정을 지으며 말했다
		PRINTFORMW 「…………어, 그게……다, 다음번에 생각해올께요」
		PRINTFORMDW 잘 생각하지 않아도 성경험들리 압도적으로 적은 이 여자아이의 말이다
		PRINTFORMDW 방금전의 자랑스러운 표정은 분명 흐름을 탄 것 뿐이라는것을 알았다……
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 2
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「지금 ,%CNAME(MASTER, TARGET)%의 정자가 제 안에 있는거죠」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%와 종족은 다르지만……아기는 생길까요？」
		;アライメント負
		ELSE
			PRINTFORMW 「알고있나요？ 지금, %CNAME(MASTER, TARGET)%가 마구 내버린 정자가 제 뱃속을 헤엄치고있어요」
			PRINTFORMW 「지금쯤, 저를 임신시키려고 노력하고 있을까요？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질싸 == 1
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「알고있나요？ 개의 사정은, 아주 길어요」
			PRINTFORMDW ……그럼, 늑대는？
			PRINTFORMW 「에？ 늑대는………………어디보자……」
			PRINTFORMDW 너는 늑대냐, 개냐……
		;얼라이먼트부
		;ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그런데, 아시다시피 저는 백랑텐구입니다만」
		PRINTFORMW 「발정기, 있다고 생각하세요？」
		PRINTFORMW 「대답은 비밀입니다. 궁금하세요？」
		PRINTFORMW 「 왜냐라니……\@ ALI(0) ? 다, 당연히 알려지는것이 부끄러우니까…… # 기대하고 있었나요？ 제가 발정기가 될 시기를 \@」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT3, 상냥하게 위로한다
;────────────────────────────────────
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"하드", "이상"라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마항문/자위중)로 지문은변화 할 가능성이 있다
;모두 묻는필요는 없기 때문에, 기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게 위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && ALI(0) && FIRSTACT(0)
		PRINTFORMW 「조금……느긋하게 있어봐요」
		PRINTFORMDW 모미지는 %CALLNAME:MASTER%의 머리에 손을 얹어 아이를 달래는것 처럼 머리를 쓰다듬었다
		PRINTFORMW 「이런일을 해도, %CNAME(MASTER, TARGET)%에게 저는 용서받을 수 없는 상대일지도 몰라요」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%가 지금, 이렇게 자유를 빼앗겨 갇혀있는 원인은 저에게 있고」
		PRINTFORMW 「실제로 조교를 하고 심한 일을 하는것도 저이기 때문에」
		PRINTFORMW 「위의 명령이니까 어쩔수 없어, 따위를 말하는것은 저 자신을 정당화하는 이유인것도 있어서」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%에게 어떠한 변명도 되지 못합니다」
		PRINTFORMDW 모미지는 풍성한 꼬리가 %CALLNAME:MASTER%의 허리께에 맴돌았다
		PRINTFORMDW 살짝 빈 손으로 쓰다듬어지는 감각은 기분좋고, 마음이 안심되는 느낌이었다……
		PRINTFORMW 「죄송해요……사과하지 않아도 상관없고, 사과해도 현실은 아무것도 별하지 않지만」
		PRINTFORMW 「그래도 죄송해요……하지못해, %CNAME(MASTER, TARGET)%가 부숴지지 않도록 노력할테니까요」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「옳지, 옳지……괜찮으니까요」
			PRINTFORMDW 모미지는 %CALLNAME:MASTER%의 머리를 위로하듯 쓰다듬었다
			PRINTFORMW 「저도 선배에게 가끔씩 이렇게 위로받곤 해요」
		;얼라이먼트부
		ELSE
;OriginalString : 「よしよし……大丈夫ですからね」
			PRINTFORMW 「옳지, 옳지……괜찮으니까요」
			PRINTFORMDW 모미지는 %CALLNAME:MASTER%의 머리를 위로하듯 쓰다듬었다
			PRINTFORMW 「저도 선배에게 가끔씩 이렇게 위로받곤 해요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT4, 엄하게 위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 할 가능성이 있다
;기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄하게 위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	;IF 구상기입 체크(0) && FIRSTACT(0)
	;ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「ふ――――っ！！」
			PRINTFORMW 「흐────읏！！」
			PRINTFORMDW 송곳니를 노출시킨채, 모미지가 위협의 신음소리를 울렸다
			PRINTFORMDW 말은 말로 귀엽기 그지없었지만,그 눈빛은 늑대에 적합한 위압감을 가지고 있었다
	;アライメント負
		ELSE
			PRINTFORMW 「흐────읏！！」
			PRINTFORMDW 송곳니를 노출시킨채, 모미지가 위협의 신음소리를 울렸다
			PRINTFORMDW 말은 말로 귀엽기 그지없었지만,그 눈빛은 늑대에 적합한 살기를 지니고 있었다
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5, 의상 변경을 요구한다
;────────────────────────────────────
;·조교자가의상의 변경을 요구해 온다
;·조교대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:밧줄가 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K41_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어와 명령했다…"되고 있어 이것이변화 하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		;얼라이먼트정
		IF ALI(0)
;OriginalString : 「で、で、では、ですね」
			PRINTFORMW 「그, 그, 그럼, 다음엔」
			PRINTFORMW 「오, 옷을……벗으세요, 입니다」
			PRINTFORMDW 남자에게 옷을 벗도록 명령하는것에 익숙하지 않았던 것일까, 모미지는 상당히 긴장한 표정으로 명령을 내렸다.
		;アライメント負
		ELSE
			PRINTFORMW 「그, 그, 그럼, 다음엔」
			PRINTFORMW 「오, 옷을……벗으세요, 입니다」
			PRINTFORMDW 남자에게 옷을 벗도록 명령하는것에 익숙하지 않았던 것일까, 모미지는 상당히 긴장한 표정으로 명령을 내렸다.
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「그, 그, 그럼, 다음엔」
			PRINTFORMW 「오, 옷을……벗으세요, 입니다」
			PRINTFORMDW 남자에게 옷을 벗도록 명령하는것이 처음은 아니라고 하는데, 모미지는 여전히 긴장한 표정으로 명령을 내렸다.
		;얼라이먼트부
		ELSE
;OriginalString : 「では、服を脱いでください」
			PRINTFORMW 「그러면, 옷을 벗어주세요」
;OriginalString : 初めての時に比べるとさすがに慣れたのか、모미지は普通に命令を下してきた
			PRINTFORMDW 처음에 비하면 익숙해졌는지, 모미지는 담담하게 명령을 내려 왔다
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「그럼……다음은 옷을 입어주세요」
		PRINTFORMW 「이걸로 끝이냐고요？」
		PRINTFORMW 「죄송합니다, 아직 끝은 아니에요」
	;얼라이먼트부
	ELSE
		PRINTFORMW 「그럼……다음은 옷을 입어주세요」
		PRINTFORMW 「이걸로 끝이냐고요？」
		PRINTFORMW 「아뇨, 아직 끝낼 수 없습니다.」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 합의를 요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀or동정(양쪽 모두의 경우도 있다)
;·이미합의를 얻고 있는 경우는 발생하지 않는다
;·조교대상가처녀에서도동정도 아닌 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를 요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「죄송합니다……여기까지 할 생각은 없었는데」
		PRINTFORMDW 모미지는 %CALLNAME:MASTER%를 엎드린 상태로 만들고,
		PRINTFORMDW 뒤쪽으로 가  %CALLNAME:MASTER%를 위에서 덮쳤다
		PRINTFORMW 「%CNAME(MASTER, TARGET)%를 조교하고있으면, 본능을 억누르기 힘들어져서」
		PRINTFORMDW 뒤에서 밀어붙이고있는 모미지의 손은, 여자의 것이라곤 생각하기 힘들정도로 강했다
		PRINTFORMDW 등에 한줄기 식은땀이 흐른다……
		PRINTFORMDW 마치 보이지않는 송곳니가 목덜미 꽂힌듯한 기분이었다
		PRINTFORMDW %CALLNAME:MASTER%는 본능적으로 사냥당하는 동물의 기분을 피부로 느꼈다
		PRINTFORMDW 다시말해, 모미지는 현재 사냥본능이 완벽하게 표출된 것이다
		PRINTFORMW 「길게 말할것도 없이, 지금, %CNAME(MASTER, TARGET)%를 사냥하고 싶습니다」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음, 받아도 되겠습니까？」
		RETURN 1
	ENDIF
	
	;=================================================
	;호감도 1000 이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애")
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%를 더 기분좋게 해주고싶습니다」
			PRINTFORMW 「먹이로서가 아닌, 한 명의 대등한 상대로서,」
			PRINTFORMW 「송곳니나 손톱이 아닌, 저의 전신을 사용해」
			PRINTFORMW 「그러니까……저를 받아주시겠습니까？」
		;アライメント負
		ELSE
			PRINTFORMW 「좀더 %CNAME(MASTER, TARGET)%의 목소리가 듣고 싶어요」
			PRINTFORMW 「기분좋은듯한 소리도, 아파하는 비명도」
			PRINTFORMW 「제가 모르는 %CNAME(MASTER, TARGET)%의 소리, 좀 더 들려주세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 모미지는 강력한 손으로 %CALLNAME:MASTER%를 억누른채 뒤로 돌아섰다
		PRINTFORMW 「죄송합니다, 슬슬 참을수가 없어요」
;OriginalString : 「\@ ALI(0) ? また、頂いてもいいですか？ # 少しの間、私の獲物になってください \@」
		PRINTFORMW 「\@ ALI(0) ? 또 한번, 받아가도 될까요？  # 잠깐만, 저의 먹이가 되어주세요 \@」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT7, 징계한다고 위협한다
;────────────────────────────────────
;·지금부터징계·한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:징계·플래그는 서 있다
;·턴방침가"소프트", "이상"라면 발생하지 않는다
;·TEQUIP:밀어넘어지는중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K41_7
;-------------------------------------------------
;여기에서는 지문의변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 징계한다고 위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;전COM가 「기분좋게 해」（이 철면피! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("기분좋게 해")
		PRINTFORMW 「잊지 않으셨나요？ 이것은 원래 텐구가 내리는 형별입니다」
		PRINTFORMW 「이 일의 본래 목적을 잊지 말아주세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「더 하드하게 해」（이 돼지째! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("더 하드하게 해")
		PRINTFORMW 「좀 더 강해게, 따위를 말하신다면」
		PRINTFORMW 「저도, 조금 짐승의 본능을 억누르기 힘들어져요？」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「용서를 빌다」（허락해 받을 수 없어했다）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("용서를 빌다")
		PRINTFORMW 「개인으로 용서해드리고싶은 마음은 있습니다만」
		PRINTFORMW 「규칙 상, 용서를 해드릴 수 없는 상황이므로」
		PRINTFORMW 「죄송하지만, 체벌입니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「자위를 시작한다」（인의 이야기를 들어라! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("자위를 시작한다")
		PRINTFORMW 「사람의 말을 듣지 않는다는 것은」
		PRINTFORMW 「보통, 상대가 누구라도 화가 날 거라고 생각하지 않으세요？」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMDW 모미지는 혀를 빼 자신의 입술을 슬쩍 핥았다
			PRINTFORMW 「그다지 난폭한 짓은 하고싶지 않았습니다만, 어쩔 수 없네요」
			PRINTFORMW 「체벌이 불가피, 하니 곧바로 실행하겠습니다」
	;アライメント負
		ELSE
			PRINTFORMW 「이제부터 체벌 시간입니다. 각오해주세요」
			PRINTFORMDW 모미지는 흥분을 감추려하지도 않고, 송곳니를 노출시킨패 입맛을 다셨다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT8, 고함치다
;────────────────────────────────────
;·큰 소리로고함치다
;·턴방침가"소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:징계·플래그에 관련되지 않고, 발생할 가능성이 있다（징계·안에 한층 더고함치다가능성등 ）
;-------------------------------------------------
@KOJO_ACT_K41_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------

;분기 마다의 내용에 차이가 없기 때문에 정리해 버립니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함치다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	PRINTFORMW 「우────！　그르르르르！　아우우────！！」
	PRINTFORMDW 모미지에겐 단순한 고함인것 같다……
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT9, 기분 나쁘게 웃는다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·금일의방침가"노닥노닥"라면 발생하지 않는다
;·화제 『웃음』가 기출이 되어, 이후, 동일조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은변화 한다
;기타·조건없음를 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분 나쁘게 웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;분기 마다의 내용에 차이가 없기 때문에 압축합니다@/L
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「わっふっふっふっ……」
		PRINTFORMW 「우후후후후후후……」
;OriginalString : なんかよく分からんけど怖い……
		PRINTFORMDW 뭔지는 모르겠지만 무섭다……
		RETURN 1
	ENDIF
ENDIF
