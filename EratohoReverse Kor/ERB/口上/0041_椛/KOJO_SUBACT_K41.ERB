;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지구상 탈의·추가 액션구상파일
;
;	조교자가조교지시 이외에 실시하는행동의 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;탈의액션구상
;────────────────────────────────────
;·조교자액션의 앞에강제탈의나자주탈의가 발생했다때에 표시
;·이구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 조교의 흐름으로 완전하게 벗게 하지 않고서노출만 하고 있는 경우도 있다.
;  어쩌면조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_DATUI_K41
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 겉옷, 2, 하반신겉옷, 3, 웃도리, 4, 상반신전부, 5, 하반신전부,
;  6, 악세서리─, 7, 악세서리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항당했다 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(0)
		PRINTFORMW 「옷을 벗어달라는것만으로 여기까지 저항할줄은 생각하지 못했습니다」
		PRINTFORMW 「전도다난前途多難인것 같네요……그럼 어떻게할까요」
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의
		PRINTFORMW 「이제, 저도 벗겠습니다」
		PRINTFORMW 「여성에게 너무 부끄러워하지 말아주세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;얼라이먼트정
		IF ALI(0)
			PRINTFORMW 「이제와서 부끄러워 할 필요는 없을텐데요」
			PRINTFORMW 「저라고해도, 언제까지나 상냥할 수 만은 없다고요？」
		;アライメント負
		ELSE
			PRINTFORMW 「싫은건가요……어쩔 수 없네요」
			PRINTFORMW 「죄송하지만, 조금은 자신의 입장을 이해해주십시오」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(10)
;------------------------------------------------
;·주로섹스계행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;내용적으로전라상정 같기 때문에 조건을 짜 버립니다@/L
IF 구상기입체크(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(10)
		PRINTFORMW 「이렇게 두 사람 모두 알몸이 되는것은 처음이네요」
		PRINTFORMW 「서로 몇 번이나 상대의 알몸을 보고있었을텐데」
		PRINTFORMW 「함께 알몸이 된다니, 왠지 부끄러워요」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가[정욕]상태
	;=================================================
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "정욕")
;OriginalString : 「わぅ……ただでさえ気持ちが高ぶってたんですが」
		PRINTFORMW 「와읏……가뜩이나 흥분해 버렸는데」
		PRINTFORMW 「둘이서 알몸이 되어버리면, 그」
		PRINTFORMW 「또, 참지 못하게 되어버려요」
		PRINTFORMDW 모미지는 자신의 몸을 숨기려하지도 않고 사냥감을 노리는 표정으로 눈앞의 인간을 바라보았다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그럼, 양쪽다 알몸이 됬고하니」
		PRINTFORMW 「옷이 더러워질 걱정도 없겠다, 부담없이 갈게요~」
	RETURN LOCAL
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세서리─마셔 FIRSTDATUI(20)
;------------------------------------------------
;·주로양말. 그 밖에장갑나소매 등
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;전라되었다（벗기 전은양말 이외 알몸이라든지 그러한 느낌）
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의 && ZENRACHECK(0)
;OriginalString : 모미지は最後に残っていた버선を脱いだ……
		PRINTFORMDW 모미지는 마지막으로 남아있던 양말을 벗었다……
		PRINTFORMW 「이걸로, 정말다 벗었습니다……」
		PRINTFORMW 「저의 전부, %CNAME(MASTER, TARGET)%에게 보여지고 있어요……」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「버선は残しておいた方がいいですか？」
		PRINTFORMW 「양말은 남겨두는 편이 좋은가요？」
		PRINTFORMW 「어느쪽이라도, 지금 벗겨버릴거지만요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(30)
;------------------------------------------------
;·이번탈의로성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTDATUI(30)
		PRINTFORMW 「그, 그럼, 갑니다」
		PRINTFORMDW 모미지는 고간을 숨기는 훈도시의 매듭에 손을 가져가 풀어 당겼다
		PRINTFORMDW 긴장하고 있는것인지 손이 떨려, 어색한 손놀림이 되어버렸다
		PRINTFORMDW 역시 남자의 앞에서 속옷을 벗는것은 거부감이 있는지 묻자
		PRINTFORMW 「다, 당연하지요. 남자에게 보이는건 처음이니까요」
		PRINTFORMDW ……지금, 엉뚱한 발언을 들은듯한 기분이 든다
		PRINTFORMW 「아, 너무해요. 『사람을 조교할 정도이니, 이미 몇 번이나 남자에게 몸을 연 문란한 생활을 보내왔겠지』라던지 생각했지요！」
		PRINTFORMDW 아니, 거기까지는 생각하지 않았다만……의외였던건 사실이다
		PRINTFORMDW 남자에게 가랑이를 보이는것이 처음이라는건 당연히……
		PRINTFORMW 「그래요！ 처녀입니다！ 후우……어차피 나는 처녀인데 이런 이상한 일을 하고있는 변태 백랑텐구입니닷！」
		PRINTFORMDW 어쩐지 멋대로 자포자기해 우울해진 모미지. 그렇다면 별로 무리하지 않아도…
		PRINTFORMW 「아─뇨, 벗을겁니다！ 저도 백랑텐구로서의 의지가 있습니다！」
		PRINTFORMDW 의지와 옷을 벗는것이 대체 무슨 상관인데……
		PRINTFORMDW 모미지는 단번에 훈도시의 매듭을 풀었다
		PRINTFORMDW 스륵, 하고 길고 흰 옷감이 바닥에 떨어지고 모미지의 남자를 모르는 하체가 완전히 노출되었다
		PRINTFORMDW 모미지는 손으로 숨기려하고 싶은것을 필사적으로 억누르며, 보라고 과시하는것처럼 허리를 내밀고있었다
		PRINTFORMW 「어, 어떠셨나요？ 조금 흥분하셨나요？」
		PRINTFORMDW 그 어리숙한 행동과, 얕게 솜털이나있는 균열이 기묘한 불쾌함을 자아내고 있었다……
		RETURN 1
	ENDIF
	
	;=================================================
	;응 이렇게 p
	;=================================================
	IF 구상기입체크(100) && PENIS(TARGET)
		PRINTFORMDW 모미지는 고간에 나있는 페니스를 드러내었다……
		PRINTFORMW 「원래 없었던것이라 그런지, 간지러운듯한 이상한 느낌이에요」
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써가슴가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && B_DATUICHECK(0) == 1
		PRINTFORMW 「가슴의 다음은, 당연히 아래인거……군요」
		PRINTFORMDW 모미지는 하반신을 덮는 천을 모두 벗어던졌다
		PRINTFORMDW 소극적인 가슴과 미성숙한 하반신, 모미지의 모두가 훤히 드러났다……
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「어, 어떤가……요. 여, 여자아이의 그곳……은」
		PRINTFORMDW 모미지는 자신의 가장 소중한 곳을 보여지는 수치에 몸을 떨면서, 비밀장소를 손가락으로 넓혔다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(40)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && !TALENT:거유 && FIRSTDATUI(40)
;OriginalString : 唖然とする%CALLNAME:MASTER%의 前で、모미지は가슴のサラシを解いていった
		PRINTFORMDW 맥없이 웃는 %CALLNAME:MASTER%의 앞에서, 모미지는 가슴의 무명천를 풀어내렸다
		SIF ALLSAMES(!TALENT:거유, !TALENT:빈유)
;OriginalString : 大きくもないが小さくもない、손にちょうど収まる程度の形のいい가슴が露わ되었다
			PRINTFORMDW 크지도, 작지도 않은, 손에 딱 맞는정도의 모양 좋은 가슴이 드러났다
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써성기가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && P_DATUICHECK(0) == 1
;OriginalString : 「なんだか順番が違う気もしますが……これで全部見せちゃってるんですね……」
		PRINTFORMW 「왠지 순서가 다른 기분이 듭니다만……이걸로 전부 보여져버렸네요……」
		PRINTFORMDW 아래에 이어 위쪽도 벗은 모미지의 소중한 곳은 지금 모두 훤히 드러나있었다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「どうですか？　私のおっぱい」
		PRINTFORMW 「어떤가요？ 제 가슴」
		PRINTFORMW 「크기엔 그다지 자신이 없지만……보아도 손해는 없다고 생각하는데요？」
		PRINTFORMW 「……아니, 그렇지만 그렇게 지긋이 보셔도 조금 곤란하다고할까」
		PRINTFORMW 「그, 저도, 사실은 조금 부끄럽다고할까」		 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(50)
;------------------------------------------------
;·이번탈의로팬티（캐릭터에 의해드로와즈·트렁크）가 보이도록(듯이)되었다경우에 표시
;·하반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;처음와 그 외의 내용적으로 바꾸어 넣고가 타당하다라고 판단 섬했다@/L
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(50) && FIRSTDATUI(30, 1)
		PRINTFORMW 「어째서 훈도시인가, 인가요？」
		PRINTFORMW 「확실히 산기슭의 인간이나 요괴분들은 드로워즈를 입으시는분이 많지만……」
		PRINTFORMW 「아, 아야씨도 있었군요」
		PRINTFORMW 「하지만, 하카마를 입으면 드로워즈는 조금 방해가 되기때문에 저희들은 모두 훈도시를 입습니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 모미지はゆっくりと、穿いている袴を下げて行った
		PRINTFORMDW 모미지는 천천히 입고있는 하카마를 아래로 밀어내렸다
		PRINTFORMDW 하얀 훈도시를 입은 모미지의 하반신이 노출되었다……
		PRINTFORMDW 모미지의 사타구니에 흰 옷감이 먹혀들어가는 눈앞의 광경에, 무심코 군침을 삼켜버렸다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(60)
;------------------------------------------------
;·이번탈의로 브라（캐릭터에 의해스포츠브라·사라시）가 보이도록(듯이)되었다경우에 표시
;·상반신속옷가 없는 캐릭터는 쓰는필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(60) && FIRSTDATUI(40, 1)
		PRINTFORMDW 모미지는 윗옷을 걷어올려 단번에 벗어던졌다
;OriginalString : その下からは、サラシを巻いた가슴が\@ !TALENT:거유 ? 慎ましやかに # \@顔を覗かせた
		PRINTFORMDW 그 아래에는, 무명천을 두른 가슴이\@ !TALENT:거유 ? 다소곳하게 # \@얼굴을 드러내고 있었다
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
;OriginalString : 「その、色気がなくてすみません」
		PRINTFORMW 「그, 색기가 없어서 죄송합니다」
		PRINTFORMDW %CALLNAME:MASTER%는 무명천이 잘 어울려, 라고 정직하게 대답했다
		PRINTFORMW 「정말인가요？ 빈말이어도 기쁘네요」
		PRINTFORMDW 그래도 모미지는 조금 기가죽은 표정으로 귀를 살랑살랑 흔들었다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(70)
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && TFLAG:자주탈의 && ZENRACHECK(0) && ! IS_NOWASSIACTNAME(, "탈의")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세서리─마셔 FIRSTDATUI(80)
;------------------------------------------------
;·주로양말. 그 밖에장갑나소매 등
;·탈이했다중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 == 6
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(80)
		PRINTFORMW 「그럼, 양말도 벗도록해요」
		PRINTFORMDW 모미지는 %CALLNAME:MASTER%의 양말을 억지로 벗겨, 상당히 먼 곳으로 던져버렸다
		PRINTFORMDW ……그렇게나 냄새가 심한건가.
		PRINTFORMDW %CALLNAME:MASTER%가 묻자, 모미지는 황급히 고개를 가로저었다
		PRINTFORMW 「그, 그런건 아니에요. 단순히 제 코가 민감하니까, 약한 냄새에도 쉽게 반응해서 그러는 것 뿐이에요」
		PRINTFORMDW 역시 강아지같아……라고 생각했지만, 입밖으론 내놓지 않았다
		RETURN 1
	ENDIF
	
	;=================================================
	;전라되었다
	;=================================================
	IF 구상기입체크(100) && ZENRACHECK(1)
		PRINTFORMW 「양말정도는 괜찮겠지, 라고요？」
		PRINTFORMW 「안됩니다. 전부 벗는것에 의미가 있으니까요」
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그럼, 양말은 이쪽에 두겠습니다」
		PRINTFORMDW 모미지는 상당히 떨어진곳에 벗긴 양말을 정중하게 내려놓았다
		PRINTFORMDW ……역시 냄새때문인걸까
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(90)
;------------------------------------------------
;·이번탈의로남성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(90)
		PRINTFORMW 「와흣？！」
		PRINTFORMDW %CALLNAME:MASTER%의 하반신이 노출되자, 모미지는 손으로 자신의 눈을 가리고 뒷걸음질쳤다
		PRINTFORMDW 그렇다고쳐도, 지금은 조교하는 쪽이라고는 생각되지 않는 신선한 반응이었는데, 설마……
		PRINTFORMW 「그, 그래요！ 남성의 물건을 보는건 처음이라구요！」
		PRINTFORMW 「하, 하지만, 이렇게 크고 그로테스크할 것이라곤 생각하지 못했으니까아……」
		PRINTFORMDW ……이제와서이지만, 텐구들은 인원 선발에 큰 실수를 한게 아닐까？
		RETURN 1
	ENDIF
	
	;=================================================
	;상냥하게 벗긴다（호감도 1000 이상·얼라이먼트정）
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애") && ALI(0)
		PRINTFORMW 「우와─우와─, 오늘도 또 훌륭하네요……」
		PRINTFORMW 「익숙해지면, 어쩐지 이것도 나름대로 사랑스럽게 보여와 이상한 기분이네요」
		;남자의 입장에서는 꽤 위화감이 있었는 장면이었다
		IF TALENT:MASTER:남자
			PRINTFORMDW 아마 모미지에게 타의는 없을테지만, 남자로서
			PRINTFORMDW 「그것 귀엽네」라는 소리는 가슴에 상처를 남기기에 충분했다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「후─하─후─하─……좋아, 마음의 준비가 되었습니다. 자아」
		PRINTFORMDW 모미지의 눈앞에 드러난 %CALLNAME:MASTER%를, 모미지는 진지한 눈으로 바라보있었다……
		PRINTFORMDW 무사의 결투도 아니니까, 거기까지 기합을 넣고 보지 않아도……
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(100)
;------------------------------------------------
;·이번탈의로 여자성기가 보이도록(듯이)되었다경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(110)
;------------------------------------------------
;·이번탈의로 젖가슴이 보이도록(듯이)되었다경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·조교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(120)
;------------------------------------------------
;·이번탈의로하반신속옷가 보이도록(듯이)되었다경우에 표시
;·조교대상가하반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(120) && FIRSTDATUI(90, 1)
		;조교대상이남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「과연……남성은 이런 속옷을 입는것이군요」
			PRINTFORMW 「훈도시와는 전혀 다른거죠？」
		;調教対象が후타나리
		;조교대상가후타나리
		;ELSEIF TALENT:MASTER:후타나리
		;조교대상가 여자
		ELSE
			PRINTFORMW 「좋겠다……세련된 속옷이네요」
			PRINTFORMW 「저도……저도……」
		ENDIF
		RETURN 1
	ENDIF
	
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		;조교대상이남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「저기……실례이지만, 속옷, 제대로 세탁한건가요？」
			PRINTFORMDW 희미하게 눈썹을 찌푸리면서 모미지가 물어왔다
			PRINTFORMDW 그렇게 냄새가 심한걸까, 하고 충격을 받았지만,
			PRINTFORMDW 그러고보니 모미지는 백랑텐구. 즉, 늑대였다는것을 기억해냈다
			PRINTFORMDW 아마 후각이 인간의 몇 십배에 달하니, 속옷의 냄새에도 민감한 것이다
		;조교대상가후타나리
		;ELSEIF TALENT:MASTER:후타나리
		;조교대상가 여자
		ELSE
;OriginalString : 「そういえば、もし生理の時期되었다ら言ってくださいね」
			PRINTFORMW 「맞다, 만약 생리주기가 오면 말해주세요」
			PRINTFORMW 「조교도 벌이라곤 하지만, 최소한의 존엄과 환경은 이쪽에서 확보할 생각이니까요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(130)
;------------------------------------------------
;·이번탈의로 브라상반신속옷가 보이도록(듯이)되었다경우에 표시
;·조교대상이남자라고 표시하지 않기 때문에, 남자한정구상에서는 쓰는필요가 없다
;·조교대상가상반신속옷가 없는 캐릭터 한정의구상이면 쓰는필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:남자
	;=================================================
	;처음
	;=================================================
	;IF 구상기입 체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(13)
	;ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「멋진 속옷이네요」
		PRINTFORMW 「저는 기능성만을 중시하고있기 때문에……」
		PRINTFORMW 「저도 가끔씩은 당신같은 귀여운 속옷을 입고싶어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(140)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라되었다」라고 하는 의미
;·「겉옷 밖에 남지 않은상태로겉옷를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입하는필요는 없다（상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(150)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(150)
		PRINTFORMDW 모미지는 입고있는 옷을 스스로 벗기 시작했다
		PRINTFORMW 「저기, 너무 빤히 보지 말라주세요？」
		PRINTFORMDW 그렇게말해도, 다른 사람 앞에서 옷을 벗는것에 익숙하지 않은걸까,
		PRINTFORMDW 가끔 우물쭈물, 벗는것에 고전하는 모미지의 모습은 위험할정도로 사랑스러웠다
		PRINTFORMDW %CALLNAME:MASTER%는 모미지의 탈의에서 눈을 떼지 못했다……
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 모미지는 벗은 옷을 정중하게 옆에 내려놓았다
		PRINTFORMW 「그럼, 옷을 벗었으니 다음에는 무엇을 할까요？」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(160)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의
	;=================================================
	;처음
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(160)
		PRINTFORMDW 모미지는 %CALLNAME:MASTER%의 위를 덮고있는 옷에 손을 대었다
		PRINTFORMDW 모미지의 완력은 가녀린 팔의 소녀라고는 생각되지 않을정도로 강해서, %CALLNAME:MASTER%의 저항은 간단하게 억눌려버렸다
		PRINTFORMW 「너무 저항한다면 옷을 찢어버릴겁니다？」
		PRINTFORMDW 조교가 끝난 후에도 입을것이 없다고 생가갛면 과연 따르지 않을 수 없었다
		PRINTFORMDW %CALLNAME:MASTER%는 어쩔 수 없이 저항의 손길을 거두었다……
		RETURN 1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 모미지는 벗은 옷을 정중하게 옆에 내려놓았다
		PRINTFORMW 「그럼, 옷을 벗었으니 다음에는 무엇을 할까요？」
		RETURN 1
	ENDIF
ENDIF

;========================================================================
;추가 액션구상
;========================================================================
;·조교자가추가행동를 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
;────────────────────────────────────
;EXTRA1, Ｃ애무
;────────────────────────────────────
;·추가로애무 또는수음를 실시한다
;·조교대상에페니스가 없는 경우는애무, 어느 경우는수음
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_1
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무 한다
;-------------------------------------------------
;·조교대상에페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && ! PENIS(MASTER)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 수음 한다
;-------------------------------------------------
;·조교대상에페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입 체크(0) && PENIS(MASTER)
ENDIF
[SKIPEND]
;────────────────────────────────────
;EXTRA2, Ｖ애무
;────────────────────────────────────
;·추가로 바지애무를 실시한다
;·조교대상이남자의 경우 발생하지 않는다
;·조교대상가처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_2
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA3, Ａ애무
;────────────────────────────────────
;·추가로애널애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_3
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ애무
;────────────────────────────────────
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_4
;-------------------------------------------------
;IF 구상기입 체크(0)
;ENDIF

;────────────────────────────────────
;미약등사용구상
;────────────────────────────────────
;·조교자가미약나로션를 사용했다때에 범용적으로 표시
;·「액션구상」로 기입 체크를=2로했다경우는 표시되지 않는다
;·쓰는 경우, 액션구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K41
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TFLAG:미약등사용구상, 0)
	;얼라이먼트정
	IF ALI(0)
		PRINTFORMW 「끈적끈적, 기분좋으신가요？」
		PRINTFORMW 「저는 조금 서둘러서」
		PRINTFORMW 「꼬리에 붙어버리면 좀처럼 떨어지지 않네요, 이것은」
	;얼라이먼트부
	ELSE
		PRINTFORMW 「칠칠맞지 못하네요 」
		PRINTFORMW 「전신 빠짐없이 미끈미끈하게 해드릴게요. 기분좋은가요？」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했다경우는입옮기기가 되기 (위해)때문에, 그 케이스는키스의 액션구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TFLAG:미약등사용구상, 1)
	;얼라이먼트정
	IF ALI(0)
;OriginalString : 「何を飲ませたか、ですか？」
		PRINTFORMW 「무엇을 먹인거냐,고요？」
		PRINTFORMW 「기분이 좋아지는 약입니다」
		PRINTFORMW 「괜찮아요. 인간이 마셔도 해는 없으니까」
		PRINTFORMW 「조금이라도 기분좋아지는편이 좋을거라고 생각해서」
	;얼라이먼트부
	ELSE
		PRINTFORMW 「무엇을 먹인거냐,고요？」
		PRINTFORMW 「후후, 말하지 않아도 알고있지 않나요？」
		PRINTFORMDW 모미지는 열을 띠기 시작한 목덜미에 혀를 기게 해왔다……
	ENDIF
	RETURN 1
ENDIF
