;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아큐구상 수치계 액션구상파일
;
;	수치계 액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;수치계
;────────────────────────────────────
;·TFLAG:ACT(40=자위/41=음부공개/42=자위과시/43=수치플레이/44=야외플레이/45=방뇨/46=코스프레(미구현))
;·TFLAG:ACT분류 == 4에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "수치")로 줍는 것이 가능
;·ACT에 따라서는거부가 가능하고 있거나 않기도 하고
;-------------------------------------------------

;*************************************************
;ACT40, 자위
;*************************************************
;·자위 해 보여라라고 명령한다
;·이 단계에서 명령에 따르는지 아닌지는 미정
;·REACT분류가소극적으로 한다/태연하게 한다/적극적으로 한다/받아들이는 이외의 경우는, 모두 REACT분류"거부"취급
;·턴방침가"이상"라면 발생하지 않는다
;·밀어넘어지는중인 경우는 발생하지 않는다
;·벌써자위중인 경우는 발생하지 않는다
;·TEQUIP(밧줄/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이 ACT에 파생은 없다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_40
;=================================================
;·지문에 분기는 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어가는 경우 있어
;·조교대상의 페니스의 유무, 페니스없음의 경우는페니스밴드의 유무에 의한 나누어 쓰고가 생각된다
;·조교대상이남자이면쾌Ｂ는 발생하지 않는다
;-------------------------------------------------

;텐프레로서는상태 마다 첫회 분기를 나누는 것도 어떨까라고 생각했으므로 통합해 최상위에 배치
;파생마다 첫회 판정했다 있고 경우는 개별의 분기에 분리해, FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2와 같이하면 좋다)
;조교대상에페니스가 없는 경우, 페니스밴드분기와 그 외 분기의 양쪽 모두에 떨어질 가능성이 있으므로
;FIRSTACT의이벤트번호를 가지런히 해 버리는지, 감히 쌍방을 살릴까는 쓰기손나름
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;조교대상에페니스 있어		TALENT:MASTER:남자/TALENT:MASTER:후타나리등으로 구별 차지지 않은 것도 없다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && PENIS(MASTER)
;OriginalString : 「……そうですね、それでは……」
		PRINTFORMW 「……좋아요、저 그럼……」
;OriginalString : 「早速なんですけど、オナニーをしてみて貰えませんか？」
		PRINTFORMW 「갑자기 죄송하지만、오나니 해 주실 수 없으신가요？」
;OriginalString : 
		PRINTFORMDL
;OriginalString : にこやかに要求して来る아큐に対し頭を振る。
		PRINTFORMDW 방긋 웃으며 요구하는 아큐에게 고개를 저었다。
;OriginalString : 
		PRINTFORMDL
;OriginalString : 「だって、흥미が有るじゃないですか」
		PRINTFORMW 「그치만、호기심이 이는걸요」
;OriginalString : 「당신様が普段、どんな風におちんちんを擦って、どういう顔でイクのか、とか……」
		PRINTFORMW 「당신이 평소、어떤 식으로 자지를 비비고、어떤 얼굴로 가버리는걸까、하고……」
;OriginalString : 「ちゃーんと見せてくれれば、その分気持ちの良い봉사が出来る様になるんですよ？」
		PRINTFORMW 「제ー대로 보여주시면、그 기분 좋은 느낌대로 봉사해 드릴 수 있게 된다구요？」
;OriginalString : 
		PRINTFORMDL
;OriginalString : 可愛らしく言っては居るが、아큐はこの「お願い」を撤回する気は無いようだ……
		PRINTFORMDW 아큐는 귀엽게 말하고 있었지만、그 「부탁」을 번복할 생각은 없는 듯하다……
		RETURN LOCAL:1
	ENDIF
	;=================================================
	;페니스밴드장착중			당연한일이면서, 페니스는 없다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:페니스밴드
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상의 분기를 모두 묻으면 여자 MASTER 분기에 상당
	;방치 해 이 개소에만 한다면 범용적으로 기술하는 것을 추천
	;=================================================
	;그 외						녀 MASTER로페니스밴드를장착 하고 있지 않는 경우
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;MASTER가남자, 혹은후타나리인 경우
;기타·조건없음를 쓴 다음, 필요에 응해 분기를 증감해 주세요
;개소에 따라서는 TALENT:MASTER:남자든지 TALENT:MASTER:후타나리로 별라고도 좋다고 생각합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상에페니스가 있는 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && PENIS(MASTER)
	;=================================================
	;전COM가가게해줘!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("가게해줘!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가기분좋게 해
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("기분좋게 해")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교대상가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:MASTER:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자도자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;오나홀장착중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:오나홀
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;애널비즈/애널바이브장착중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:애널비즈 || TEQUIP:애널바이브
		;애널비즈사용중
		IF TEQUIP:애널비즈
;OriginalString : 
			PRINTFORMW 
		;애널바이브사용중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;만족보너스로"하드"를 선택하고 있는 경우는징계·플래그가 립 하며 가 되므로
	;LOCAL:1 = PERCENT(X)로 확률에했다 (분)편이 좋은 경우도 있다
	;=================================================
	;징계·모드
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:징계·플래그
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
;OriginalString : 「ねぇ、オナニーを見せて下さいませんか」
			PRINTFORMW 「저어、오나니를 보여주시겠어요?」
;OriginalString : 「お一人で気持よく成る様を、この目に焼き付けとうございます……♪」
			PRINTFORMW 「당신 혼자서 기분 좋아지는 모습을、이 눈에 새겨넣을 생각이에요……♪」
;OriginalString : 
			PRINTFORMDL
;OriginalString : 아큐は華の様に微笑み、慇懃な입調で命令を下す……
			PRINTFORMDW 아큐는 마치 꽃처럼 미소짓으며、정중한 말투로 명령을 내렸다……
		ELSE
;OriginalString : 「挽回の機会を与えましょう」
			PRINTFORMW 「만회할 기회를 드리지요」
;OriginalString : 「股を見せつけるように開いて蹲踞になり、ご自分でその貧相な包茎をしごいて下さい」
			PRINTFORMW 「다리를 벌리고 앉아서、스스로 그 궁상맞은 포경 자지를 비비세요」
;OriginalString : 「거부権は有りませんよ？　さぁ、早く」
			PRINTFORMW 「거부할 권리는 없어요？　자아、어서」
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;필요에 응해 이 네스트를 개방해, 분기를 더한 다음 사용해 주세요
;! PENIS(MASTER) 분기에 통합해 버리는손도 있습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상가페니스밴드를장착 하고 있는 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TEQUIP:페니스밴드
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;필요에 응해 분기를 증감해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상에페니스가 없는 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !PENIS(MASTER)
	;=================================================
	;전COM가가게해줘!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("가게해줘!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가기분좋게 해
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("기분좋게 해")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교대상가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:MASTER:가버려
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자도자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;바이브/애널바이브장착중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:바이브 || TEQUIP:애널바이브
		;바이브사용중
		IF TEQUIP:바이브
;OriginalString : 
			PRINTFORMW 
		;애널바이브사용중
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;만족보너스로"하드"를 선택하고 있는 경우는징계·플래그가 립 하며 가 되므로
	;LOCAL:1 = PERCENT(X)로 확률에했다 (분)편이 좋은 경우도 있다
	;=================================================
	;징계·모드
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:징계·플래그
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT41, 음부공개
;*************************************************
;·조교자가조교대상의 비패를 공개 한다. 소위 「쿠빠아」
;·조교대상이남자의 경우는 발생하지 않는다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"의 경우는 발생하지 않는다
;·TEQUIP:삼각목마장착중은 발생하지 않는다
;·밀어넘어지는중는 발생하지 않는다
;·성교중/성교봉사중는 발생하지 않는다
;·이 ACT에 파생은 없다
;·현재 리액션 후의@ACTION_APPLY2에 처리 없음
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_41
;=================================================
;·안면승마, 자위중는 해제된다
;·하반신전부강제탈의의 처리 있어
;-------------------------------------------------

;TEQUIP(눈가리개/밧줄) 등의 분기를 마련해 주는 것도 재미있을지도 모릅니다
;필요에 응해 분기를 증감해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT41, 음부공개
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;처음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		;조교대상가처녀
		IF TALENT:MASTER:처녀
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교대상가처녀
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:처녀
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
;OriginalString : 
			PRINTFORMW 
		;윤활불발
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
;OriginalString : 
			PRINTFORMW 
		;윤활불발
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT42, 자위를과시한다
;*************************************************
;·조교자가 과시하도록(듯이)자위를 시작한다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·조교자가 벌써자위중인 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_42
;=================================================
;조교자욕망, 기교, 조교레벨, 조교자기교, 손가락기술소질에 응해쾌Ｃ/쾌ＢSOURCE 증가
;바이브소지의 경우의 처리는 0203판 시점에서는봉인 되고 있다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에페니스가 있는 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && PENIS(TARGET)
	;=================================================
	;조교대상도자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================

ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드장착중
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TEQUIP:조교자페니스밴드
	;=================================================
	;조교대상도자위중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에페니스가 없는 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !PENIS(TARGET)
	;=================================================
	;처음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
			PRINTFORMW 「당신……부끄럽지만、들어 주시겠어요……？」
			PRINTFORMDL
			PRINTFORMDW 균열에 살풋이 손가락을 얹은 채、부들부들 쾌감에 떨고 있는 목소리。
			PRINTFORMDW 어린 아이라고는 도저히 생각할 수 없는 손놀림에、%CALLNAME:MASTER%(은)는 꿀꺽 하고 숨을 삼켰다。
			PRINTFORMDL
			PRINTFORMW 「아무래도 전、태어난 직후부터 성지식이 있었으니까요……」
			PRINTFORMW 「저、주위의 아이들보다 훨씬、『이런』 일을 시험하는 게 빨랐어요」
			PRINTFORMW 「나이 다섯 살 즈음에는 이미、클리토리스를 자극하는 기술을、익혔다고요……？」
			PRINTFORMDL
			PRINTFORMDW 손가락의 움직임이 한층 더 격렬해졌고、쮸욱쮸욱 하는 물소리가 울려퍼지며、
			PRINTFORMDW 달디 단 교성이 %CALLNAME:MASTER%의 뇌리에 새겨지고 있었다。
			PRINTFORMDL
			PRINTFORMW 「당신은……이렇게나 정숙하지 못한 저를、『여자』로 만들어 주시겠나요……？」
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;눈가리공개착중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:눈가리개
		;조교대상도자위중
		IF GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
			PRINTFORMW 
		;그 외
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교대상도자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 0)
;OriginalString : 
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;구속중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:밧줄
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
			PRINTFORMW 「당신……너무나、부끄러운 말이지만요……」
			PRINTFORMDL
			PRINTFORMDW 음란하게 다리를 벌린 채、균열에 손가락을 집어넣으며 아큐는 말했다。
			PRINTFORMDL
			PRINTFORMW 「보이면서……그、가는 게、버릇이 되어버린 것 같아요」
			PRINTFORMW 「아큐의 오나니……봐 주시겠어요……？」
			PRINTFORMDL
			PRINTFORMDW 달콤하게 불태우는 듯한 물소리가 %CALLNAME:MASTER%의 심지를 떨게 했다……。
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT43, 수치플레이
;*************************************************
;·조교대상를거울의 전에 데리고 간다
;·TFLAG:징계·플래그가 서 있으면 발생하지 않는다
;·턴방침가"이상"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP:삼각목마장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이미수치플레이중이라면, 수치플레이종료가 된다
;-------------------------------------------------
;TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_43
;=================================================
;·현상 본체측에서는노출SOURCE 증가의 처리가 있을 뿐
;-------------------------------------------------

;요컨데 개시, 종료시에게만 표시되는 개소. 지문도 매정함
;간결하게 끝마쳐 버리는지, 감히 열중한 일을 할까는 쓰기손나름
;TCVAR:MASTER:상태변화등으로 나누는 것도 재미있을지도 모릅니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT43_0, 수치플레이개시
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;처음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;조건으로서는 레어이지만 현상, 이 가능성은 일단 고려해 두는 것에 월했다일은 없을까라고 한다
	;조교도구가 다수 있는 경우, 한 번의조교에 사용할 수 있는도구의 수를 규제할까? 그렇다고 하는 이야기도 있으므로
	;장래적으로는 미묘한 서는 위치의 분기일지도 모른다
	;=================================================
	;눈가리공개착중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:눈가리개
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT43_1, 수치플레이종료
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT44, 야외플레이
;*************************************************
;·조교대상에개목걸이를 붙여 밖에 데리고 나간다
;·턴방침가"소프트", "노멀"라면 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·TEQUIP(밧줄/삼각목마)장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이미야외플레이중이라면, 야외플레이종료가 된다
;-------------------------------------------------
;TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_44
;=================================================
;개시시에자위중, 안면승마중인 경우, 그것들은 해제된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT44_0, 야외플레이개시
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;처음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT44_1, 야외플레이종료
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT45, 방뇨
;*************************************************
;·오줌누어 보여라라고 명령한다
;·이 단계에서는 명령에 따를지 어떨지 확정하고 있지 않다
;·REACT분류가소극적으로 한다/태연하게 한다/적극적으로 한다/받아들이는 이외의 경우는, 모두 REACT분류"거부"취급
;·턴방침가"소프트"라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드)장착중에는 발생하지 않는다
;·밀어넘어지는중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·소변(BASE:MASTER:소변)가 5000 이하라면 발생하지 않는다
;-------------------------------------------------
;ACT파생없음
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_45
;=================================================
;·IF LOCAL로서 1개에 모아 버려도 괜찮은 생각도 듭니다
;·다만, 구래부터 태연히 지나가고 있는 응석 긴 IF네스트를 박멸하는 것을 시선했다시스템인 것으로 마주 잡을 수 있는 아않다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처음
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;조교대상이남자의 경우
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:남자
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교대상가후타나리의 경우
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:후타나리
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기서 범용적으로 기술할까 상층의 분기에서 개별적으로 나누어 쓸까는 쓰기손나름
	;모두 묻는다면, 조교대상가 여자인 경우가 됩니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상이남자의 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TALENT:MASTER:남자
	;=================================================
	;징계·모드
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:징계·플래그
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상가후타나리의 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TALENT:MASTER:후타나리
	;=================================================
	;징계·모드
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:징계·플래그
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교대상가여자의 경우
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !PENIS(MASTER)
	;=================================================
	;징계·모드
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:징계·플래그
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
