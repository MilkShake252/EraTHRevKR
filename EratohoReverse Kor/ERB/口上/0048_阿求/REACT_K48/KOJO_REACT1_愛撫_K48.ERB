;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아큐구상 애무계 개별 리액션구상파일
;
;	애무계 리액션구상를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;애무계
;────────────────────────────────────
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:애무계의 경우, 기본적으로애무계 COM(COM10~COM16)가선택지가 됩니다만
;·상황에 따라서는 그 외의 것이선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻는필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT 및 ACT파생내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 리액션에 관련되지 않고, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·요컨데, 상정당했다시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  기본적으로 지문은 TFLAG:REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를 빌다/소극적으로 한다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운 행동/무리없는 행동/무리한 행동)로 파생
;  ACT_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;  우선 ACT파생 마다 테두리를 분리해 있습니다만, 1개에 모은 다음 범용적인 기술로손간을 줄인다고 하는손도 있습니다
;  정직, 방식은 얼마든지 있으므로, 이하에 나타내는 방법은 하나의 예라고 생각해 주세요
;-------------------------------------------------

;*************************************************
;REACT10, 손으로 애무·개별 리액션
;*************************************************
;·TFLAG:ACT파생(0=애무/1=수음/2=페니스밴드수음)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_10
;=================================================

;ACT파생 0에 대해서는 참고로서쾌락계 COM를 모두 망라되어 있습니다
;필요에 응해 분기를 증감해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애무
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;싫어했다					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고 모욕한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM13, 기분 나쁘게 웃는다(REACT분류"태연하게 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 나쁘게 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 1는 파생 0으로 같은 종류의 것을 다른 형태로 제시해 보겠습니다
;아무튼 이런 사용 방법도 있다고 하는 일로
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 수음
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")

		;=================================================
		;사정직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「……안ー돼욧、가는 얼굴 제대로 보여주시지 않으면 싫어욧」
			PRINTFORMW 「심술 부리시면、도중에 멈춰버릴 거예요？」
			PRINTFORMW 「확실하게 뷰웃 뷰웃하는 모습도、아큐에게 보여 주세요♪」
		ENDIF

		IF CFLAG:153 == 0		;CFLAG:153은 대입을 하지 않기 때문에 0밖에 되지 않습니다. ELSE 이후는 죽어구상가 됩니다@/L
			PRINTFORMW 「자극이 너무 강했나요？」
			PRINTFORMW 「그게 아니면……후후、유혹하시는 건가요？」
		ELSE
			PRINTFORMW 「그대로 좀 반성하고 계세요」
			PRINTFORMW 「남자의 존엄에 대한 거니까요、이 조루」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고 모욕한다")

		;=================================================
		;사정직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
;OriginalString : 「
			PRINTFORMW 「
		ENDIF

		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;손간을 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 쾌감을 받아들인다/기분 좋아!
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋아!")
	
		;=================================================
		;사정직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「에에、제게 전부 맡겨 주세요」
			PRINTFORMW 「이 손가락과、부드러운 손바닥으로……당신의 특농 정ー액、확실하게 짜내 드릴 테니까요」
		ENDIF

		;소비기력에 의한 파생
		IF CFLAG:153 == 0
			SELECTCASE GET_COM_RANK()
				;자연스러운 행동
				CASE "자연스러운 행동"
					PRINTFORMW 「뭐어……칭찬해 드릴게요 잘 하셨어요、당신」
					PRINTFORMW 「이 상태 그대로、아큐의 기술에 녹아 주세요♪」
				;무리없는 행동
				CASE "무리없는 행동"
					PRINTFORMW 「뭐어……칭찬해 드릴게요 잘 하셨어요、당신」
					PRINTFORMW 「이 상태 그대로、아큐의 기술에 녹아 주세요♪」
				;무리한 행동
				CASE "무리한 행동"
					PRINTFORMW 「조금、솔직해지신 것 같아서……기뻐요♪」
					PRINTFORMW 「물론、앞으로도 훈련을 계속할 생각이지만요」
			ENDSELECT
		ELSE
			PRINTFORMW 「……무참하게 허덕이시기만 할 생각인가요？」
			PRINTFORMDW 아큐는 눈쌀을 찌푸리며、페니스를 잡아당겨 손가락으로 강하게 쥐었다……
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류 2, 태연하게 한다＆COM, 기분 나쁘게 웃는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("기분 나쁘게 웃는다")
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;COM가 방해/애원에 파생했다등 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적으로 한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을 참는다") && TFLAG:REACT파생 < 1
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 0/1을 참고에 확장하든지, 통합하든지
;그런 (뜻)이유로 여기는 최소한으로 해 둡니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드수음
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;COM12, 서투르다고 모욕한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고 모욕한다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT10, 손으로 애무

;*************************************************
;REACT11, 가슴애무·개별 리액션
;*************************************************
;·0203판 현재, 지문은 ACT파생 2, 몸싸움과 그 외로 분기
;·ACT파생 2, 몸싸움에 대해 REACT분류(소극적으로 한다/태연하게 한다/적극적으로 한다/받아들인다) 이외는"거부"취급
;-------------------------------------------------
;  파생마다 온전히 하면손간이 심상치 않기 때문에, ACT파생분기의 상층에서 범용 처리했다
;  각 ACT파생내의 COM 분기등은 일부의 것에 좁혀 버리는 것이 좋다고 생각합니다
;  또 ACT파생로 단락짓지 않고, 상당히기로 정리해 버리는손도 있습니다만, 손가 붐빈 일을 하기에는 적합하지 않다라고 생각합니다
;-------------------------------------------------
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_11
;=================================================
;정해진 사용 방법 따위는 없기 때문에, 이하에 나타내는 것은 어디까지나 하나의 예라고 하는 일로
;분기의 증감, COM에 흘리는지 아닌지의 판단은 작자의 판단에 맡깁니다
;-------------------------------------------------
;-------------------------------------------------
;COM 단체의 분기 ACT파생의 상층에서 처리
;-------------------------------------------------

;ACT파생 2는 거동이 다르므로 조건으로 연주한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다") && TFLAG:ACT파생 !=2
	;=================================================
	;쾌감에 대해서부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 상황에 대해서부끄러워한다취급
	;파생 1을방치했다경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("싫어한다")
	;ACT파생 2의 경우는 ACT파생 2 분기내의 IS_COMGRONAME("거부")로 처리
	;=================================================
	;싫어해 방해했다(REACT분류 6, 날뛴다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1 && TFLAG:ACT파생 !=2
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 싫어한(REACT분류 1, 소극적으로 한다) 취급
	;파생 1을방치했다경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;=================================================
;COM12, 서투르다고 모욕한다
;기입 체크
LOCAL = 0
;=================================================
IF LOCAL && IS_NOWCOMNAME("서투르다고 모욕한다")
	;=================================================
	;강한척 해(REACT분류:소극적으로 한다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 미숙한 테크닉에 대해서(REACT분류:태연하게 한다) 취급
	;파생 1을방치했다경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, ACT파생 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 가슴애무
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM13, 기분 나쁘게 웃는다(REACT분류"태연하게 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 나쁘게 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;조교자에 소질(유치/혀기술)이 없는 경우는 발생하는 확립은 낮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 유두 들이마셔
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM13, 기분 나쁘게 웃는다(REACT분류"태연하게 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 나쁘게 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;봉사계 COM가선택지가 되는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 몸싸움
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;요구에 응하지 않았다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("거부")
		;부끄러워해
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
;OriginalString : 
			PRINTFORMW 
		;싫어해
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;-----------------------------------------------------------------
	;이하, COM를 병렬화
	;손간을 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;-----------------------------------------------------------------
	
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 쾌감을 받아들인다/기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋아!")
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	;=================================================
	;REACT분류 2, 태연하게 한다＆COM, 기분 나쁘게 웃는다/태연하게 한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("기분 나쁘게 웃는다/태연하게 한다")
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류 2, 적극적으로 한다＆COM, 적극적으로 한다/애정을 담아 한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("적극적으로 한다") && IS_NOWCOMNAME("적극적으로 한다/애정을 담아 한다")
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;COM 싫어해 방해/애원에 파생했다등 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적으로 한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을 참는다") && TFLAG:REACT파생 < 1
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;조교자에 소질(유치/혀기술)이 없는 경우는 발생하는 확립이 높다
;조교대상이남자의 경우는 여기에 밖에 떨어지지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 3, 유두 고문
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 3
	;=================================================
	;COM13, 기분 나쁘게 웃는다(REACT분류"태연하게 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 나쁘게 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT11, 가슴애무

;*************************************************
;REACT12, 입으로 애무·개별 리액션
;*************************************************
;  파생마다 단락지어 있습니다만, 예를 들면 ACT파생 1만 써 뒤는 범용에 던진다고 하는 일도 할 수 있습니다
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=펠라)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_12
;=================================================

;ACT파생 0에 대해서는 참고로서쾌락계 COM를 모두 망라되어 있습니다
;필요에 응해 분기를 증감, 없고 구조를 개변해 사용해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 쿤닐링구스
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;싫어했다					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고 모욕한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM13, 기분 나쁘게 웃는다(REACT분류"태연하게 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 나쁘게 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 1는 파생 0으로 같은 종류의 것을 다른 형태로 제시해 보겠습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 펠라
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고 모욕한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;손간을 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 쾌감을 받아들인다/기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋아!")
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류 2, 태연하게 한다＆COM, 기분 나쁘게 웃는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("기분 나쁘게 웃는다")
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;COM가 방해/애원에 파생했다등 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적으로 한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을 참는다") && TFLAG:REACT파생 < 1
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT12, 입으로 애무

;*************************************************
;REACT13, 애널애무·개별 리액션
;*************************************************
;·지문은 ACT파생로 분기. ACT파생 1, 2 분기내의 파생 없음
;·ACT파생 0, 애널애무만 REACT분류로 파생 처리된다
;-------------------------------------------------
;·TFLAG:ACT파생（0=애널애무/1=전립선자극/2=애널바이브출납）
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_13
;=================================================

;ACT파생 0에 대해서는 참고로서쾌락계 COM를 모두 망라되어 있습니다
;필요에 응해 분기를 증감, 없고 구조를 개변해 사용해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애널애무
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")

		;=================================================
		;사정직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「괜찮아요、엉덩이로 가버리는 건 절대 부끄러운 게 아니에요……♪」
			PRINTFORMW 「그도 그럴 게 히에다의 기술이 너무도 기분 좋아서니까요？」
			PRINTFORMW 「당신은 조금도 나쁘지 않으니까、부끄러워 하지 마시고 기분 좋아지세요……」
		ENDIF

		IF CFLAG:153 == 0
			PRINTFORMW 「역시、엉덩이를 다른 사람에게 보이는 건 부끄러운가 보군요……」
			PRINTFORMW 「그렇지만、여자아이는 모두 하고 있는 일이라고요？」
			PRINTFORMW 「그러니까、이렇게 괴롭힘당하는 것도……점점、기분 좋아지실 거에요♪」
		ELSE
			PRINTFORMW 「울먹일 정도로 부끄러우시다면、어째서 페니스를 발기하고 계신 건가요？」
			PRINTFORMW 「결국、당신 같은 인간이 히에다에 거스르는 건 불가능한 거예요」
			PRINTFORMW 「계집애 하나에 엉덩이 구멍을 쑤셔져서、일어서는 것조차 불가능한걸요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;싫어했다					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고 모욕한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고 모욕한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM13, 기분 나쁘게 웃는다(REACT분류"태연하게 한다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 나쁘게 웃는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋아!")

		;=================================================
		;사정직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「에에、자지 뒤쪽을 자극당하는 쾌감은、어떠신가요？」
			PRINTFORMW 「히에다의 기술에 가버리는 걸 맛보면、버릇이 되어버려도 이상하지 않다고요……？」
			PRINTFORMW 「엉덩이를 쑤시지 않은 날은 뭔가 부족해서 좀이 쑤시고、결국엔 자기 손가락으로 엉덩이를 헤집게 되어……」
			PRINTFORMW 「만약 그렇게 되시더라도、아큐는 당신을 사랑할 테니까요？」
		ENDIF

		IF CFLAG:153 == 0
			PRINTFORMW 「후후……당신도 완전히、히에다의 손기술에 포로가 되신 것 같군요……」
			PRINTFORMW 「전립선에、아큐가 새긴 증표……」
			PRINTFORMW 「이걸로、만약 어디로 가버린다고 하더라도、제 곁으로 돌아오게 되겠네요♪」
		ELSE
			PRINTFORMW 「마침내 저항하지도 않게 되었나요……」
			PRINTFORMW 「아뇨、상관 없어요。부디、이루는대로 가만히 계셔 주세요」
			PRINTFORMW 「저도、그럴 생각으로 훈련시킨 거니까요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;조교대상이남자의 경우에만 파생
;ACT파생 0를 참고에 확장하든지, 통합하든지
;그런 (뜻)이유로 여기는 최소한으로 해 둡니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 전립선자극
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다/COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 0를 참고에 확장하든지, 통합하든지
;그런 (뜻)이유로 여기는 최소한으로 해 둡니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 애널바이브출납
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;COM14, 쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
		;파생:애원					REACT분류"용서를 빌다"
		IF TFLAG:REACT파생 == 1
;OriginalString : 
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을 받아들인다/COM16, 기분 좋아!
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다/기분 좋아!")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT13, 애널애무

;*************************************************
;REACT14, 항문펠라·개별 리액션
;*************************************************
;·지문은 REACT분류로 분기
;·ACT의 파생 없음
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_14
;=================================================
;ACT파생는 없기 때문에 COM 분기에서 완전하게 단락짓습니다
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;쾌감에부끄러워한다
	IF TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
	;상황에부끄러워한다
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("싫어한다")
	;싫어해 방해했다			REACT분류"날뛴다"
	IF TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
	;싫어했다					REACT분류"소극적으로 한다"
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고 모욕한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("서투르다고 모욕한다")
	;강한척 해					REACT분류"소극적으로 한다"
	IF TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
	;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 기분 나쁘게 웃는다(REACT분류"태연하게 한다")
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분 나쁘게 웃는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을 참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
	;파생:애원					REACT분류"용서를 빌다"
	IF TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
	;파생:내구					REACT분류"소극적으로 한다"
	ELSE
;OriginalString : 
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋아!
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분 좋아!")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT14, 항문펠라

;*************************************************
;REACT15, 키스·개별 리액션
;*************************************************
;·지문은 REACT분류로 분기
;·ACT의 파생 없음
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_15
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
	;=================================================
	;REACT파생 1, 애원(REACT분류"용서를 빌다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 썼을 경우는 REACT파생 0(파생:내구) 취급
	;IF LOCAL:1 && TFLAG:REACT파생 < 1등으로 해서, REACT의 파생 1을 삭제해 COM로 처리하는 것도 있어
	;=================================================
	;그 외·REACT부하파생
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;소비기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
;OriginalString : 
				PRINTFORMW 
			;무리없는 행동
			CASE "무리없는 행동"
;OriginalString : 
				PRINTFORMW 
			;무리한 행동
			CASE "무리한 행동"
;OriginalString : 
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
ENDIF

;1개로 정리한 다음 && TFLAG:REACT부하 < 1등으로 해서
;특정의 파생은 COM로 처리라든지, 그런 일도 가능합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")
	;소비기력에 의한 파생
	SELECTCASE GET_COM_RANK()
		;자연스러운 행동
		CASE "자연스러운 행동"
;OriginalString : 
			PRINTFORMW 
		;무리없는 행동
		CASE "무리없는 행동"
;OriginalString : 
			PRINTFORMW 
		;무리한 행동
		CASE "무리한 행동"
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋아!
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분 좋아!")
	;소비기력에 의한 파생
	SELECTCASE GET_COM_RANK()
		;자연스러운 행동
		CASE "자연스러운 행동"
;OriginalString : 
			PRINTFORMW 
		;무리없는 행동
		CASE "무리없는 행동"
;OriginalString : 
			PRINTFORMW 
		;무리한 행동
		CASE "무리한 행동"
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	RETURN LOCAL
ENDIF
;REACT15, 키스

;*************************************************
;REACT16, 성기를 서로 비빈다·개별 리액션
;*************************************************
;TFLAG:ACT파생（=0, 조개 우려라/=1, 스마타/=2, 페니스밴드스마타）
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_16
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 조개 우려라
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 스마타
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드스마타
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT16, 성기를 서로 비빈다

;*************************************************
;REACT17, 파이즈리한다·개별 리액션
;*************************************************
;·지문은 REACT분류로 분기
;·ACT의 파생 없음
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_17
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL&& IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 받아들인다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT17, 파이즈리한다

;*************************************************
;REACT18, 풋잡하기·개별 리액션 FIRSTTIME(218)
;*************************************************
;=================================================
@KOJO_REACT_K48_18
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을 받아들인다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을 받아들인다")

		;=================================================
		;사정직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「이런이런……괜찮으신가요？　이대로라면、당신에게 정액이 쏟아지게 될 텐데요？」
			PRINTFORMW 「양 발가락에 엉망진창으로 휘저어져……사정해도、괜찮으신 건가요？」
			PRINTFORMW 「알겠어요……부ー디、건강한 정액을 하늘 높이 뿜어 주세요」
			PRINTFORMW 「아큐를 교배시킬 때의 기세를、견학하게 해 주세요……♪」
		ENDIF

		IF CFLAG:153 == 0
			PRINTFORMW 「완전히 버릇이 되어버리셨네요、어린 아이에게 짓밟히는 것에」
			PRINTFORMW 「그다지 밖에 나가본 적이 없으니까요、아큐의 발바닥 탄력있고 부드럽죠？」
			PRINTFORMW 「마치 이런 식으로、자지에 문지르기 위해 만들어진 것처럼……♪」
		ELSE
			PRINTFORMW 「음낭을 유녀（어린 여아）의 발에 마음대로 짓밟히고서、저항할 마음도 없으신가요？」
			PRINTFORMW 「그게 아니면、제가 실제로는 심한 짓을 하지 않으리라、얕보고 계신 건가요」
			PRINTFORMW 「확실히、익숙해져 버려서 곤란한 건 저도 마찬가지에요……이렇게나 무방비하면、마가 낀다고들 하죠……？　쿡쿡」
		ENDIF 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명을 지른다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명지른다")
		IF CFLAG:153 == 0
			PRINTFORMW 「……안 돼요、그렇게 귀여운 목소릴 내시면……」
			PRINTFORMW 「그럴 생각이 없었는데、좀 더 괴롭히고 싶어지잖아요……♪」
		ELSE
			PRINTFORMW 「도와줄 사람 따위 없다고요？　집안사람들은、제가 무슨 짓을 하는지 이미 알고 있고 말이죠」
			PRINTFORMW 「……아아 그렇지、다음엔 어느 정도 조교가 진행됐는지 집안사람들에게 보여주는 것도 괜찮겠네요……」
		ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;날뛴다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("날뛴다") && !GROUPMATCH(TFLAG:REACT파생, 0, 1)
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT18, 풋잡하기

;*************************************************
;REACT19, 손가락삽입
;*************************************************
;TFLAG:ACT파생(0=손가락삽입/1=Ｇ스팟 자극/2=바이브출납)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_19
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 손가락삽입
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, Ｇ스팟 자극
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 바이브출납
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;조교대상가후타나리
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:후타나리
		;-------------------------------------------------
		;쾌감을 받아들인다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;쾌감을 참는다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;조교대상에페니스없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && !PENIS(MASTER)
		;-------------------------------------------------
		;쾌감을 받아들인다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;쾌감을 참는다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT19_1, Ｇ스팟 자극
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0 ;off
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;조교대상가후타나리
	;기입 체크
	LOCAL:1 = 0 ;off
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:후타나리
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교대상에페니스없음
	;기입 체크
	LOCAL:1 = 0 ;off
	;=================================================
	IF LOCAL:1 && !PENIS(MASTER)
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT19_0, 손가락삽입
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해미약등구상를 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;쾌감을 받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 받아들인다")
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을 참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을 참는다")
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;*************************************************
;REACT27, 밀어 넘어트린다·개별 리액션
;*************************************************
;TFLAG:ACT파생（=0, 밀어 넘어트린다/=1, 밀어넘어짐종료）
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_27
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 밀어 넘어트린다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;REACT분류받아들인다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다")
;OriginalString : 「……愛してます、당신様……」
		PRINTFORMW 「……사랑하고 있어요、당신……」
		RETURN LOCAL:1
	ENDIF
	;=================================================
	;REACT분류적극적으로 한다/태연하게 한다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		PRINTFORMW 「이렇게、저승까지 함께해 주신다면……」
		PRINTFORMW 「이 얼마나、사치스러운 일일까요」
		RETURN LOCAL:1
	ENDIF

	;=================================================
	;REACT분류용서를 빌다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("용서를 빌다")
		IF CFLAG:153 == 0
			PRINTFORMW 「그런……싫으신가요？」
			PRINTFORMW 「요괴도 아니고、잡아먹을 리가 없는데도……」
		ELSE
			PRINTFORMW 「이건 벌이에요？　종마가 쓸모 없어지면、도살장에 보내는 게 당연하잖아요？」
			PRINTFORMW 「그게 싫다면……알고 있겠죠？」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류소극적으로 한다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적으로 한다")
		IF CFLAG:153 == 0
			PRINTFORMW 「아직은、전부 받아들이기엔 너무 딱딱하려나……」
			PRINTFORMW 「그치만……천천히、사랑을 다져가도록 하죠、당신」
		ELSE
			PRINTFORMW 「이제부터、당신 자지에 연속으로 자극을 가할 거에요」
			PRINTFORMW 「이번에는 확실하게、참아 주세요、종마 씨」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 밀어넘어짐종료
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
ENDIF
