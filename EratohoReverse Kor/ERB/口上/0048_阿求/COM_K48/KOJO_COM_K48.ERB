;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;		[아큐]구상 범용 리액션구상파일
;
;	개별 리액션(REACT)으로부터 빠져 온 것을 해당 COM로 처리하는 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;=====================================================================
;범용 리액션구상
;=====================================================================
;·개별 리액션구상를 표시하지 않았던 경우에 표시
;·KOJO_REACT_KX1_Y1의 RESULT가 0이었을 경우, 범용 리액션이 불려 간다
;·REACT로 표시했다인가 아닌가는 RESULT로 판정되므로, REACT로 표시해 RETURN 0으로 한층 더 범용도 표시시킨다일도 가능한 것이긴 함
;·KOJO_COM_KX1_Y1로부터 RESULT 0이 돌아가면 통괄 범용 리액션(KOJO_REACT_KX)이 불려 간다
;---------------------------------------------------------------------
;간이 플로우
;    표시 타이밍·조교대상의 리액션（커맨드·반응）
;        액션번호별의, 개별 리액션구상
;            표시하지 않았던 경우, 커맨드번호별의 범용 리액션구상←(지금 여기)
;                표시하지 않았던 경우, 통괄 범용 리액션구상
;---------------------------------------------------------------------
;관련 변수/함수류…
;
;ACT분류계
;TFLAG:ACT분류 이 턴의조교메뉴(0=회화/1=애무/2=도구/3=성교/4=수치/5=봉사/6=가학/7=이상/8=사역/9=휴식/10=성교봉사)
;GET_ACTM ACT번호로부터 ACT분류번호를취득 하는 함수
;MENUMATCH 지정했다ACT분류에 포함되어 있으면 진을반환(회화/애무/도구/성교/수치/봉사/가학/이상/사역/휴식/성봉)
;
;COM계
;TFLAG:REACT파생 각 SELECTCOM의 파생. COM마다 파생 내용은 다르다
;TFLAG:REACT부하 선택했다COM의기력소비를 판별한다(0=자연스러운 행동/2=무리없는 행동/무리한 행동)
;COMCOR_POSI 주로소극적으로 한다계 COM의기력소비가 큰 때(TFLAG:REACT부하 == 2인 경우)에, COM보정치가호의적인가 어떤가를 판단
;
;그 외
;TFLAG:REACT인상 각 SELECTCOM의 상손에의 심상(0=영향없음/1=좋은인상/2=약간좋은인상/3=나쁜인상/4=악인상대/5=0에서멀어짐/6=0에가까워짐)
;TFLAG:징계·포인트( 100모이면징계·개시)
;FIRSTCOM  첫회 범용 리액션 판정 함수  SELECTCOM를 자동으로 참조하므로, 현재선택했다COM 마다번호가 독립해 보존된다
;ONCECOM  조교중첫회 판정용 함수. FIRSTTIME계와 달라조교전에 초기화된다. 기본적인 사양은 FIRSTCOM와 같이
;RANDCOM(AUTORAND계)  RAND 처리로이벤트종류마다의번호을반환가, 전종 볼 때까지 같은 숫자는 돌려주지 않는다. 모두 표시했다때에 초기화.
;
;---------------------------------------------------------------------

;=====================================================================
;※ACT분류와 그 파생에 대해
;
;COM에 따라서는 발생하는 ACT분류가 복수에 걸칩니다
;각 COM내를 TFLAG:ACT분류로 나누어 주는 일에 의해, 보다 정확하게 상황을 참조하는 것이 가능합니다
;다만, ACT분류자체도 파생하는 것이 있어, 이 경우, TFLAG:ACT분류에서는 파생 상황을 참조 할 수 있지 못하고, 약간 이상한 일이 됩니다
;
;예를 들면…, ACT55, 발핥기강제는 본래 ACT분류:봉사계입니다만, 이상계로부터 발생하는 경우가 있어,
;TFLAG:ACT분류의 단락에서는, 상정 그대로의구상가 표시되지 않는다고 말하는 것이 생각됩니다
;
;거기서 TFLAG:ACT분류는 아니고, @MENUMATCH든지@GET_ACTM로 ACT분류 마다의 분기를 마련해 주면, 파생처를 참조하는 것이 가능하게 되어
;실제로 행해지고 있는 ACT분류로 처리되게 됩니다
;IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "봉사")와 같은 테두리를 마련해 주면, 위에 올린 것 같은 상황에도 대응이 가능합니다
;MENUMATCH의 분류 마다의 정의에 대해서는 이 파일의 28행째를 참조해 주세요
;
;ACT분류파생 일람…
;（각 수치는 ACT번호）
;회화 (기분 0/호의 1/지식 2/위로 3/위협 4/의상 5/합의 6/처분 7/고함치다 8/비웃는 9/매도 66)
;애무 (손으로 10/가슴을 11/입으로 12/Ａ손 13/Ａ입 14/키스 15/Ｃ만짐 16/Ｂ만짐 17/발로 18/로터 20/압도 27)
;도구 (로터 20/바이 21/A바이 22/A비 23/Ｃ사랑 24/유두 25/관장 68)
;성교 (정상 30/후배 31/승마 32/대면 33/배면 34/ＡＳ35)
;수치 (자위 40/공개 41/보이고 42/거울 43/야외 44/방뇨 45/코스(미구현)46/방치 73)
;봉사 (손으로 50/입으로 51/가슴로 52/스마타 53/발지 55/이라 56/승마 32/가슴을 57)
;가학 (평손 60/회초리 61/바늘 62/줄 63/눈가림 64/입 형벌 도구 65/매도 66/목마 67/관장 68/이라 56)
;이상 (Fist70/ＡFi71/양Fi72/방치 73/공중 74/비웃는 9/발로 18/발지 55/목마 67)
;사역 (미구현)
;휴식 (휴일 90/복약 91/치료 92)
;성교(봉사) (정상 95/후배 96/승마 103/대면 97/배면 98/ＡＳ99)
;
;=====================================================================
[SKIPSTART]
MENUMATCH소분기 견본…

다른 분기와의우선도에 주의해, 필요에 응해 각 COM에 추가해 사용해 주세요
	;=================================================
	;ACT분류, 회화
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "회화")
	ENDIF
	;=================================================
	;ACT분류, 애무
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "애무")
	ENDIF
	;=================================================
	;ACT분류, 도구
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "도구")
	ENDIF
	;=================================================
	;ACT분류, 성교
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성교")
	ENDIF
	;=================================================
	;ACT분류, 수치
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "수치")
	ENDIF
	;=================================================
	;ACT분류, 봉사
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "봉사")
	ENDIF
	;=================================================
	;ACT분류, 가학
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "가학")
	ENDIF
	;=================================================
	;ACT분류, 이상
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "이상")
	ENDIF
	;=================================================
	;ACT분류, 휴식
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "휴식")
	ENDIF
	;=================================================
	;ACT분류, 성봉
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성봉")
	ENDIF
[SKIPEND]
;---------------------------------------------------------------------

;※각 분기에는 대응하는 지문을 제시되어 있습니다만, 최종적으로 어떠한 형태로 할까 아직 뭐라고도 말할 수 없는 곳

;=====================================================================
;회화계 반응 COM0 접수대
;=====================================================================
;·주로 ACT분류:회화계/휴식계 시에 발생한다
;·발생은 트리거이벤트제. 특정의 ACT가 트리거가 된다
;·ACT분류:회화에 대해서는 REACT로 완결시킨다의 것이 바람직함
;-------------------------------------------------

;────────────────────────────────────
;COM0, 침묵한다
;────────────────────────────────────
;·따랐던 것에는 안 된다
;·ACT1, 지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다의 경우는 REACT파생 1, "질문에 답하지 않는다"되어징계·포인트가산
;·REACT인상:나쁜인상
;·의존도변화(-1, -1)
;-------------------------------------------------
;·ACT가지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다의 경우는 파생 1, 질문에 답하지 않는 취급
;·그 외의 경우는 파생 0, 단순한 침묵
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류 0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성적인 이야기를 한다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분 나쁘게 웃는다
;ACT분류 2:도구
;	ACT:밧줄/눈가리개/볼개그/삼각목마
;ACT분류 4:수치
;	ACT:수치플레이/야외플레이
;ACT분류 6:가학
;	ACT:매도
;ACT분류 7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류 9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;--------------------------------------------------
;·TFLAG:REACT파생(0=단순한 침묵/1=질문에 답하지 않는다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_0
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다의 경우에 파생한다
	;=================================================
	;REACT파생 1, 질문에 답하지 않는다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		IF KFLAG:153 == 0
			PRINTFORMW 「대답해 주시겠어요？」
			PRINTFORMW 「모처럼이니、부부 사이에 친목을 도모하고 싶은데……안되나요？」
		ELSE
			PRINTFORMW 「……무언、인가요」
			PRINTFORMW 「뭐、변명을 하는 것만큼 보기 흉하지는 않네요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;말을 삼켜, 입다물고 있었다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분이 안좋은 것 같게 밀기묵하며 였다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 아무것도 말하지 않았다…
		IF KFLAG:153 == 0
;OriginalString : 「」
			PRINTFORMW 「」
		ELSE
;OriginalString : 「」
			PRINTFORMW 「」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%의 모습을 엿보는 것 같은 기색을 보였지만,
		;아무것도 말하지 못하고 입다물고 있었다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「……너무 조용히 계시면、좀 편하지가 않아요」
			PRINTFORMW 「저기……저、가능한 한 능숙하게 할 테니까요」
		ELSE
			PRINTFORMW 「……」
			PRINTFORMDW 아큐 역시、뚱한 표정으로 이쪽을 내려다보고 있다
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM1, 마음 약하게 응답한다
;────────────────────────────────────
;·REACT분류는 파생에 의해변화
;·ACT("엄하게 위협한다/징계한다고 위협한다/고함치다/밀어 넘어트린다")로 일정 조건을 채우면 REACT파생 1, 애원에 파생
;·REACT인상:약간좋은인상
;·의존도변화(0, +1)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성적인 이야기를 한다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분 나쁘게 웃는다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
;·TFLAG:REACT파생(0=마음 약하게 응답한다/1=애원 한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_1
;=================================================
;·REACT파생로 내용이 다르므로 파생마다 나누어 버립니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다(REACT분류:용서를 빌다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 응석부린 기분을 뿌리쳐,
			;허약하면서, 벌써 그만두고라고 말했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하고,
			;이제(벌써) 이런 일 그만두고라고 말했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 심약구, 이제(벌써) 이런 일 그만두고라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서
		;이제(벌써) 이런 일 그만두고라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「마음에 들지 않으시나요？」
			PRINTFORMW 「아큐의 작은 그곳、찌걱찌걱하게 휘저어서……」
			PRINTFORMW 「당신의 끈적거리는 정액을、질에 드……음뿍 싸 주시지 않으면、곤란해요」
			PRINTFORMW 「그렇죠？　조금만 더、힘내 주세요……？」
		ELSE
			PRINTFORMW 「그 무슨 바보 같은 말을……」
			PRINTFORMW 「그렇게까지 한심하게 간원하실 거라면、다음에는 좀더 참아주세요」
			PRINTFORMW 「이래 가지고는 그저 자업자득이에요？」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 마음 약하게 응답한다(REACT분류:소극적으로 한다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신의 기분에 희미하게 깨달으면서도,
			;마음이 약하게 거리를 취하는 것 같은 대답을했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하게 대답했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 심약구대답했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서대답했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:애태움도
			IF KFLAG:153 == 0
				PRINTFORMW 「몸을 뒤로 젖힌 채 움찔거리면서도 반응해 주시다니……」
				PRINTFORMW 「아큐、기뻐요。좀 더 커다란 목소리、내 주세요……」
			ELSE
				PRINTFORMW 「자지를 괴롭혀져서 그렇게 바보같이 대답한다는 건、그 정도가 한계인 거네요」
				PRINTFORMW 「역시、좀 더 혹독하게 조교할 필요가 있겠네요」
			ENDIF
			KFLAG:151 += 1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMW 「그렇게 사양하지 않으셔도 괜찮다고요？」
				PRINTFORMW 「우리들은 이미 부부니까요……좀 더 다정하게 불러 주세요。」
			ELSE
				PRINTFORMW 「시시한 대답이군요。좀 더 재미있는 비명은 없나요？」
				PRINTFORMW 「뭐、괜찮아요。그 한심한 상판만큼은 일생 잊지 않고 기억해 드리죠」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM2, 무뚝뚝하게 응답한다
;────────────────────────────────────
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·징계·포인트가 가산된다
;·REACT인상:나쁜인상
;·의존도변화(-2, -2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성적인 이야기를 한다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분 나쁘게 웃는다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_2
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게 응답한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;아무것도 아니면 무뚝뚝하게 손질했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 저항을 담아 무뚝뚝하게 응했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 무뚝뚝하게\@ IS_NOWACTNAME("밀어 넘어트린다") ? 대답 # 반응 \@했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심지의 강한 눈동자로%CALLNAME%를 응시해 무뚝뚝하게 응했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「……뭐어、이제부터니까요？　그렇죠？」
			PRINTFORMDW 아큐는 자신을 격려하듯이 중얼거렸다。
		ELSE
			PRINTFORMW 「……」
			PRINTFORMDW 아큐는 차가운 눈으로 %CALLNAME:MASTER%을 쏘아보고 있다……。
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM3, 상냥하게 응답한다
;────────────────────────────────────
;·REACT분류:적극적으로 한다
;·REACT인상:좋은인상
;·의존도변화(+3, +1)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성적인 이야기를 한다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_3
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 상냥하게 응답한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%는 비위를 맞추려고 일부러 웃는 얼굴로 응했다…
		PRINTFORMW 「으ー응……뭐어、마음만은 받아 둘게요」
		PRINTFORMW 「좀 더、자연스럽게 계셔 주세요」
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 애교 좋게\@ IS_NOWACTNAME("밀어 넘어트린다") 반응했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 기쁜듯이%CALLNAME%에 미소지어,
		;애교 좋게 대답했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:애태움도
			IF KFLAG:153 == 0
				PRINTFORMW 「기분 좋으신가요？　후후、기뻐요。」
				PRINTFORMW 「좀 더 좀 더 솔직하게、울어 주실 수 있도록、한층 더 단련할게요」
			ELSE
				PRINTFORMW 「그래요、착한 아이시네요 멍멍이 씨。시킨 대로 『기다려』도 할 수 있게 됐네요？」
				PRINTFORMW 「그게 아니라 가축어가 아니면 알아들을 수 없는 건가요……？」
				PRINTFORMW 「자 그럼、멍멍머ー엉」
			ENDIF
			KFLAG:151 += 1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMDW 아큐는 뿅 하고 얼굴을 새빨갛게 물들인 후、손으로 뺨을 가리며 아양을 부렸다。
				PRINTFORMW 「싫어요、부끄러워요、당신……」	
				PRINTFORMW 「다、다시 한 번 말씀해 주시겠어요……？」
			ELSE
				PRINTFORMW 「……말씀은 잘 하시네요。」
				PRINTFORMW 「뭐어 좋아요。나는 귀가 먹은 남근쪽은 제 손으로 훈륙하면 되니까요」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM4, 강하게 응답한다
;────────────────────────────────────
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·ACT, 밀어 넘어트린다의 경우는 REACT파생 1, "밀어넘어짐에 굴하지 않는다"에 파생
;·REACT인상:나쁜인상
;·의존도변화(-1, -3)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성적인 이야기를 한다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분 나쁘게 웃는다/의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:밀어 넘어트린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_4
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게 응답한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 밀어 넘어트린다의 경우 파생
	;=================================================
	;REACT파생 1, 밀어넘어짐에 굴하지 않는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%는 이제 그만두라고 강하게 말했다…
;OriginalString : 
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;%CALLNAME%에는 관계없으면 강하게 말했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 강한척 해 응했지만, 소리는 조금 떨고 있다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 강해 대답했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 노려봐, 강해 응했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
;OriginalString : 아큐は心なしかしょんぼりとしている。
		PRINTFORMDW 아구는 마음 탓인지 멍하니 하고 있다.
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM5, 빈정거린다
;────────────────────────────────────
;·ACT, 지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다의 경우는 REACT분류:거부
;·그 이외의 경우는 REACT분류:태연하게 한다가 된다
;·징계·포인트가산
;·REACT인상:나쁜인상
;·의존도변화(-3, -1)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:밧줄/눈가리개/볼개그/삼각목마
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_5
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는솔직인 기분을입으로 하려고했다가,
			;왠지 부끄러워져, 야유 섞임에 응했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 작은 악의를 말에 혼합해 따끔 반했다…
			;말 있고는했다의 것의, 기분을 해치지 않은가 끊임없이 신경쓰고 있다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 빈정거리면서도, 말하는 일에는솔직에 따라 보였다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 여유를 보이는 것 같은 태도로 홀연히 따르면서,
		;야유 섞임에 응답했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「네、후후후。」
			PRINTFORMW 「농담할 수 있을 정도로、마음을 터놓으신 것 같으니 다행이에요」
			PRINTFORMW 「회화가 없는 것보다는 훨씬 나으니까요」
		ELSE
			PRINTFORMW 「……호？　이 히에다의 여식과 입으로 다투려 하시는 건가요？」
			PRINTFORMW 「해보시지요。듬뿍……씹어드릴 테니까」
			PRINTFORMDW 사악한 웃음을 띄운 아큐에게、철저하게 논파당해버렸다……
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM6, 더 하드하게 해
;────────────────────────────────────
;·REACT분류:받아들인다
;·조교자의 소질, BASE:초조함등에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·REACT파생 1의 경우는징계·포인트가산
;·REACT인상:0에서멀어짐
;·의존도변화(0, +3)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/상냥하게 위로한다
;		징계한다고 위협한다
;ACT분류:1:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/키스/성기를 서로 비빈다/파이즈리한다
;ACT분류:2:도구
;	ACT:Ｃ애무도구/유두캡/밧줄/눈가리개/볼개그/삼각목마
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스펭킹
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_6
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「이 철면피의 암퇘지놈!」
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	PRINTFORMW 「……좀 더 격렬하게 해줬으면、합니까？」
	PRINTFORMDL
	PRINTFORMDW 아큐를 감싸고 있던 분위기가、어딘지 꺼끌꺼끌하게 변했다。
	PRINTFORMDL 
	PRINTFORMW 「당신이 그렇게 말씀하신다면 상관 없습니다만……」
	PRINTFORMW 「무너져버리지 말아주세요、돼지 씨？」
	PRINTFORMDL
	PRINTFORMDW 정숙한 분위기를 떨쳐버리고、새디스틱하게 비웃는 아큐에게 %CALLNAME:MASTER%(은)는 등줄기를 떨었다……
	KFLAG:153 = 1
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SOURCE:MASTER:쾌Ｃ + SOURCE:MASTER:쾌Ｖ + SOURCE:MASTER:쾌Ａ + SOURCE:MASTER:쾌Ｂ > 10
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 반 자포자기가 되어, 좀 더하드로 하면 좋다고 말했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 좀 더 굉장한 것당했다실 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 굉장한 일 하고라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 기분을 정리하면서말을 선택해,
			;%CALLNAME%로 된다면 좀 더하드라도 좋다고 말했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자포자기가 되어, 좀 더하드로 하면 좋다고 말했다…
			;말 있고는했다의 것의, 입술을 딱 닫아 어깨를 진동시키고 있다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는흥미반으로, 더 하드하게 해도 좋다고 말해 보았다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 좀 더 기분이 좋은 일을 해 주었으면 해져,
		;무심코더 하드하게 해라고 말해 버렸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「저……좀 더 괴롭혀 줬으면、하신、건가요？」
			PRINTFORMW 「당신이 말씀하시는 거라면 선처하겠습니다만……조금、특이하군요」
		ELSE
			PRINTFORMW 「하아……반성은 커녕、즐기고 계셨을 줄이야」
			PRINTFORMW 「실로 실망스러운 휴지조각 같네요、불면 날아가버릴 것처럼 말이죠？」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM7, 용서를 빌다
;────────────────────────────────────
;·REACT분류:용서를 빌다. 파생했다경우는 REACT분류:없음
;·조교자의 소질, BASE:이성, 얼라이먼트에 의해 REACT파생 1, 「의견을 내다니 좋은 번가슴군요!」(이)가 된다
;·턴방침가이상의 경우는 파생 1이 되기 쉽다
;·파생 1의 경우는징계·포인트가산
;·REACT인상:0에가까워짐
;·의존도변화(0, +2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성적인 이야기를 한다/상냥하게 위로한다
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분 나쁘게 웃는다
;ACT분류:1:애무
;	ACT:애널애무/항문펠라/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:애널바이브/애널비즈/관장세트/페니스밴드
;ACT분류:3:성교
;	ACT:후배위/배면좌위/항문섹스
;ACT분류:4:수치
;	ACT:자위/음부공개/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스펭킹/채찍/바늘
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착/피스트퍽/애널피스트/양쪽피스트/공중육변기플레이
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:역강간
;
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=의견을 내다니 좋은 번가슴군요!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_7
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를 빌다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 공중육변기플레이에서는 파생하지 않는다
	;=================================================
	;REACT파생 1, 의견을 내다니 좋은 번가슴군요!
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		IF KFLAG:153 == 0
			PRINTFORMW 「……앗……괴로워、지신 건가요？」
			PRINTFORMW 「그치만、안 돼요。그렇게 우는 소리만 하셔서는、시간이 얼마가 지나도 저를 임신시킬 수 없다고요？」
			PRINTFORMW 「자、쓰다듬 쓰다듬 해 드릴 테니까 힘내 주세요」
		ELSE
			PRINTFORMW 「아우성치는 것보다 행동으로 보여 주시는 게 어떤가요？」
			PRINTFORMW 「이、쓰레기를、조금은 쓸 수 있게 해 보세요、라고 말하고 있는 거라고요？」
			PRINTFORMDW 아큐는 말의 리듬을 타고 탁탁 손가락으로 국소를 튕겼다。
			PRINTFORMDW %CALLNAME:MASTER%의 몸에 전기 같은 자극이 흘렀다……
			PRINTFORMW 「……후훗、좀 더 체벌이 필요한 것 같네요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 이상해지고 있는 것을 자각해,
			;더 이상 이상하게 되는 것은 무섭다고 말했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 아첨하도록(듯이)%CALLNAME%를 올려보면, 이제(벌써) 허락해와 바랐다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 이제(벌써) 허락하고라고 해 출했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 눈물을 머금어%CALLNAME%에 이제(벌써) 허락해 주세요간원했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:애태움도
			IF KFLAG:153 == 0
				PRINTFORMW 「후후……싸고 싶으신 거군요……？」
				PRINTFORMW 「부드럽게、상냥하ー게 이끌어 드릴 테니까……」
				PRINTFORMW 「최고의 타이밍에 뷰룻뷰룻、해버리는 게 기분 좋을 거예요……？」
			ELSE
				PRINTFORMW 「용서를 비는 것 정도는 학습하신 거군요」
				PRINTFORMW 「자 그럼……교육은 사탕과 채찍이 중요하다고 하니까요……」
			ENDIF
			KFLAG:151 += 1
			RETURN LOCAL:1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMW 「그렇게 심한 짓을 한 적은 없는데요……」
				PRINTFORMW 「그、저와 같이 있는 게、싫으신 건가요……？」
				PRINTFORMDW 아큐는 눈물을 글썽이며 당신을 올려다보고 있다……
			ELSE
				PRINTFORMW 「찰나적인 열락에 한심하게 빠져들면서、이번에는 용서해 달라」
				PRINTFORMW 「정말로、이러니까 가축을 기르는 건 싫증이 나지 않는 거예요」
			ENDIF
			RETURN LOCAL:1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM8, 기분좋게 해
;────────────────────────────────────
;·REACT분류:받아들인다
;·조교자의 소질, BASE:이성, 얼라이먼트에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·파생 1의 경우는징계·포인트가산
;·REACT인상:0에서멀어짐
;·의존도변화(0, +2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성적인 이야기를 한다/상냥하게 위로한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:애널애무/항문펠라/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:페니스밴드
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:역강간
;
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_8
;=================================================
;·REACT파생 0는 사실상 COM 명칭 그대로 해석 해 줄 수 있으면 좋을까
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 이 철면피의 암퇘지놈!
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL:1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SOURCE:MASTER:쾌Ｃ + SOURCE:MASTER:쾌Ｖ + SOURCE:MASTER:쾌Ａ + SOURCE:MASTER:쾌Ｂ > 10
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해, 이대로 하고라고 말했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 좀 더기분좋게 해계속 이라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 자주(잘) 해와 졸랐다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「네、물론……기대에 따르겠어요」
		PRINTFORMDW 입가를 소매로 감춘 채、아큐는 얕게 웃었다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;본체측의 처리를 나누어지고 있으므로, 텐프레측도 일단 나누기라고 했다가
;현시점에서는 내용에 그다지 차이가 없기 때문에, 이 개소만이라도 좋은 생각은 듭니다
;다만 이 바리안트, 지문이 단번에 증가하거나 바뀌거나 하므로 뭐라고도 말할 수 없는 곳
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 욕망에 정직하게
			;좀 더 기분 좋은 일을당했다실 말했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해,
			;어차피라면 기분 좋아지고 싶다고 했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는기분좋게 해라고 해 출했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는정욕의 포로되었다야 게, 좀 더 기분 좋은 일 해라고 호소했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「네、물론……기대에 따르겠어요」
		PRINTFORMDW 입가를 소매로 감춘 채、아큐는 얕게 웃었다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;쾌락계 반응
;=====================================================================
;·쾌SOURCE나 특정의 PALAM가 트리거가 되어 발생한다
;·COM에 따라서는 ACT분류가 트리거가 된다
;-------------------------------------------------

;──────────────────────────────────
;COM10, 부끄러워한다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·징계·포인트가산
;·쾌SOURCE가 일정 이상의 경우는 파생 1, 쾌감에부끄러워한다가 된다
;·의존도변화(+1, 0)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:성적인 이야기를 한다/의상 변경을 요구한다
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/애널애무/항문펠라/키스/성기를 서로 비빈다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈/Ｃ애무도구/유두캡
;ACT분류:수치
;	ACT:자위/음부공개/자위과시/수치플레이/야외플레이/방뇨
;ACT분류:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/가슴애무강제
;ACT분류:이상
;	ACT:방치플레이
;
;※SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, PALAM:치정가 트리거가 된다
;-------------------------------------------------
;·TFLAG:REACT파생(0=상황에부끄러워한다/1=쾌감에부끄러워한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_10
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 쾌감에 대해서부끄러워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 좀 더 기분 좋아지고 싶다고 생각하고 있는 자신을 의식해 버려,
			;뺨을 물들여 부끄러워했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감이 주어지는 일에 강한수치를 느껴
			;몸을 움츠러들게 해 부끄러워했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 복받치는 쾌감을 숨기지 못하고, 부끄러워해 얼굴을 돌렸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감으로 소리를 흘려, 깜짝했다야 게숨을 삼키면
		;시선을 피해 부끄러워했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
;OriginalString : 「
			PRINTFORMW 「
;OriginalString : 「
			PRINTFORMW 「
		ELSE
;OriginalString : 「
			PRINTFORMW 「
;OriginalString : 「
			PRINTFORMW 「
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1, 쾌감에부끄러워한다를 썼을 경우는 REACT파생 0, 상황에부끄러워한다취급
;파생 1을 쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;이하에 나타내는 지문은 REACT파생 0의 것
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 모습을 볼 수 있는 것으로 이상한 기분이 되어 버려,
			;더욱 더 뺨을 물들여 부끄러워했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자신을 욕보이는 시선에 견딜 수 없게 되어,
			;몸을 안도록(듯이)해 부끄러워했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 욕보여지고 있는 상황에 견딜 수 있지 못하고, 수치의 너무 얼굴을 돌렸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%가 빠는 것 같은 시선을 느껴
		;무심코 시선을 피해 부끄러워했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「얼굴、돌리시면 싫어요……」
			PRINTFORMW 「제 눈、제대로 봐 주세요……네？」
		ELSE
			PRINTFORMW 「우유 탱크에도 부끄러워 할 정도의 지능은 있나 보네요」
			PRINTFORMW 「그런 것보다 먼저、밸브를 달아야 할 텐데요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM11, 싫어한다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT파생 1는 ACT 방해가 되어 REACT분류:날뛴다취급
;·의존도변화(-1, -1)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:의상 변경을 요구한다
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/애널애무/항문펠라/키스/성기를 서로 비빈다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/음부공개
;ACT분류:봉사
;	ACT:파이즈리/발코키
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;
;※SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, PALAM:치정가 트리거가 된다
;-------------------------------------------------
;·TFLAG:REACT파생(0=싫어한다/1=싫어해 ACT를 방해)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_11
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 싫어해 방해한다(REACT분류:날뛴다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 순간에 싫어해손를 내 버렸지만,
			;%CALLNAME%에 미안한 것 같은 표정을 보였다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의기력를 쥐어짜, 싫어해%CALLNAME%로부터 피했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 싫어해 몸을 비틀어,%CALLNAME%의 방해를했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 싫어해%CALLNAME%를 밀쳤다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「꺗……」
		PRINTFORMDW 저항당한 아큐는、눈을 반쯤 뜬 채 %CALLNAME:MASTER%을 바라보고 있다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 싫어한다가 ACT는 방해하지 않고 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 내심에서는 그렇게 그렇지만도 않다고 생각하면서도,
			;부끄럽기 때문에 싫어해 보였다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의기력를 쥐어짜, 떨리는 소리로 싫다고 말했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 고개를 저어 싫어했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 그런 것 싫다고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「사실은 그런 표정、짓게 하고 싶지 않지만……」
		PRINTFORMW 「……조만간 알아 주실 거라고、믿고 있어요」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM12, 서투르다고 모욕한다
;──────────────────────────────────
;·REACT분류:태연하게 한다
;·REACT파생 1(강한척 한다)는 REACT분류:소극적으로 한다취급
;·징계·포인트가산
;·파생 0, 새겨 내린다(REACT인상:악인상대)
;·파생 1, 강한척 한다(REACT인상:나쁜인상)
;·의존도변화(-2, -3)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/애널애무/항문펠라/키스/성기를 서로 비빈다
;		파이즈리한다/풋잡하기
;ACT분류:성교
;	ACT:정상위/후배위/대면좌위/배면좌위
;
;※TFLAG:ACT분류(1=애무/3=성교)가 트리거
;-------------------------------------------------
;·TFLAG:REACT파생(0=새겨 내린다/1=강한척 한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_12
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 강한척 해(쾌감을 느끼면서)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%는 허덕이는 소리 섞임의 뻔히 보임의 허세로,%CALLNAME%에 하손라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;착실하게 쾌감을 느끼면서도 %CALLNAME:MASTER%는 하손라고 우겼다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 얼굴을 비뚤어지게 하면서도, 이런 것은 느끼지 않는다고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:애태움도
			PRINTFORMW 「어라……의외로 여유가 있으시군요」
			PRINTFORMW 「이렇게 움찔움찔 떨면서도、좀 더 강하게 해 주셨으면 하는 건가요？」
			PRINTFORMW 「그러시다면……좀 더 참으실 수 있는 거지요？」
			KFLAG:151 -= 1
		ELSE
			PRINTFORMW 「그렇게 말씀하시지만、이 곳은 괜찮을까요？」
			PRINTFORMW 「봐요……눈이 흐리멍덩 해지잖아요……」
			PRINTFORMW 「후후、당신이 어떤 표정을 지어도 다 알고 있으니까」
			PRINTFORMW 「그러니까、귀엽게 춤춰 주세요……？」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 새겨 내리는(실제로 하손인 경우) 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%는 힘껏의 허풍으로,%CALLNAME%에 하손라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:TARGET%의 미숙한 테크닉에, %CALLNAME:MASTER%는 하손라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이것이라면 자신이 상손실 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「기분 좋지는……않은 것 같네요」
		PRINTFORMW 「음、막상 실제로 해보는 건 다른군요……」
		PRINTFORMDW 기분 탓인지、아큐는 풀이 죽어 보인다。
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM13, 기분 나쁘게 웃는다
;──────────────────────────────────
;·REACT분류:태연하게 한다
;·징계·포인트가산
;·REACT인상:나쁜인상
;·의존도변화(-2, -3)
;-------------------------------------------------
;·트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ, SOURCE:고통/구속, ACT분류:가학
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/애널애무/항문펠라/키스/성기를 서로 비빈다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위/음부공개
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/매도/삼각목마/관장세트
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_13
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 기분 나쁘게 웃는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;PRINTFORML %CALLNAME:MASTER%는 자신은 아직 제정신이라고 확인하도록(듯이) 웃어 보였다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 들러붙은 것 같은 미소를 반했다가, 눈이 헤엄치고 있다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 일부러 %CALLNAME:TARGET%를 도발하도록(듯이) 웃었다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%가조교에 고심하고 있는 것을 조소했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM14, 쾌감을 참는다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·일정 조건으로 REACT파생 1 「애원(REACT분류:용서를 빌다)」취급
;·파생 1의 경우 REACT인상는 0에가까워짐
;·의존도변화(+1, -1)
;-------------------------------------------------
;※트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무, 가슴애무, 입으로 애무, 애널애무, 항문펠라, 키스, 성기를 서로 비빈다
;		파이즈리한다, 풋잡하기
;ACT분류:도구
;	ACT:로터, 바이브, 애널바이브, 애널비즈, Ｃ애무도구, 유두캡
;ACT분류:성교
;	ACT:정상위, 후배위, 기승위, 대면좌위, 배면좌위, 항문섹스
;ACT분류:수치
;	ACT:자위, 음부공개
;ACT분류:봉사
;	ACT:파이즈리, 스마타
;ACT분류:이상
;	ACT:피스트퍽, 애널피스트, 양쪽피스트
;-------------------------------------------------
;·TFLAG:REACT파생(0=쾌감을 참는다/1=애원 한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_14
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 쾌감으로 제정신을 잃는 일에 무서워하고 있다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고, 그만두어와 간원했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 강한 쾌감을참다하면서, 이제(벌써) 이런 일 그만두어와 간원했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감을참다하면서허가를 바라고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 쾌감을참다취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;사정직전
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
	SELECTCASE TFLAG:ACT
		CASE 18
			PRINTFORMW 「발로는 역시 불만이신가요？」
			PRINTFORMW 「자지쪽은、그다지 그렇게 말하지 않는 것 같은데요～」
			PRINTFORMW 「에ー잇、양 발로 이렇게 주ー욱주욱……」
			PRINTFORMW 「쿡쿡、사정을 허가하면、빨리 사정하는 것도 훈련의 일부분이에요？」
		CASE 13
			PRINTFORMW 「참아봤자、무리에요」
			PRINTFORMW 「당신의 약점은 전부 알고 있으니까……」
			PRINTFORMW 「엉덩이를 헤집어져서、싸버릴 수밖에 없다고요？」
		CASEELSE
			PRINTFORMW 「이제 참을 필요는 없다고 하는데도……」
			PRINTFORMW 「혹시、계속하고 싶으셨던 건가요？」
			PRINTFORMW 「그치만、그렇게 계속 참아서、펑ー……하고 고장나버려도 곤란하니까」
			PRINTFORMW 「지금은 제게 관리 당해서、사정해 주세요」
	ENDSELECT
	RETURN LOCAL:1
	ENDIF
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는욕망를 뿌리쳐, 쾌감을 견디려고했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고참다 하고 있다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 필사적으로 쾌감을참다했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 분명한 쾌감을 느끼면서도, 그것을참다 하려고 하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:애태움도
			IF KFLAG:153 == 0
				IF (KFLAG:150 + KFLAG:151) >= 9
					SELECTCASE TFLAG:ACT
						CASE 18
							PRINTFORMW 「자지를 발로 밟힌 채、이를 악물고 침을 흘리는 모습이라니……」
							PRINTFORMW 「제가 아니었다면、환멸했을 지도 몰라요？」
							PRINTFORMW 「괜찮아요、저는 당신을 사랑하고 있으니까요♪」
							PRINTFORMW 「말끔하게、특농 "정액"만 만들어 주신다면……말예요」
						CASE 13
							PRINTFORMW 「불편한 곳은 없으신가요ー？」
							PRINTFORMW 「……네에、이、조금 부풀어 오른 곳 말이죠♪」
							PRINTFORMW 「이제 조금만 더、조금만 더에요♪」
							PRINTFORMW 「눈을 깜빡깜빡 하면서、조금만 더 참으면、사정해도 좋아요♪」
							PRINTFORMW 「오ー……사……삼……이ー……」
						CASEELSE
							PRINTFORMW 「그런데、당신이 너무 귀여운 얼굴을 해버리니까、제 꿀도 흘러넘치고 있어요」
							PRINTFORMW 「알고 계시나요？　아직 털도 나지 않은、새하얀 보지가……」
							PRINTFORMW 「당신의 자지를 집어 삼키는 것을 상상하며、엣찌하게 실을 늘어뜨리고 있다구요……♪」
							PRINTFORMW 「자、상상해 보세요？　꽈악꽈악 질척질척한、여자애 보지……」
							PRINTFORMW 「어린아이인데도 엄마가 되고 싶어서、속에 뷰웃、뷰웃 하고 싸주기를 바라고 있는……」
							PRINTFORMW 「……더 힘들어지는 것 같다？　어라、죄송해요、저도 참♪」
					ENDSELECT
				ELSEIF (KFLAG:150 + KFLAG:151) >= 7
					SELECTCASE TFLAG:ACT
						CASE 18
							PRINTFORMW 「아아……아큐의 발가락이、당신의 자지에 달라붙어서……」
							PRINTFORMW 「허리에 쓸데없는 힘을 넣으셔서는 안 돼요？」
							PRINTFORMW 「세세한 힘조절이 불가능해지기 때문에……뿌직、해버릴지도……」
						CASE 13
							PRINTFORMW 「어라？　당신、고깃구멍을 자극 당해서……꿀이 똑똑、흘러 나오고 있지 않나요？」
							PRINTFORMW 「안 된다구요？　저는 아직、허가해 주지 않았어요？」
							PRINTFORMW 「자、무릎을 똑바로 서지 않으시면 손가락을 엉덩이 안에서 마구 휘저러버릴 거예요」
							PRINTFORMW 「힘내세요ー、인내、인내……」
						CASEELSE
							PRINTFORMW 「……머릿속이 새하얗게 되고 계시죠？」
							PRINTFORMW 「가볍ー게 몸을 더듬는 것만으로……움찔움찔 하며 반응하시네요」
							PRINTFORMW 「뭔가 다른 일을 생각하는 게、시름을 덜지도 모르겠어요」
					ENDSELECT
				ELSEIF (KFLAG:150 + KFLAG:151) >= 3
					PRINTFORMW 「그래요、숨을 들이쉬고……내쉬고……」
					PRINTFORMW 「처ー언천히 호흡하면서、발에 힘을 빼 주세요」
					PRINTFORMW 「여분의 힘을 빼고……자지에만 의식을 집중하세요？」
					PRINTFORMW 「향을 기ー잎게 들이마시면、머리가 멍해지기 시작하지요……？」
				ELSE
					PRINTFORMW 「네、그래요。참는 거에요……꼬ー옥、참아 주세요」
					PRINTFORMW 「어금니를 꽉 깨물고……불끈、불끈、하고 치밀어오르는 정액을、밀어 넣는 거예요」
					PRINTFORMW 「시키는 대로 하게 되면、상을 드릴 테니까……알겠죠？」
				ENDIF
			ELSE
				IF (KFLAG:150 + KFLAG:151) >= 9
					SELECTCASE TFLAG:ACT
						CASE 18
							PRINTFORMW 「헤에、꽤 열심히 하시네요。틀림없이、발로 남자의 존엄을 짓밟히면서 좋아하는 변태라고 생각했는데요」
							PRINTFORMW 「힘껏 음낭을 발로 눌려서、비참하게 싸버릴 거라고 생각했는데……」
							PRINTFORMW 「에잇！　이렇게！　불알을！　찌부러뜨려서！」
							PRINTFORMW 「……흐응、견디시는 건가요？　입에서 거품이 나고 있는데요」
						CASE 13
							PRINTFORMW 「이보세요、울음소리는 어떻게 된 건가요？　제대로 음매ー음매ー 울어 주세요」
							PRINTFORMW 「……더 큰 목소리로。그러지 않으면、손가락 모두 찔러 넣어버릴 거예요？」
							PRINTFORMW 「그래요。이를 악물고 견디는 것 따위、정말 쉽다니까요」
							PRINTFORMW 「그치만、평생 잊을 수 없는 아픔이나 쾌감이라든지……저와 같네요……쿠훗」
						CASEELSE
							PRINTFORMW 「정말、무슨 짓을 당하고 있는지도 잘 모르고 있는 얼굴이네요」
							PRINTFORMW 「인간으로서의 존엄 따위 있지도 않지만……뭐、하반신으로 생각하는 생물이란 거 어차피 인간도 아니고 말이죠」
							PRINTFORMW 「차ー암 잘했어요、고기 바이브 씨。인간 님께서 상을 내려 드릴게요」
					ENDSELECT
				ELSEIF (KFLAG:150 + KFLAG:151) >= 6
					PRINTFORMW 「……상당히 잘 견디시는군요。아무래도 반성하고 있는 건 분명한 것 같네요」
					PRINTFORMW 「뭐어、가축은 채찍만으로 기르는 건 아니니까、바라는 바는 아니지만 먹이를 드릴게요
					PRINTFORMW 「힘내라ー、자지、힘내라ー、자지……」
				ELSEIF (KFLAG:150 + KFLAG:151) >= 3
					PRINTFORMW 「이거 이거、이 정도 단계에서、이렇게나 쿠퍼액을 흩뿌리다니 괜찮으신가요？」
					PRINTFORMW 「이렇게나 질척질척해버리면、쓸데없이 기분 좋아져 버린다고요？」
					PRINTFORMW 「알겠으면 수도꼭지를 꼭 잠가서 쿠퍼액 누출을 막아 주세요」
					PRINTFORMW 「그러지 않으면、요도까지 "훈련"해야 할 거예요？」
				ELSE
					PRINTFORMW 「불량 자지라도、확실히 학습기능 정도는 있나 보네요」
					PRINTFORMW 「네、그걸로 좋아요。그다지 저를 성가시게 하지 말아주세요」
					PRINTFORMW 「도무지 쓸 수 없다고 생각이 들면、곧 부러뜨려버릴 테니까요？」
				ENDIF
			ENDIF
			KFLAG:151 += 1
		ELSE
			IF KFLAG:153 == 0
				IF RAND:3 == 0
					PRINTFORMW 「마음에 들지 않으시나요？　당신」
					PRINTFORMW 「그게 아니면、여자 아이에게 괴롭힘당하는 건……자존심이 허락하지 않나요？」
				ELSEIF RAND:2 == 0
					PRINTFORMW 「그렇게 뺨을 붉히시는 모습、귀여워요」
					PRINTFORMW 「그림으로 남기고 싶을 정도로……후훗♪」
				ELSE
					PRINTFORMW 「에에、알고 있다마다요……남아대장부、프라이드가 있는 법이니」
					PRINTFORMW 「그걸 꽃 꺾듯이 꺾는 것도、소녀가 할 일이니까요……」
				ENDIF
			ELSE
				PRINTFORMW 「돼지는 돼지나름 생각하는 게 있나 보네요」
				PRINTFORMW 「뭐、솔직히 쓸데없는 저항도 마음에 들지만요。싫어하지는 않는다고요？」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM15, 쾌감을 받아들인다
;──────────────────────────────────
;·COM16, 기분 좋아! (와)과 거의 같음. 차이는의존도의변화만
;·조교대상가 의존하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도변화(+1, +5)
;-------------------------------------------------
;※트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/애널애무/항문펠라/키스/성기를 서로 비빈다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위, 음부공개
;ACT분류:봉사
;	ACT:파이즈리, 스마타
;ACT분류:이상
;	ACT:피스트퍽, 애널피스트, 양쪽피스트
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_15
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을 받아들인다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;쾌감에방심 해 버리면, %CALLNAME:MASTER%의 심은 순식간에 녹기 시작했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 달콤한 교성을 주면서 쾌감을 받아들였다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 자신이 기분 좋아지는 것인 만큼 필사적으로 쾌감을 탐내고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:애태움도
			IF KFLAG:153 == 0
				PRINTFORMW 「자자、늠름한 얼굴이 눈물로 흠뻑 젖어버렸다고요？」
				PRINTFORMW 「아무것도 생각할 수 없다는 듯한 표정이네요……제대로 참고 계시나요？」
			ELSE
				PRINTFORMW 「허리를 흔들어도 좋다고 한 적은 없는데요？」
				PRINTFORMW 「이 정도의 자극으로 히익 거리며 기뻐하다니……」
				PRINTFORMW 「정말이지、얕디 얕다는 건 이런 걸 두고 하는 말이네요」
				PRINTFORMW 「더 강하게、때리는 쪽이 더 나을까요？」
			ENDIF
			KFLAG:151 -= 1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMW 「충분히 맛보고 계시나요？」
				PRINTFORMW 「저는 이제、당신의 것이니까요……」
				PRINTFORMW 「사양하지 마시고、무엇이든 명령해 주세요？」
				PRINTFORMW 「이 유녀（어린 여아）의 몸으로 가능한 일이라면、무엇이든……」
			ELSE
				PRINTFORMW 「칠칠치 못하게 혀를 빼물고……마치 개 같군요」
				PRINTFORMW 「부정하고 싶으신가요？　그렇다면、이 빳빳하게 선 물건이라도 좀 가라앉혀 보시겠어요？」
				PRINTFORMW 「제게 관리당하는 게 행복이 될 때까지、조교해 드릴게요……」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM16, 기분 좋아!
;──────────────────────────────────
;·COM15, 쾌감을 받아들인다와와 거의 같음. 차이는의존도의변화만
;·주인님의 의존을 권하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도변화(+4, +1)
;-------------------------------------------------
;※트리거가 되는행동, 소스, 파라미터
;·SOURCE:쾌Ｃ/쾌Ｖ/쾌Ａ/쾌Ｂ
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/애널애무/항문펠라/키스/성기를 서로 비빈다
;		파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:수치
;	ACT:자위, 음부공개
;ACT분류:봉사
;	ACT:파이즈리, 스마타
;ACT분류:이상
;	ACT:피스트퍽, 애널피스트, 양쪽피스트
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_16
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분 좋아!
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%는 아첨하도록(듯이) 기분 좋은 것 같은 소리로 허덕이고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는쾌락를 얻을 수 있는 행복을 감사하는것 같이 소리에 출했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 달콤한 소리로 쾌감과 기쁨을%CALLNAME%에게 전했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;성교계 반응 COM20 접수대
;=====================================================================
;·ACT분류:성교/성봉, ACT:파이즈리한다/성기를 서로 비빈다등이 트리거가 되어 발생
;-------------------------------------------------

;──────────────────────────────────
;COM20, 허리를 흔든다
;──────────────────────────────────
;·주도권(SEX_INITIATIVE)에 의해 내용이 다른 COM
;-------------------------------------------------
;·ACT분류가성교봉사, ACT:기승위의 경우는 주도권은조교대상(SEX_INITIATIVE(MASTER))
;·REACT분류:적극적으로 한다
;·의존도변화(+5, +3)
;-------------------------------------------------
;·ACT분류가성교, ACT:역강간/성기를 서로 비빈다/파이즈리한다의 경우는 주도권은조교자
;·※성기를 서로 비빈다/파이즈리한다는 SEX_INITIATIVE(TARGET)에서는 참조 할 수 없다
;·REACT분류:받아들인다
;·의존도변화(+4, +8)
;-------------------------------------------------
;·REACT인상:좋은인상
;-------------------------------------------------
;·애태움중은 실행 불가
;·ACT:파이즈리한다의 경우, 조교자가빈유라고 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:성기를 서로 비빈다/파이즈리한다
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:성봉
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다/역강간
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_20
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;주도권은조교대상(ACT분류가성교봉사/ACT:기승위의 경우)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SEX_INITIATIVE(MASTER)
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME%의 환심을 얻기 위해, %CALLNAME:MASTER%는 힘껏 허리를 사용했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 하려고 허리를 사용하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는허리기술로 상손를 느껴시킨다일로 기쁨을 찾아내고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;SEX_INITIATIVE(MASTER)를 쓰지 않았던 경우는 여기서 보완됩니다만 범위가 넓기 때문에
;최저한, TEQUIP:성교중/성교봉사중, IS_COMGRONAME/IS_NOWACTNAME든지로 나누어 쓰지 않으면 정합성은 무너지는 일이 될까하고
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우(ACT분류가성교/ACT:역강간/성기를 서로 비빈다/파이즈리한다의 경우)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%가 허리를 아무리 털어도, 자신의 쾌감이 강해질 뿐이었다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME%의 신체에 빠지도록(듯이), %CALLNAME:MASTER%의 허리는 승손에 움직여 쾌감을 탐낸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 자신이 기분이 좋게 여차저차허리를 흔들고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「그런 식으로、문지르시면……응읏♪」
			PRINTFORMW 「별로 운동을 안 하니까……살이 꽤 붙어있는 게、부끄러워요……」
		ELSE
			PRINTFORMW 「무얼 굽실굽실 허리를 흔들고 있어요？」
			PRINTFORMW 「포로로서는 더할나위 없지만、제대로 참지 못하면 또 심한 짓을 당할 거에요？」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM21, 안에 싸!
;──────────────────────────────────
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·조교대상이남자의 경우는 튕겨진다
;·의존도변화(+10, +10)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_21
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에 싸!
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해져쾌락로 마음을 어지럽혀지면서
		;자포자기되었다야 게, 이제(벌써) 안으로 좋은, 안에 내고라고 말했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;정상위, 대면좌위의 경우는 지문이변화
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;정상위/대면좌위의 경우	%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 허리를 양발로 단단히안아, 안에 내와 속삭였다…
		;그 외의 경우			%CALLNAME:MASTER%는 관철해지는 쾌감에 빠져, 안에 싸! 라고 외쳤다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해지는 쾌감과정욕에 지배되어
		;황홀한 기분의 표정으로 어떻게 되어도 괜찮은, 안에 갖고 싶다고 외쳤다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM22, 안에 싸면 안돼!
;──────────────────────────────────
;·REACT분류:용서를 빌다
;·REACT파생 1(안에 낸다)의 경우는징계·포인트추가. REACT분류:없음
;·REACT인상:나쁜인상
;·의존도변화(0, -10)
;·참다플래그는 소멸한다
;·조교대상이남자의 경우는 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;-------------------------------------------------
;·TFLAG:REACT파생(0=소원을 (들)물은/1=안에 낸다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_22
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에 싸면 안돼!
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;0203판 현재, 파생해도 지문은 공통
	;=================================================
	;REACT파생 1, 안에 낸다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 수치도 세상소문도 없고%CALLNAME%를 요구하고 싶은 감정을 열심히 뿌리쳐,
			;중만은 안돼, 밖에 내라고 외쳤다…
;OriginalString : 
			PRINTFORMW 
		ELSE
			;%CALLNAME:MASTER%는%CALLNAME%의분노를 사는 것을 무서워하면서도,
			;교배되는 것을 무서워해, 안은 그만두어, 내서는 안돼와 비통하게 외쳤다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 관철해지는 쾌감을 참으면서, 안에 싸면 안돼! 라고 외쳤다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 힘 없고%CALLNAME%에 범해져 축하면서도
		;중만은 그만두어, 허락해와애원했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;고통계 반응
;=====================================================================
;·가학계행동나삽입를 수반하는 각종행동로 볼 수 있다
;·0203판 현재, 고통계에의존도변화의 처리 없음
;-------------------------------------------------

;──────────────────────────────────
;COM30, 고통을 참는다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT인상:약간좋은인상
;·SOURCE:고통가 없는 경우는 튕겨진다
;-------------------------------------------------
;·트리거가 되는행동, 소스, 파라미터
;·SOURCE:고통/구속
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/애널애무/항문펠라/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이마라치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_30
;=================================================
;·일정 이상의 쾌SOURCE가 있는 경우 지문이변화 한다
;※REACT파생에 대입되어 있지 않기 때문에, TFLAG:REACT파생에서는 참조 할 수 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SOURCE:MASTER:쾌Ｃ + SOURCE:MASTER:쾌Ｖ + SOURCE:MASTER:쾌Ａ + SOURCE:MASTER:쾌Ｂ > 10
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 감정을 삼켜, 쾌감과고통를 동시에 참았다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는고통안에 쾌감을 의식해, 무서워하면서 양쪽 모두를참다했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;쾌감과고통가 동시에 덮쳐, %CALLNAME:MASTER%는 흐려진 소리내질렀다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는고통를 감추기 (위해)때문에, 동시에 복받치는 쾌감에 달라붙었다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「왠지……」
		PRINTFORMW 「목소리에 『색』이 섞이기 시작하지 않으셨나요……？」
		PRINTFORMDW 필사적으로 머리를 흔드는 %CALLNAME:MASTER%(을)를 、아큐는 눈을 치켜 뜬 채 올려다 보았다。
		PRINTFORMW 「이거 이거……후후、기대되는걸요？」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는고통를 견디면서,%CALLNAME%의 행위를 받아들였다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 고민의신음를 루등하면서고통를참다했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는고통를참다했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이빨을 이를 악물어고통를 참았다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「아프시지요？　슬프시지요……」
		PRINTFORMW 「그렇지만、아플 만한 짓을 해서 이러는 거예요……？」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM31, 비명지른다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT인상:0에서멀어짐
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:엄하게 위협한다/징계한다고 위협한다/고함치다/기분 나쁘게 웃는다
;ACT분류:애무
;	ACT:가슴애무/입으로 애무/애널애무/항문펠라/키스/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이마라치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양쪽피스트/공중육변기플레이
;-------------------------------------------------
;·TFLAG:REACT파생(0=고통에 의한 비명/1=공포에 의한 비명)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_31
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 공포에 의한 비명
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 아첨하는 것 같은 비명을 올렸다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 무서워한 것처럼 비명을 올려 보였다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는공포를 느껴 비명을 올렸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이상한공포를 느껴 비명을 올렸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1를 썼을 경우는 REACT파생 0, 고통에 의한 비명 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%가 좋아하는고통의 비명을 자연히(에) 소리에 출했다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 비명을 올려고통에 몸부림쳐 보였다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는고통를 견딜 수 있지 못하고 비명을 올렸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는고통의 너무 비통한 외침을 올렸다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「아아、어떻게 하죠……」
		PRINTFORMW 「그 목소리를 듣고 있으면、어째선지 오싹오싹해지는걸요……」
		PRINTFORMDW %CALLNAME:MASTER%의 비명을 듣고선、아큐는 황홀한 웃음을 띄웠다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM32, 날뛴다
;────────────────────────────────────
;·파생에 의해 날뛰고 성공(REACT분류:날뛴다), 날뛰고 실패(REACT분류:소극적으로 한다)로 나누어진다
;·날뛰고가 통과하면도구계/성교계/성교봉사계 커맨드를 방해했다취급이 된다
;·징계·포인트추가
;·REACT인상:악인상대
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로 애무/가슴애무/입으로 애무/애널애무/항문펠라/키스
;		성기를 서로 비빈다/파이즈리한다/풋잡하기
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈/Ｃ애무도구/유두캡
;ACT분류:성교
;	ACT:정상위/후배위/기승위/대면좌위/배면좌위/항문섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스펭킹/채찍/바늘/밧줄/눈가리개/볼개그/관장세트
;ACT분류:휴식
;	ACT:치료한다
;ACT분류:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다
;-------------------------------------------------
;·TFLAG:REACT파생(0=날뛰고 성공/1=밧줄구속에 의한 날뛰고 실패/2=제지에 의한 날뛰고 실패)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_32
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT파생 2, 제지에 의한 날뛰고 실패(REACT분류:소극적으로 한다)
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 2
		;%CALLNAME:MASTER%는 날뛰어 저항하려고했다가, %CALLNAME:TARGET%에 억제 당했다…
		PRINTFORMW 「자、잠깐……너무 저항하지 말아 주세요！」
		PRINTFORMW 「난폭한 행동은 좋지 않아요……알겠죠？」
		PRINTFORMDW 급소를 꽈악 움켜쥐고서 빙긋 웃는 아큐에게、%CALLNAME:MASTER%(은)는 고개를 끄덕이는 수밖에 없었다……
		KFLAG:151 -= 1
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 1, 밧줄구속에 의한 날뛰고 실패(REACT분류:소극적으로 한다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		;속박된 %CALLNAME:MASTER%는 날뛰어 저항하려고했다가, 밧줄가 한층 더 먹혀들어 다만 아픈 것뿐이다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1으로 2를 썼을 경우는 파생 0, 날뛰고 성공(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
	;※파생 마다의 내용의 차이가 비교적 크기 때문에, 나누어 쓰는 것이 무난하다라고 생각합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		;%CALLNAME:MASTER%는 날뛰어 %CALLNAME:TARGET%의 행동를 방해했다. %CALLNAME:TARGET%는 꽤 화나 있는 것 같다…
;OriginalString : 
		PRINTFORMDL
		PRINTFORMW 「꺗……」
		PRINTFORMDL
		PRINTFORMDW 연약한 몸을 튕켜올린 아큐가、다다미 위를 굴렀다。
		PRINTFORMDW 긁힌 것일까、그 손바닥이 빨갛게 물들어 있다……
		PRINTFORMDW 잠시 멍해져 있던 아큐였으나、손바닥의 피를 핥고서、%CALLNAME:MASTER%의 고간을 용서 없이 걷어찼다。
		PRINTFORMDW 아파서 데굴데굴 구르며 괴로워하는 %CALLNAME:MASTER%을 、아큐는 차가운 눈으로 내려다보았다……
		PRINTFORMDL
		PRINTFORMW 「아무리 종마라도、자꾸 날뛰면 거세해버릴 수밖에 없어요？　알고 있나요？」
		PRINTFORMW 「……좋아요。일단、부부답게 하기 전에……한번 상하관계를 확실하게 하는 쪽이、서로에게 좋겠죠？」
		KFLAG:153 = 1
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM33, 무서워한다
;──────────────────────────────────
;·REACT분류:용서를 빌다
;·일정 조건으로분노를 사, 징계·포인트추가
;-------------------------------------------------
;·트리거이벤트제
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성에 관한 이야기를 함
;		엄하게 위협한다/징계한다고 위협한다/고함치다/기분 나쁘게 웃는다
;ACT분류:1:애무
;	ACT:풋잡하기/밀어 넘어트린다
;ACT분류:2:도구
;	ACT:밧줄/눈가리개/볼개그/관장세트/애널바이브/애널비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/공중육변기플레이/페니스밴드장착
;ACT분류:9:휴식
;	ACT:휴식시킨다/영양제를 먹인다/치료한다
;ACT분류:10:성교봉사
;	ACT:
;-------------------------------------------------
;·TFLAG:REACT파생(0=용서를 빌다/1=의견을 내다니 좋은 번가슴군요!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_33
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 공중육변기플레이에서는 파생하지 않는다
	;=================================================
	;REACT파생 1, 징계·포인트추가
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%보다 오히려 이상하게 될 것 같은 자신에게 무서워하고 있는 것 같다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 생각을 삼켜, 얌전하게 공포의 시선을 향했다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는공포를 느끼고 무서워하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 작은 동물과 같이 무서워하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「무서우、신가요……？」
		PRINTFORMW 「괜찮아요、힘을 빼세요……제가 반드시、행복하게 해 드릴 테니까……」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;봉사계 반응 COM40 접수대
;=====================================================================
;·조교자에의애무를 포함한행동로 볼 수 있다
;·ACT분류:봉사+트리거이벤트제
;-------------------------------------------------

;────────────────────────────────────
;COM40, 거부한다
;────────────────────────────────────
;·REACT분류:거부
;·REACT인상:악인상대
;·징계·포인트추가
;·의존도변화(-2, -4)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다/합의를 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:영양제를 먹인다
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/항문섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_40
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 거부한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 미혹을 보이면서도 요구에는 응하지 않았다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 압도되면서도 요구는 삼키지 않았다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 요구를거부했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 강한 의지로 요구를 퇴짜놓았다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「쌀쌀맞으시군요」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM41, 소극적으로 한다
;──────────────────────────────────
;·REACT분류:소극적으로 한다
;·REACT파생 1는 "소극적인의가 마음에 들지 않는다"되어징계·포인트추가
;·REACT인상:약간좋은인상
;-------------------------------------------------
;·TFLAG:REACT파생 == 0, 소극적이지만 좋은가
;·의존도변화(-3, +2)
;-------------------------------------------------
;·TFLAG:REACT파생 == 1, 소극적인의가 마음에 들지 않는다
;·의존도변화(-5, 0)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_41
;=================================================
;·파생에 관련되지 않고 지문은 공통
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적으로 한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;본체 측에 전용의 처리 없음
	;=================================================
	;파생 1, 소극적인의가 마음에 들지 않는다（징계·포인트가산）
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신으로부터행동 하는 용기가 없는 것 같다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 듯이 하면서도 거역하려고는 하지 않는다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 어떻게든%CALLNAME%가 말하는 대로 하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 역네 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「낯간지러워요。조금만 더 격렬하게……」
			PRINTFORMW 「제 취향도、확실하게 익혀 주세요……」
		ELSE
			PRINTFORMW 「정말이지、이러니까 버터견이 더 쓸모가 있다는 거예요」
			PRINTFORMW 「알겠어요？　동물 이하라고요、동물 이하」
			PRINTFORMW 「그렇지 않아도 성기능이 떨어지는데、거기에다 봉사까지 못한다면……식재료로밖에 쓸 수 없어요？」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM42, 적극적으로 한다
;──────────────────────────────────
;·REACT분류:적극적으로 한다
;·REACT인상:좋은인상
;·참다플래그해소
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_42
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적으로 한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자기보다%CALLNAME%를우선 하려고 하고 있다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 의식해 기특하게 거절하는마이구토 하고 있는 것 같다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 진행되어%CALLNAME%에 힘쓰고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 어쨌든%CALLNAME%를 기쁘게 하고 싶은 것 같다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「응、앗……♪　만져 주시는 곳에서、사랑이 느껴져요……♪」
			PRINTFORMW 「거깃、거기이！　더 상냥하게 해 주세요오……」
			PRINTFORMDW 허리를 움찔움찔 흔들며、아큐는 %CALLNAME:MASTER%의 봉사에 견디고 있다……
		ELSE
			PRINTFORMW 「응……돼지 치고는、훌륭하군요……♪」
			PRINTFORMW 「엉덩이 구멍까지、주ー욱 핥게 한 보람이 있네요」
			PRINTFORMW 「이제 제 알아서 엉덩이에 얼굴을 파묻을 정도까지 됐으니까요……♪」
		ENDIF
		KFLAG:151 += 1
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM43, 애정을 담아 한다
;──────────────────────────────────
;·일정 이상의호감도, 조교레벨가필요
;·REACT분류:적극적으로 한다
;·REACT인상:좋은인상
;·참다플래그해소
;·반발각인가 있으면 튕겨진다
;·의존도변화(+6, +10)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_43
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을 담아 한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%는 평정을 가장하고 있지만, 기쁜 듯한 표정을 숨길 수 없다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는 넋을 잃고했다 얼굴로 정중하게 명령을 진한했다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%의 태도는 사랑스러운 사람에 힘쓸 때의 그것 그 자체였다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「아훗、당신……♪」
		PRINTFORMW 「저도、사랑하고 있어요……♪」
		PRINTFORMW 「자아、오늘 밤도 확실하게 씨를 부어넣을 수 있도록、기운차게 보내도록 해요？」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;────────────────────────────────────
;COM44, 태연하게 한다
;────────────────────────────────────
;·REACT분류:태연하게 한다
;·REACT인상:약간좋은인상
;·의존도변화(+2, -2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:의상 변경을 요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:기승위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/파이즈리/스마타/발핥기강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴식
;	ACT:
;ACT분류:10:성교봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/항문섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_44
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게 한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 평상심을 유지하려 하고 있다…
;OriginalString : 
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 무리를하면서도 평정을 가장하고 있다…
;OriginalString : 
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 해 환심을 권하려고 하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%에는%CALLNAME%의 모습을 살필 뿐(만큼)의 여유가 있는 것 같다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;그 외의 반응 COM50 접수대
;=====================================================================
;·특정의 장면에서 발생하는 이상한행동
;-------------------------------------------------

;────────────────────────────────────
;COM50, 자위를 시작한다
;────────────────────────────────────
;·REACT분류:받아들인다
;·파생 1, 승손에 자위 한데의 경우는징계·포인트추가
;·REACT인상:나쁜인상
;·참다플래그해소
;-------------------------------------------------
;·트리거이벤트제
;-------------------------------------------------
;·밧줄로 속박되고 있으면 안돼
;·벌써자위 하고 있다, 성교중, 성교봉사중는 튕겨진다
;-------------------------------------------------
;TFLAG:REACT파생 == 0, 승손에 자위 하고 있지만 좋은가, 의존도변화(-1, 0)
;TFLAG:REACT파생 == 1, 승손에 자위 하지마, 의존도변화(-3, +2)
;-------------------------------------------------
;트리거하는행동나 상황…
;ACT분류:0:회화
;	ACT:지금 기분을 물어본다/자신을 어떻게 생각하는지 물어본다/성에 관한 이야기를 함
;ACT분류:1:애무
;	ACT:키스
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시/수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:손으로 애무를 강요/입으로 애무를 강요/발핥기강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이
;ACT분류:9:휴식
;	ACT:휴식시킨다
;ACT분류:10:성교봉사
;	ACT:
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_50
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위를 시작한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;본체 측에 전용의 처리 없음
	;=================================================
	;REACT파생 1, 승손에 자위 하지마
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 「……무슨 짓을 하고 계시는 거죠？」
		PRINTFORMDL
		PRINTFORMDW 아큐가 극히 차가워진 눈동자로 %CALLNAME:MASTER%을 쏘아보았다。
		PRINTFORMDW 그 눈동자에는 감출 수 없는 실망까지도 배어나오고 있다……
		PRINTFORMDL
		PRINTFORMW 「참아라、고 말하는데도 오나니를 하다니」
		PRINTFORMW 「하아……알았어요。돼지처럼 몸에 직접 가르쳐 주는 게 좋다 이거군요」
		KFLAG:153 = 1
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리한 행동"
		;%CALLNAME:MASTER%는자위를 하는 것으로 쾌감에 빠져 버리려고 하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리없는 행동"
		;%CALLNAME:MASTER%는참다 하지 못하고 승손에 자신을 위로하고 있다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%의 손는 무의식 중에 자신의성기를 만지작거리고 있는 것 같다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를방치했다경우는 이 개소가 보완합니다
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「자위해라、고 한 기억은 없는데요……」
		PRINTFORMW 「뭐、사정에 달하지만 않으면 상관 없지만 말예요」
		PRINTFORMW 「그래도 그거、당신이 괴로워질 뿐일 텐데요？」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;──────────────────────────────────
;COM51, 도구를 제거한다
;──────────────────────────────────
;·REACT분류:날뛴다
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/유두캡/눈가리개/볼개그/페니스밴드)를장착 하고 있는 것이 조건
;·조교대상가자위중인 경우는 중단이 된다
;·REACT인상:악인상대
;·의존도변화(-3, -10)
;-------------------------------------------------
;·제외하는도구는 한 번에 1개까지
;※우선도는 이하와 같다
;(눈가리개>관장기＋플러그>애널바이브>바이브> 오나홀>클리캡>볼개그>유두캡> 애널비즈)
;-------------------------------------------------
;선택 가능한 조건…
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/유두캡/눈가리개/볼개그/페니스밴드)를장착 하고 있는 것이 조건
;·TEQUIP:밧줄/성교중는 실행 불가
;-------------------------------------------------
;·밖했다도구의 TEQUIP번호가 TFLAG:REACT파생에 들어간다
;·0203판 현재, 이 COM가선택지에 나오지 않는 불편 있어?
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_51
;=================================================
;·지문은 범용 처리가 있을 뿐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM51, 도구를 제거한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;REACT파생분기를 추가하는 경우는 이 개소의 상층에서
	;=================================================
	;기타·조건없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;%CALLNAME:MASTER%는장착당했다도구를 승손에 제외해, %CALLNAME:TARGET%는 매우 화나 있는 것 같다…
;OriginalString : 
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;COM52, 이루는대로
;────────────────────────────────────
;·효과 없음. 실행할 수 있는 반응이 한 개도 없을 때의 구제
;·조교자에 좋을대로 희롱해지고 한편, 하라는 대로 되는 목표 상황
;·REACT파생 마다 REACT분류, 의존도변화가 바뀐다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_52
;=================================================

;------------------------------------------------
;REACT분류:받아들인다/의존도변화(+4, +10)
;성교계 ACT·역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「무저항에 범해진다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;PRINTFORML %CALLNAME:MASTER%는 힘이 들어가지 않는 신체를 무저항에 범해져 이송되는쾌락에 허덕이고 있다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;REACT분류:받아들인다/의존도변화(+10, +10)
;성교계봉사계 ACT·기승위
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 「빠진 것처럼허리를 흔든다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 2
	;%CALLNAME:MASTER%는%CALLNAME%의 신체에 빠진 것처럼, 숨을 어지럽혀 오로지 허리를 흔들고 있다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;ACT(의상변경/자위/방뇨/공개/이라마치오)
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 3, 「말해지는 대로 천천히 따른다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 3
	;%CALLNAME:MASTER%는 다만%CALLNAME%에 말해지는 대로 따랐다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;봉사계 ACT
;REACT분류:받아들인다/의존도변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 4, 「매료되어진 것처럼봉사를 반복한다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 4
	;마치 매료되어진 것처럼, %CALLNAME:MASTER%는 무심하게%CALLNAME%에봉사했다…
	;심 여기에 있지 않고라고 하는 모습으로, 그손 다하고는 이상한 것이다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;ACT(위협/징계·과 선언/고함치다/기분 나쁘게 웃는다/매도)
;반항적인 소질이 있어, 굴복각인 5 이하의 경우
;REACT분류:소극적으로 한다/의존도변화(0, -2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 5, 「분한 듯이 노려본다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 5
	;%CALLNAME:MASTER%는 분한 듯이 입술을 깨물어,%CALLNAME%를 올려보았다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;ACT(위협/징계·과 선언/고함치다/기분 나쁘게 웃는다/매도)
;REACT분류:소극적으로 한다/의존도변화(+2, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 6, 「무서워한 것처럼 응시한다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 6
	;%CALLNAME:MASTER%는 무서워한 것처럼%CALLNAME%를 응시했다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;가학계 ACT·고통가 큰 경우
;REACT분류:소극적으로 한다/의존도변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 7, 「소리가 되지 않는 비명을 올려 몸부림쳐 뒹군다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 7
	;%CALLNAME:MASTER%는고통의 그다지 소리가 되지 않는 비명을 올려 몸부림쳐 뒹굴었다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 또한자위중인 경우
;REACT분류:받아들인다/REACT인상:나쁜인상/의존도변화(-2, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 8, 「오나니─에 열중해 듣지 않았다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 8
	;%CALLNAME:MASTER%는자위에 몰두해 온전히 대답을 할 수 없었다…
	;%CALLNAME%는 불만인 것 같다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT
;REACT분류:받아들인다/의존도변화(+4, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 9, 「기분 좋아서 선대답」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 9
	;%CALLNAME:MASTER%는 쾌감에 몸부림하면서 수긍할 수 밖에 없었다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 이외의 경우
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 10, 「머리 진흰색 황홀상태」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 10
	;%CALLNAME:MASTER%는 주어지는 쾌감에 희롱해져 황홀이 되고 있다…
	;녹초가된 머릿속은 새하얗게 물들어, 아무것도 생각하는 것이 할 수 없다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;회화계 ACT
;REACT분류:받아들인다/의존도변화(+8, +4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 11, 「스스로도 모르는 채 부끄러운 일을입 달린다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 11
	;%CALLNAME:MASTER%는 어딘가 망연히했다대답했다…
	;스스로도 잘 모르는 채로 부끄러운 일을입 달리고 있는 생각이 든다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;대만족보너스로쉬게를선택했다경우
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한일회휴게의 경우
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:일회휴게 && IS_POLICY("금일", "휴식")
	;%CALLNAME:MASTER%는 안심했다야 게체가 힘을 뺐다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;조교자, 혹은조교대상의상태에 의해일회휴게되었다경우
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;일회휴게의 경우
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:일회휴게
	;%CALLNAME:MASTER%는 안심했다야 게체가 힘을 뺐다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;REACT분류:받아들인다/의존도변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;%CALLNAME:MASTER%는 신체를 내던져, 되는 대로 되어 있다…
;OriginalString : 
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;=====================================================================
;특수한 반응 COM70～
;=====================================================================

;──────────────────────────────────
;COM70, 가게해줘!
;──────────────────────────────────
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도변화(+10, +6)
;-------------------------------------------------
;선택 가능한 조건…
;·애태움중은 아니다
;·TCVAR:MASTER:가버려가 서 있다
;·애무계 ACT(키스/밀어넘어짐 이외) 또한 각 부위의 PALAM가 5000이상이다
;※상기 조건에 들어맞는 경우는 가능
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해줘 준다/2=가게해줘 주지 않는다）
;-------------------------------------------------
;ACT에 있어서 TCVAR:MASTER:애태움도 == 1(가게 한다)에서 만나도 COM70의선택로 TCVAR:MASTER:애태움도 > 1(초조하게 한다)(이)가 되는 경우 있어
;현재의 사양에서는 이손의변화를 참조할 방법은 없다. 본체측의 대응이 바람직한다!
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_70
;=================================================
;우선 최소한의 분기를 놓아둡니다만
;PENIS(MASTER) 등으로페니스의 유무를 참조했다 (분)편이 무난할지도 모릅니다
;-------------------------------------------------

;%CALLNAME:TARGET%는 심술궂은 미소를 띄우며 %CALLNAME:MASTER%를 애태우기 시작했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 가게해줘 주지 않는다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 2
	;=================================================
	;ACT분류, 애무
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 「움찔、움찔 하고 몸이 떨리네요……」
		PRINTFORMW 「침을 그렇게 흘리면서、여자애를 향해 애원할 정도로 절정하고 싶으신 건가요？」
		PRINTFORMDW 쿡쿡 웃으며、아큐는 애무를 이어갔다。
		PRINTFORMDW 배꼽이나 회음부까지 정중하게 희롱당하며、%CALLNAME:MASTER%은 비명을 질렀다……
		PRINTFORMW 「자、좀 더 잔뜩 부탁해 주세요」
		PRINTFORMW 「조그마한 아이에게 괴롭힘 당해서、필사적으로 애원하는 거라고요？」
		KFLAG:151 += 1
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성봉") || TEQUIP:성교봉사중
		IF V_SEX(MASTER)
			PRINTFORMW 「아직……아직 안 돼요！」
			PRINTFORMW 「한계까지、모으고、모아서……」
			PRINTFORMW 「한방에、절 임신시켜 주시지 않으면 싫어욧！」
			PRINTFORMDW 끈적거리는 물소리를 일으키면서、조그마한 몸으로 아큐는 열심히 허리를 흔들고 있다……
		ELSE
			PRINTFORMW 「응읏……안 돼、안 돼요……」
			PRINTFORMW 「엉덩이 따위에 싸버리시면、기ー껏 모아놓은 정액님……」
			PRINTFORMW 「전부、무용지물이 되어버린다구요……？」
			PRINTFORMW 「그게 아니면、파파는 보지보다……엉덩이 구멍이 더 좋으신건가효……？」
			PRINTFORMDW 나이에 걸맞지 않는 여자의 얼굴로、아큐는 유유히 %CALLNAME:MASTER%의 물건을 집어 삼키고 있다……
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「느긋하게 자극하는 게 안타까워요……」
		PRINTFORMW 「그치만、그 얼굴……좀 더 확실하게、익혀 주세요」
		PRINTFORMW 「제대로 참지 않으시면……또、심한 꼴이 될 거에요？」
		RETURN LOCAL:1
	ENDIF
ENDIF

;%CALLNAME:TARGET%는 상냥하게 미소지으면서애무를 계속했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1, 가게해줘 준다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;ACT분류, 애무
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 「네에、제ー대로 참으신다면」
		PRINTFORMW 「얼마든지、극락 절정을 맛보게 해 드릴게요」
		PRINTFORMW 「그러니까……제가 하는 말을 자ー알 들으세요……알았죠？」
		PRINTFORMDW 끈적끈적한 애무로、아큐는 %CALLNAME:MASTER%의 성감을 끌어올리고 있다……
		PRINTFORMDW 녹아버릴 듯이 미소짓는 소녀에게、%CALLNAME:MASTER%(은)는 몸을 떨었다。
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성봉") || TEQUIP:성교봉사중
		IF V_SEX(MASTER)
			PRINTFORMW 「아큐의、몸은、어떠신가요……？」
			PRINTFORMW 「아읏……칭찬해 주시니、기뻐、요♪　당신♪」
			PRINTFORMW 「아큐웃、힘낼게요♪」
			PRINTFORMDW 아큐는 빙긋 웃고、허리를 흔들며 %CALLNAME:MASTER%의 질내사정을 조르고 있다……
			PRINTFORMDW 꾸욱꾸욱 하고 꾸물거리는 질이、빨아들이는 것처럼 남근을 몰아세우고 있다……
		ELSE
			PRINTFORMW 「믿을 수가 없어요……모처럼의 정액、엉덩이에 내버리다니……」
			PRINTFORMW 「싫엇、그치만、기분 좋아요……♪」
			PRINTFORMW 「자지로、엉덩이 구멍、쿠포옷♪　하고 벌려져버려요오♪」
			PRINTFORMDW 항학의 쾌감에 열중하고 있는지、아큐의 말은 엉망진창이다。
			PRINTFORMDW 저질스러울 정도로 소리를 내는 허리놀림으로、아큐는 %CALLNAME:MASTER%에게서 주도권을 빼앗았다。
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「좋아요……자、제게 몸을 맡기세요……？」
		PRINTFORMW 「힘을 빼고……숨을 들이쉬고……아큐의 목소리 만을、들어 주세요……」
		RETURN LOCAL:1
	ENDIF
ENDIF

;파생 0의 경우의 처리 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 0, 요구를 들은체 만체 한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 0
	SELECTCASE RANDCOM(3, 0)
		CASE 0
;OriginalString : 
			PRINTFORMW 
		CASE 1
;OriginalString : 
			PRINTFORMW 
		CASE 2
;OriginalString : 
			PRINTFORMW 
	ENDSELECT
	RETURN LOCAL
ENDIF
